---
phase: 01-cms-stability-and-base-upgrade
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - project/scripts/smoke-cms-routes.sh
  - project/scripts/smoke-post-rebuild.sh
  - project/PATCHLOG.md
  - project/TEST_REPORT.md
autonomous: true
requirements:
  - CMS-01
  - CMS-02
  - CMS-03
  - INFRA-03

must_haves:
  truths:
    - "smoke-cms-routes.sh exists and tests all 15 critical CMS routes with correct HTTP status expectations"
    - "smoke-post-rebuild.sh exists and tests admin login, kiosk product display, and CMS health endpoint"
    - "Both scripts are executable (chmod +x), use LF line endings, and run without errors on a healthy stack"
    - "PATCHLOG.md has a v3.4.6 Phase 1 entry documenting the CMS stability work"
    - "TEST_REPORT.md has a Phase 1 section stub ready to receive smoke results"
  artifacts:
    - path: "project/scripts/smoke-cms-routes.sh"
      provides: "Automated curl-based route verification for all 15 Strapi content type APIs"
      min_lines: 40
    - path: "project/scripts/smoke-post-rebuild.sh"
      provides: "Post-rebuild verification for admin login, kiosk products, CMS health"
      min_lines: 30
    - path: "project/PATCHLOG.md"
      provides: "Phase 1 patch entry documenting what/why/risk/rollback"
      contains: "v3.4.6"
    - path: "project/TEST_REPORT.md"
      provides: "Phase 1 test entry stub"
      contains: "Phase 1"
  key_links:
    - from: "project/scripts/smoke-cms-routes.sh"
      to: "http://127.0.0.1:1337/api/*"
      via: "curl with Bearer token auth"
      pattern: "curl.*Authorization.*Bearer"
    - from: "project/scripts/smoke-post-rebuild.sh"
      to: "http://127.0.0.1:1337/_health"
      via: "curl, expects HTTP 204"
      pattern: "curl.*_health"
---

<objective>
Write the two smoke scripts that Wave 2 plans depend on, and create the required PATCHLOG/TEST_REPORT entries for Phase 1.

Purpose: The VALIDATION.md designates these two shell scripts as Wave 0 dependencies — they must exist before the rebuild plan runs on the VPS. Without them, the executor has no automated way to verify CMS route health after the rebuild.

Output:
- project/scripts/smoke-cms-routes.sh — curls all 15 critical Strapi API routes + singleType routes, prints PASS/FAIL, exits non-zero if any route fails
- project/scripts/smoke-post-rebuild.sh — verifies CMS health, admin login, and kiosk product endpoint
- PATCHLOG.md v3.4.6 entry
- TEST_REPORT.md Phase 1 stub entry
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-cms-stability-and-base-upgrade/01-RESEARCH.md
@.planning/phases/01-cms-stability-and-base-upgrade/01-VALIDATION.md

<interfaces>
<!-- CMS routes to verify. Source: RESEARCH.md "Post-rebuild route smoke check" section + ROADMAP.md Phase 1 success criteria. -->

collectionType routes (GET /api/{plural} → expect 200 or 403/401 if auth required):
  products, orders, customers, ingredients, payments, delivery-assignments,
  funnel-events, inbound-messages, feedbacks, suppliers, loyalty-tiers,
  marketing-campaigns, delivery-zones

singleType routes (GET /api/{singular} → expect 200 or 403/401):
  system-config, restaurant-brand

Custom handler routes (not DB-backed — test separately):
  GET /api/control-plane/status  (auth: users-permissions)
  GET /api/metrics               (auth: users-permissions)
  GET /api/realtime/orders/stream (auth: false — token query param)

CMS health endpoint: GET http://127.0.0.1:1337/_health  → expect HTTP 204 (Strapi 5 standard)

Auth endpoint (to get JWT for authenticated route checks):
  POST http://127.0.0.1:1337/api/auth/local
  Body: {"identifier":"<STRAPI_EMAIL>","password":"<STRAPI_PASSWORD>"}
  Returns: {"jwt":"..."}

Admin dashboard login (via gateway proxy):
  POST https://api.srv1258231.hstgr.cloud/v1/portal/api/auth/local
  Or directly: POST http://127.0.0.1:1337/api/auth/local (from VPS localhost)

Kiosk product endpoint (via gateway):
  GET https://api.srv1258231.hstgr.cloud/v1/strapi/api/products  → expect 200 + data array

singleType URL rule: Strapi 5 uses singularName for singleType, NOT plural.
  GET /api/system-config  (NOT /api/system-configs)
  GET /api/restaurant-brand  (NOT /api/restaurant-brands)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write smoke-cms-routes.sh</name>
  <files>project/scripts/smoke-cms-routes.sh</files>
  <action>
Create project/scripts/smoke-cms-routes.sh as a bash script with LF line endings (important: write with Unix line endings, not CRLF). The script must:

1. Accept optional positional args: $1 = CMS_URL (default: http://127.0.0.1:1337), $2 = STRAPI_EMAIL (default: $STRAPI_EMAIL env var), $3 = STRAPI_PASSWORD (default: $STRAPI_PASSWORD env var)

2. Obtain a JWT token via:
   curl -s -X POST "${CMS_URL}/api/auth/local" \
     -H 'Content-Type: application/json' \
     -d "{\"identifier\":\"${EMAIL}\",\"password\":\"${PASS}\"}" | jq -r '.jwt'
   If jq is not available, use python3 -c "import sys,json; print(json.load(sys.stdin)['jwt'])"
   If token is empty, print "ERROR: Could not obtain JWT — check credentials" and exit 2

3. Test collectionType routes (expect 200 — authenticated):
   products, orders, customers, ingredients, payments, delivery-assignments,
   funnel-events, inbound-messages, feedbacks, suppliers, loyalty-tiers,
   marketing-campaigns, delivery-zones

4. Test singleType routes (expect 200 — authenticated):
   system-config, restaurant-brand

   For each route: curl -s -o /dev/null -w "%{http_code}" -H "Authorization: Bearer $TOKEN" "${CMS_URL}/api/${route}"
   Print "PASS  <route>  (<status>)" or "FAIL  <route>  (<status>)"

5. Test custom handler routes (these use non-standard paths):
   - GET ${CMS_URL}/api/control-plane/status  (with auth header) — expect 200
   - GET ${CMS_URL}/api/metrics  (with auth header) — expect 200 or 401 (acceptable)
   Note: realtime/orders/stream is SSE — skip in smoke test (would hang curl)

6. Track PASS_COUNT and FAIL_COUNT. At end:
   - Print summary: "Results: N/M passed"
   - If FAIL_COUNT > 0, exit 1 (allows CI to detect failures)
   - If FAIL_COUNT == 0, exit 0

Do NOT include any credentials hardcoded in the script. Use env vars and positional args only.
Script shebang: #!/usr/bin/env bash
  </action>
  <verify>
    <automated>bash -n "$(pwd)/project/scripts/smoke-cms-routes.sh" && echo "SYNTAX OK"</automated>
  </verify>
  <done>
    File exists at project/scripts/smoke-cms-routes.sh, passes bash -n syntax check, contains all 15 route checks (13 collectionType + 2 singleType), uses curl with Authorization Bearer header, exits 1 on any failure.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write smoke-post-rebuild.sh</name>
  <files>project/scripts/smoke-post-rebuild.sh</files>
  <action>
Create project/scripts/smoke-post-rebuild.sh as a bash script with LF line endings. The script covers INFRA-03 verification: it checks that a freshly rebuilt container stack works end-to-end.

Script must:

1. Accept optional args: $1 = BASE_URL (default: https://api.srv1258231.hstgr.cloud), $2 = CMS_INTERNAL_URL (default: http://127.0.0.1:1337), $3 = STRAPI_EMAIL, $4 = STRAPI_PASSWORD

2. Check 1 — CMS health endpoint:
   curl -s -o /dev/null -w "%{http_code}" "${CMS_INTERNAL_URL}/_health"
   Expect: 204
   Print: "PASS  CMS health (204)" or "FAIL  CMS health (got: <status>)"
   Note: Strapi 5 returns HTTP 204 (no body) for /_health when online.

3. Check 2 — CMS login (Strapi users-permissions auth):
   POST ${CMS_INTERNAL_URL}/api/auth/local with test credentials
   Expect: HTTP 200 + non-empty jwt field in response
   Print: "PASS  CMS login (JWT obtained)" or "FAIL  CMS login (<status>)"

4. Check 3 — Kiosk products via gateway:
   GET ${BASE_URL}/v1/strapi/api/products
   Expect: HTTP 200 + "data" array in response body (check with grep or jq)
   Print: "PASS  kiosk products via gateway (200, data present)" or "FAIL  kiosk products (<status>)"

5. Check 4 — Admin login via gateway:
   POST ${BASE_URL}/v1/portal/api/auth/local (or /v1/strapi/api/auth/local if portal not configured)
   Expect: HTTP 200 + jwt in response
   Print: "PASS  admin login via gateway (200, JWT)" or "FAIL  admin login (<status>)"

6. Track PASS/FAIL counts, print summary, exit 1 if any failures, exit 0 if all pass.

Shebang: #!/usr/bin/env bash
Do NOT hardcode credentials.
  </action>
  <verify>
    <automated>bash -n "$(pwd)/project/scripts/smoke-post-rebuild.sh" && echo "SYNTAX OK"</automated>
  </verify>
  <done>
    File exists at project/scripts/smoke-post-rebuild.sh, passes bash -n, contains all 4 checks (CMS health, CMS login, kiosk products, admin login), exits 1 on any failure.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add PATCHLOG and TEST_REPORT entries</name>
  <files>project/PATCHLOG.md, project/TEST_REPORT.md</files>
  <action>
Prepend a new entry to project/PATCHLOG.md immediately after the `# PATCHLOG — RESTO BOT` heading (before the existing v3.4.5 entry). Read the file first, then write the updated version. The entry:

```
## v3.4.6 — Phase 1: CMS Stability & Base Upgrade (2026-03-18)

### What
- Eliminate the docker-cp runtime injection hack: all 42 Strapi content type routes now exist in TypeScript source under inventory-cms/src/api/ and survive any container rebuild
- Verify Node.js 20-alpine base image is in place for all services (admin-dashboard, kiosk-app, inventory-cms Dockerfiles)
- Clean CMS image rebuild with --no-cache to prove routes are baked in (not injected at runtime)
- Add smoke test scripts: scripts/smoke-cms-routes.sh and scripts/smoke-post-rebuild.sh

### Why
P0 blocker: prior approach injected compiled JS into a running container via docker cp. Any container restart or rebuild lost all 15 custom routes, causing CMS and dependent services to fail silently.

### Risk
- CMS rebuild takes 15-30 min on VPS (2 CPU); npm ci downloads ~500MB of packages
- ENOSPC risk on 119GB VPS — disk pre-check required (minimum 10GB free)
- Requires nginx -s reload after CMS restart to flush DNS cache

### Rollback
If rebuilt CMS container fails health check:
  docker compose stop cms
  docker tag current-cms:previous current-cms:rollback-20260318
  docker compose up -d cms  (uses previous image if not pruned)
  docker exec current-gateway-1 nginx -s reload
If rebuild fails mid-build: previous running container is unaffected; docker compose does not replace it until successful build + up -d.
```

Then prepend a new entry to project/TEST_REPORT.md after the `# TEST_REPORT — RESTO BOT` heading:

```
## Phase 1 — CMS Stability & Base Upgrade (2026-03-18)

### Static Checks (Node.js Base Images)

| Check | Command | Result |
|-------|---------|--------|
| admin-dashboard Dockerfile | `grep "FROM node:20" project/admin-dashboard/Dockerfile` | PENDING |
| kiosk-app Dockerfile | `grep "FROM node:20" project/kiosk-app/Dockerfile` | PENDING |
| inventory-cms Dockerfile | `grep "FROM node:20" project/inventory-cms/Dockerfile` | PENDING |

### CMS Route Smoke Test (post-rebuild)

| Test | Command | Result |
|------|---------|--------|
| All 15 CMS routes return 200 | `bash project/scripts/smoke-cms-routes.sh` | PENDING |
| Admin login + kiosk products | `bash project/scripts/smoke-post-rebuild.sh` | PENDING |

*Results to be filled in after Plan 02 rebuild executes.*
```

Both files must be written with actual content preserved (read first, prepend, write full updated content).
  </action>
  <verify>
    <automated>grep -c "v3.4.6" "$(pwd)/project/PATCHLOG.md" && grep -c "Phase 1 — CMS" "$(pwd)/project/TEST_REPORT.md"</automated>
  </verify>
  <done>
    PATCHLOG.md contains "v3.4.6" entry with what/why/risk/rollback. TEST_REPORT.md contains "Phase 1 — CMS Stability" section with table rows marked PENDING. Both files retain all prior content.
  </done>
</task>

</tasks>

<verification>
After all three tasks complete:
1. bash -n project/scripts/smoke-cms-routes.sh — must exit 0
2. bash -n project/scripts/smoke-post-rebuild.sh — must exit 0
3. grep "v3.4.6" project/PATCHLOG.md — must match
4. grep "Phase 1" project/TEST_REPORT.md — must match
5. Confirm both .sh files use LF endings (not CRLF): file project/scripts/smoke-cms-routes.sh should show "ASCII text" not "CRLF"
</verification>

<success_criteria>
- smoke-cms-routes.sh exists, passes bash -n, covers all 15 routes (13 collection + 2 singleType), exits 1 on failure
- smoke-post-rebuild.sh exists, passes bash -n, covers 4 checks (health, login, products, admin login), exits 1 on failure
- PATCHLOG.md v3.4.6 entry present with rollback section
- TEST_REPORT.md Phase 1 section present with PENDING rows
</success_criteria>

<output>
After completion, create `.planning/phases/01-cms-stability-and-base-upgrade/01-01-SUMMARY.md`
</output>
