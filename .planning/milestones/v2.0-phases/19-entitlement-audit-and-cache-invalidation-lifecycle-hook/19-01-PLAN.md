---
phase: 19-entitlement-audit-and-cache-invalidation-lifecycle-hook
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/adr/0003-entitlement-audit-placement.md
  - db/migrations-strapi/2026-06-20_entitlement_audit_uuid.sql
autonomous: true
requirements: [AUD-01]
user_setup: []

must_haves:
  truths:
    - "An ADR (docs/adr/0003-entitlement-audit-placement.md) records the cross-DB resolution: entitlement_audit_log lives in the strapi DB (created by db/migrations-strapi/2026-04-06_saas_modules_entitlements.sql:114-126) and the writer uses raw Knex strapi.db.connection('entitlement_audit_log') because the table is NOT a Strapi content type (no schema.json / UID exists) — so strapi.db.query('api::…') is impossible; NO cross-DB connection, NO n8n-DB writer, NO table move"
    - "The ADR fixes the cache-key contract Phase 20 (GRD-01) consumes: ralphe:entitlement:{tenant_id}:{module_key} (ROADMAP:147), exact-key DEL on the entitlement path; and records the O-1 product-module decision (audit-only invalidation, TTL-bounded) and the O-2 bulk decision (single-row now, *Many out of scope) as accepted, reasoned positions"
    - "A new idempotent migration db/migrations-strapi/2026-06-20_entitlement_audit_uuid.sql migrates entitlement_audit_log.tenant_id VARCHAR(255)→uuid (guarded ALTER … TYPE uuid USING tenant_id::uuid, only when the column is not already uuid) and adds a nullable FK to tenants(tenant_id) live-safely (ADD CONSTRAINT … NOT VALID then VALIDATE CONSTRAINT), mirroring admin_audit_log.tenant_id (decision O-3 = migrate now)"
    - "The migration is fully idempotent (applies cleanly TWICE on an ephemeral Postgres = no-op) and the live prod apply (with the LIVE tenant UUID, never hardcoded …0001) is marked 🔴 VPS-deferred in a header note"
  artifacts:
    - path: "docs/adr/0003-entitlement-audit-placement.md"
      provides: "Cross-DB placement decision (strapi DB + raw-Knex writer) + cache-key contract + O-1/O-2/O-3 decisions"
      min_lines: 60
      contains: "ralphe:entitlement:"
    - path: "db/migrations-strapi/2026-06-20_entitlement_audit_uuid.sql"
      provides: "entitlement_audit_log.tenant_id VARCHAR→uuid + nullable FK to tenants, idempotent + live-safe"
      contains: "uuid"
  key_links:
    - from: "docs/adr/0003-entitlement-audit-placement.md"
      to: "strapi.db.connection('entitlement_audit_log') raw-Knex writer (19-02)"
      via: "ADR records the writer mechanism + the table-is-not-a-content-type reason"
      pattern: "strapi.db.connection|raw Knex"
    - from: "db/migrations-strapi/2026-06-20_entitlement_audit_uuid.sql"
      to: "strapi-DB entitlement_audit_log.tenant_id"
      via: "guarded ALTER … TYPE uuid USING tenant_id::uuid + ADD CONSTRAINT … NOT VALID / VALIDATE"
      pattern: "ALTER TABLE entitlement_audit_log"
---

<objective>
Record the cross-DB placement decision for `entitlement_audit_log` (Phase 19 success-criterion 2) in an
ADR, and land the deferred `entitlement_audit_log.tenant_id` `VARCHAR(255)→uuid` + nullable-FK migration
that ADR 0001 (line 101) assigned to Phase 19 (decision O-3 = migrate now). The ADR is the authoritative
record that the table stays in the strapi DB and the writer is raw Knex `strapi.db.connection` (because
the table is NOT a Strapi content type — `strapi.db.query('api::…')` is impossible), and it locks the
`ralphe:entitlement:{tenant_id}:{module_key}` cache-key contract Phase 20 (GRD-01) consumes.

Purpose: The two hardest "open questions" of this phase are settled by decisions, not code — where the
audit table lives, how it is written, and the exact cache key. Writing them down in an ADR + a concrete
migration is what makes 19-02 (the hook) a mechanical execution against a settled placement, and the
uuid migration is the DB-level enforcement point the writer's canonical-UUID validation parallels.

Output: One ADR markdown + one idempotent `db/migrations-strapi/` SQL file. This plan owns ONLY those
two files — fully disjoint from 19-02 (the `lifecycles.ts` + `audit-hook.ts`) and 19-03 (the CI
fixtures/assertions/yml + node-test + harness), so it runs as an independent executor pass.

🔴 VPS DEFERRED: applying `2026-06-20_entitlement_audit_uuid.sql` to the production **strapi** DB (using
the LIVE tenant UUID discovered on prod per ADR 0001 — never hardcode `…0001`) is deferred to a
prod-connected session. Nothing in this plan touches prod.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-entitlement-audit-and-cache-invalidation-lifecycle-hook/19-RESEARCH.md

<read_first>
- `19-RESEARCH.md` section 2 (Cross-DB Resolution — the A/B/C options table + the evidence chain),
  section 5 (cache-key contract `ralphe:entitlement:{tenant_id}:{module_key}` + the O-1 product-module
  decision), the "Open Questions" block (O-1/O-2/O-3 with the recommended defaults), and "🔴 VPS
  Deferrals".
- `db/migrations-strapi/2026-04-06_saas_modules_entitlements.sql:114-126` — the `entitlement_audit_log`
  DDL (`tenant_id VARCHAR(255) NOT NULL`, the index `idx_entitlement_audit_tenant`).
- `docs/adr/0001-canonical-tenant-key.md:88-104` — the "migrate to uuid + nullable FK in Phase 19"
  promise + the `admin_audit_log.tenant_id uuid NULL REFERENCES tenants(tenant_id)` precedent
  (`db/bootstrap.sql:987-988`) the FK mirrors; the "never hardcode …0001 on prod" rule.
- `docs/adr/0002-tenant-id-fallback-inventory.md` and `docs/adr/0001-…md:1-8` — the ADR header
  style to mirror (`# ADR NNNN: …`, `**Status:** Accepted`, `**Date:**`, `**Phase:**`, `**Requirement:**`).
- `db/migrations-strapi/2026-06-20_strapi_order_customer_tenant.sql:1-30` — the strapi-DB migration
  header + idempotency-note + `SET lock_timeout`/`statement_timeout` + 🔴-VPS-note style to mirror.
</read_first>

<interfaces>
<!-- Verified facts to BAKE INTO the ADR + migration (re-confirm by reading the sources above). -->

Cross-DB resolution (from 19-RESEARCH §2 — Option A CHOSEN):
  - entitlement_audit_log is in the STRAPI DB (created by db/migrations-strapi/2026-04-06_…sql:114-126,
    the PGDATABASE=strapi pass). Strapi connects to that DB by default (config/database.ts DATABASE_NAME
    default 'strapi'). A lifecycle runs inside that Strapi/Knex connection -> writes the same DB.
  - The table is NOT a Strapi content type — no schema.json / UID exists for it (grep-confirmed in
    research). Therefore strapi.db.query('api::…') is IMPOSSIBLE; the writer MUST be raw Knex
    strapi.db.connection('entitlement_audit_log').insert({...}) (precedent: agent-chat.ts:107-109,
    control-plane.ts:76).
  - Option B (move to n8n DB) and Option C (explicit n8n-DB connection) REJECTED — the entitlement
    source-of-truth (tenant_entitlements) is in the strapi DB; co-locating the audit there is correct;
    a 2nd connection adds infra for no benefit.

Cache-key contract (from 19-RESEARCH §5 — locked by ROADMAP:147):
  - ralphe:entitlement:{tenant_id}:{module_key}  (exact key — the DEL must match Phase-20 GRD-01's GET
    byte-for-byte or revocation won't invalidate -> security regression). {tenant_id} = canonical UUID
    string; {module_key} = tenant-entitlement.module_key (= product-module.key).

Open-question decisions to RECORD as accepted in the ADR:
  - O-1: product-module change = AUDIT-ONLY invalidation (write the row, do NOT global-flush); the
    ≤5-min positive TTL (Phase 20) bounds staleness. Avoids a prod SCAN/KEYS. (Recorded as a
    TTL-bounded known gap.)
  - O-2: single-row hooks now; updateMany/deleteMany bulk audit is OUT OF SCOPE this milestone
    (no self-serve bulk grant exists).
  - O-3: migrate the tenant_id VARCHAR→uuid NOW (the phase ADR 0001 named); 🔴 VPS apply deferred.

uuid migration shape (from O-3 + ADR 0001:101 + the admin_audit_log precedent):
  - guarded ALTER … TYPE uuid USING tenant_id::uuid (only run when the column is not already uuid —
    check pg_catalog/information_schema so a re-run is a no-op).
  - nullable FK to tenants(tenant_id), live-safe: ADD CONSTRAINT … NOT VALID, then VALIDATE CONSTRAINT
    (so the live apply does not take a long ACCESS EXCLUSIVE lock / does not fail on legacy rows).
  - idempotent: every step guarded so applying twice on a fresh ephemeral PG = no-op.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write ADR 0003 — entitlement-audit placement, raw-Knex writer, cache-key contract</name>
  <files>docs/adr/0003-entitlement-audit-placement.md</files>
  <read_first>19-RESEARCH.md §2 (cross-DB resolution table + evidence chain), §5 (cache-key contract +
  O-1), "Open Questions" (O-1/O-2/O-3); docs/adr/0001-…md:1-8 + 88-104 (header style + the Phase-19
  uuid promise); docs/adr/0002-…md (header style).</read_first>
  <action>
Create `docs/adr/0003-entitlement-audit-placement.md` matching the ADR 0001/0002 header style:
  `# ADR 0003: Entitlement Audit-Log Placement + Raw-Knex Writer + Cache-Key Contract`
  `**Status:** Accepted` / `**Date:** 2026-06-20` / `**Phase:** 19` / `**Requirement:** AUD-01, AUD-02`

Sections (re-read the sources and cite file:line):
  1. `## Context` — the table exists but has ZERO writers (dead since the Phase-16 migration); ROADMAP
     success-criterion 2 asks the cross-DB write question; ADR 0001:101 promised the uuid migration here.
  2. `## Decision 1 — The audit table stays in the strapi DB; the writer is raw Knex.` Record:
     - `entitlement_audit_log` is created by `db/migrations-strapi/2026-04-06_saas_modules_entitlements.sql:114-126`
       (the PGDATABASE=strapi pass) and Strapi connects to that same DB by default
       (`inventory-cms/config/database.ts` DATABASE_NAME default `strapi`). A lifecycle runs inside that
       Knex connection -> NO cross-DB write, NO 2nd/n8n-DB connection, NO table move.
     - The table is NOT a Strapi content type (no `schema.json`/UID), so `strapi.db.query('api::…')` is
       IMPOSSIBLE — the writer MUST be raw Knex `strapi.db.connection('entitlement_audit_log').insert({...})`
       (precedent: `agent-chat.ts:107-109`, `control-plane.ts:76`).
     - Include the Option A (chosen) / B (move to n8n DB) / C (explicit n8n-DB connection) table from
       research §2 with the one-line rejection rationale for B and C.
  3. `## Decision 2 — Cache-key contract (Phase-20 GRD-01).` State the canonical key
     `ralphe:entitlement:{tenant_id}:{module_key}` (cite ROADMAP:147), that the hook issues an exact-key
     `DEL` (O(1), no SCAN/KEYS on the hot path), and that it MUST match the Phase-20 guard's GET
     byte-for-byte (else a stale grant survives revocation — the exact AUD-02 regression).
  4. `## Decision 3 — product-module = audit-only invalidation (O-1).` product-module lifecycle WRITES
     the audit row but does NOT global-flush; the ≤5-min positive TTL (Phase 20) bounds staleness —
     recorded as a TTL-bounded known gap; the `SCAN *:{key}` fan-out is the documented scale-out option
     if a later phase shows staleness matters.
  5. `## Decision 4 — single-row audit now; bulk *Many out of scope (O-2).`
  6. `## Decision 5 — migrate tenant_id VARCHAR→uuid now (O-3).` Supersede the ADR 0001:90 "KEEP
     VARCHAR through Phase 15" with "migrate now in Phase 19" — point at the
     `2026-06-20_entitlement_audit_uuid.sql` migration (Task 2), mirroring
     `admin_audit_log.tenant_id uuid NULL REFERENCES tenants(tenant_id)` (`db/bootstrap.sql:987-988`).
  7. `## Fail-loud posture` — validate `tenant_id` to a canonical UUID and THROW before insert
     (pre-write, loud); the post-commit audit insert + cache DEL log-at-error/warn + increment a counter
     (after-hooks fire post-commit so a throw can't roll back the grant — explain this); never a bare
     `continueOnFail`/`.catch(()=>{})`.
  8. `## 🔴 VPS Deferrals` — apply the uuid migration on the prod strapi DB with the LIVE tenant UUID
     (never `…0001`); rebuild the CMS; confirm the prod Redis the DEL targets == the Phase-20 guard's.
  9. `## Consequences` — 19-02 implements a raw-Knex writer + exact-key DEL against this settled
     placement; the DB enforces the uuid plane.
  </action>
  <verify>
    <automated>f=docs/adr/0003-entitlement-audit-placement.md; test -f "$f" || { echo "FAIL: ADR missing"; exit 1; }; grep -qi "strapi DB\|strapi database" "$f" || { echo "FAIL: strapi-DB placement decision missing"; exit 1; }; grep -qiE "raw Knex|strapi\.db\.connection" "$f" || { echo "FAIL: raw-Knex writer decision missing"; exit 1; }; grep -qi "not a Strapi content type\|no schema.json\|strapi.db.query" "$f" || { echo "FAIL: not-a-content-type rationale missing"; exit 1; }; grep -q "ralphe:entitlement:" "$f" || { echo "FAIL: cache-key contract missing"; exit 1; }; grep -qi "audit-only\|TTL-bounded\|O-1" "$f" || { echo "FAIL: O-1 product-module decision missing"; exit 1; }; grep -qiE "VARCHAR.*uuid|uuid migration|VARCHAR\(255\)" "$f" || { echo "FAIL: O-3 uuid migration decision missing"; exit 1; }; grep -qi "VPS" "$f" || { echo "FAIL: VPS deferral note missing"; exit 1; }; lc=$(wc -l < "$f"); [ "$lc" -ge 60 ] || { echo "FAIL: ADR too short ($lc lines)"; exit 1; }; echo "PASS ADR 0003 records placement + writer + cache-key + O-1/O-2/O-3"</automated>
  </verify>
  <done>
`docs/adr/0003-entitlement-audit-placement.md` exists (≥60 lines, Accepted, Phase 19, AUD-01/AUD-02),
records the strapi-DB placement + raw-Knex-writer (with the not-a-content-type rationale + the A/B/C
table), the `ralphe:entitlement:{tenant_id}:{module_key}` cache-key contract, and the O-1 (audit-only),
O-2 (single-row), O-3 (migrate-now) decisions, plus the fail-loud posture and the 🔴 VPS deferrals.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write the idempotent, live-safe entitlement_audit_log.tenant_id uuid migration</name>
  <files>db/migrations-strapi/2026-06-20_entitlement_audit_uuid.sql</files>
  <read_first>19-RESEARCH.md "Open Questions" O-3 + "🔴 VPS Deferrals"; ADR 0001:101 (the uuid + nullable
  FK promise) + the admin_audit_log precedent (db/bootstrap.sql:987-988); the existing
  db/migrations-strapi/2026-04-06_saas_modules_entitlements.sql:114-126 (current VARCHAR(255) DDL) and
  db/migrations-strapi/2026-06-20_strapi_order_customer_tenant.sql:1-30 (header/idempotency/lock-timeout
  + 🔴-VPS-note style to mirror).</read_first>
  <action>
Create `db/migrations-strapi/2026-06-20_entitlement_audit_uuid.sql` (target: the **strapi** DB). Header
comment block mirroring `2026-06-20_strapi_order_customer_tenant.sql`: a 🔴 VPS-DEFERRED note (alters the
LIVE strapi-DB `entitlement_audit_log`; apply via the migrations-strapi PGDATABASE=strapi pass; on prod
use the LIVE tenant UUID per ADR 0001, **never** `…0001`), a SAFE-TO-RE-RUN idempotency note, and the
background (ADR 0001:101 assigned this VARCHAR→uuid + nullable-FK migration to Phase 19). Then
`SET lock_timeout = '5s'; SET statement_timeout = '120s';` like the sibling migration.

Steps, all idempotent + live-safe:

1. `-- STEP 1: tenant_id VARCHAR(255) -> uuid, only if not already uuid (guarded -> re-run is a no-op).`
   Use a `DO $$ BEGIN IF EXISTS (SELECT 1 FROM information_schema.columns WHERE table_name =
   'entitlement_audit_log' AND column_name = 'tenant_id' AND data_type <> 'uuid') THEN
   EXECUTE 'ALTER TABLE entitlement_audit_log ALTER COLUMN tenant_id TYPE uuid USING tenant_id::uuid';
   END IF; END $$;`  (The `USING tenant_id::uuid` cast converts existing canonical-UUID strings; any
   non-UUID legacy value would error LOUDLY — which is correct, it must be cleaned first. Note that in
   a DO block. Do NOT use SAVEPOINT/ROLLBACK TO inside the DO block.)

2. `-- STEP 2: nullable FK to tenants(tenant_id), live-safe (NOT VALID then VALIDATE).` Add the
   constraint only if absent, mirroring `admin_audit_log.tenant_id uuid NULL REFERENCES tenants`:
   `DO $$ BEGIN IF NOT EXISTS (SELECT 1 FROM pg_constraint WHERE conname =
   'fk_entitlement_audit_tenant') THEN
   EXECUTE 'ALTER TABLE entitlement_audit_log ADD CONSTRAINT fk_entitlement_audit_tenant
   FOREIGN KEY (tenant_id) REFERENCES tenants(tenant_id) NOT VALID'; END IF; END $$;`
   then a separate guarded `ALTER TABLE entitlement_audit_log VALIDATE CONSTRAINT
   fk_entitlement_audit_tenant;` (VALIDATE on an already-validated constraint is a no-op).
   Keep the column NULLABLE (the FK is nullable so seed/migration writes with no tenant context, and
   the existing data, are tolerated — parity with admin_audit_log).

3. `-- STEP 3 (idempotent re-assert):` keep the existing
   `CREATE INDEX IF NOT EXISTS idx_entitlement_audit_tenant ON entitlement_audit_log (tenant_id, created_at DESC);`
   so the index survives the type change (it does, but re-assert idempotently).

4. A trailing note: `-- 🔴 VPS: on prod, the FK target tenants(tenant_id) must exist in the strapi DB.
   -- If tenants is NOT present in the strapi DB on prod, the FK step is skipped (the guard above no-ops)
   -- and the type migration still applies — record this in the prod-apply runbook.` (Make the FK step
   ALSO guard on `to_regclass('tenants') IS NOT NULL` so a strapi DB without a local `tenants` table does
   not fail — the CI fixture in 19-03 creates a minimal `tenants` so the FK path is exercised.)

The file MUST be valid SQL that applies cleanly TWICE against a fresh ephemeral Postgres
`entitlement_audit_log` table (idempotency). Do NOT use `SAVEPOINT`/`ROLLBACK TO SAVEPOINT` inside any
`DO` block (Phase 18 §5 pitfall — illegal in a DO block).
  </action>
  <verify>
    <automated>f=db/migrations-strapi/2026-06-20_entitlement_audit_uuid.sql; test -f "$f" || { echo "FAIL: migration missing"; exit 1; }; grep -qi "TYPE uuid" "$f" || { echo "FAIL: no VARCHAR->uuid type change"; exit 1; }; grep -qi "tenant_id::uuid" "$f" || { echo "FAIL: no USING cast"; exit 1; }; grep -qi "NOT VALID" "$f" || { echo "FAIL: no live-safe NOT VALID FK"; exit 1; }; grep -qi "VALIDATE CONSTRAINT" "$f" || { echo "FAIL: no VALIDATE step"; exit 1; }; grep -qi "information_schema\|pg_constraint\|to_regclass" "$f" || { echo "FAIL: no idempotency guard"; exit 1; }; if grep -qiE "SAVEPOINT|ROLLBACK TO" "$f"; then echo "FAIL: SAVEPOINT in DO block (illegal)"; exit 1; fi; grep -qi "VPS" "$f" || { echo "FAIL: no VPS note"; exit 1; }; echo "PASS uuid migration structural checks (run the ephemeral-PG apply-twice idempotency check from 19-VALIDATION.md Test Infrastructure to prove re-runnability)"</automated>
  </verify>
  <done>
`db/migrations-strapi/2026-06-20_entitlement_audit_uuid.sql` exists, migrates
`entitlement_audit_log.tenant_id` VARCHAR→uuid via a guarded `ALTER … TYPE uuid USING tenant_id::uuid`
(only when not already uuid), adds a nullable FK to `tenants(tenant_id)` live-safely (`NOT VALID` then
`VALIDATE CONSTRAINT`, guarded on the FK + the `tenants` table existing), re-asserts the tenant index,
uses NO `SAVEPOINT` in any DO block, carries the 🔴 VPS-deferred note, and applies cleanly twice on an
ephemeral Postgres (idempotent — see 19-VALIDATION.md local-run mechanism).
  </done>
</task>

</tasks>

<verification>
- `docs/adr/0003-entitlement-audit-placement.md` exists (≥60 lines): strapi-DB placement + raw-Knex
  writer (with not-a-content-type rationale + A/B/C table), `ralphe:entitlement:{tenant_id}:{module_key}`
  contract, O-1/O-2/O-3 decisions, fail-loud posture, 🔴 VPS deferrals.
- `db/migrations-strapi/2026-06-20_entitlement_audit_uuid.sql` does the guarded VARCHAR→uuid + nullable
  FK (`NOT VALID`→`VALIDATE`), no `SAVEPOINT` in a DO block, idempotent, 🔴 VPS note.
- Idempotency proven by applying the migration TWICE on an ephemeral PG (19-VALIDATION.md mechanism).
- (Cross-plan) Plan 19-03's `19-entitlement-audit-seed.sql` creates the table in the already-uuid shape
  + a minimal `tenants` so the helper test (19-03) and the migration's FK path are both exercisable.
</verification>

<success_criteria>
The cross-DB write question (Phase 19 success-criterion 2) is RESOLVED and documented — the table stays
in the strapi DB and the writer is raw Knex `strapi.db.connection`, recorded in ADR 0003 along with the
locked `ralphe:entitlement:{tenant_id}:{module_key}` cache-key contract and the O-1/O-2/O-3 decisions —
and the `entitlement_audit_log.tenant_id` is migrated VARCHAR→uuid + nullable FK idempotently and
live-safely (decision O-3 = migrate now), with the prod apply 🔴 VPS-deferred.
</success_criteria>

<output>
After completion, create
`.planning/phases/19-entitlement-audit-and-cache-invalidation-lifecycle-hook/19-01-SUMMARY.md`.
</output>
