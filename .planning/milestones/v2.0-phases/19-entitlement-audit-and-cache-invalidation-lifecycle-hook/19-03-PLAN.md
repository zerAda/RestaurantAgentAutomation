---
phase: 19-entitlement-audit-and-cache-invalidation-lifecycle-hook
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - db/ci-fixtures/19-entitlement-audit-seed.sql
  - db/ci-assertions/19-entitlement-audit.sql
  - inventory-cms/src/api/tenant-entitlement/content-types/tenant-entitlement/__tests__/audit-hook.test.mjs
  - scripts/test-phase19.sh
  - .github/workflows/phase-19-assertions.yml
autonomous: true
requirements: [AUD-02]
user_setup: []

must_haves:
  truths:
    - "A node --test file (audit-hook.test.mjs) drives the PURE audit-hook.ts helper (no Strapi boot) against an ephemeral Postgres + ephemeral Redis: it SETs the canonical key ralphe:entitlement:{tenant_id}:{module_key}, runs invalidateCache(), GETs nil (no stale grant survives — AUD-02), and asserts a row is written to entitlement_audit_log per op (created/config_changed/deleted) with old→new captured"
    - "scripts/test-phase19.sh boots ephemeral PG (system postgres user via /usr/lib/postgresql/16/bin) + ephemeral redis-server (/usr/bin/redis-server on a high port), applies the seed, runs the node-test, and tears both down — so the whole helper suite runs locally with docker DOWN"
    - "A psql DO-block assertion file (19-entitlement-audit.sql) proves a row exists per op and that an invalid (non-canonical-UUID) tenant_id is rejected — using nested BEGIN..EXCEPTION (NEVER SAVEPOINT/ROLLBACK TO inside a DO block)"
    - "A CI gate (.github/workflows/phase-19-assertions.yml) mirrors phase-18-assertions.yml and ADDS a redis:7-alpine service: a PG job (seed + audit-row + DO-block assertions) + a Redis job (the node-test invalidation round-trip) + a structural job (greps ralphe:entitlement: in audit-hook.ts and asserts both lifecycles.ts exist) — failing the build on any miss"
  artifacts:
    - path: "db/ci-fixtures/19-entitlement-audit-seed.sql"
      provides: "entitlement_audit_log DDL (already-uuid shape) + minimal tenants + a tenant_entitlements seed under the canonical UUID"
      contains: "entitlement_audit_log"
    - path: "db/ci-assertions/19-entitlement-audit.sql"
      provides: "DO-block: a row per op + invalid-uuid rejection via nested BEGIN..EXCEPTION (no SAVEPOINT)"
      contains: "entitlement_audit_log"
    - path: "inventory-cms/src/api/tenant-entitlement/content-types/tenant-entitlement/__tests__/audit-hook.test.mjs"
      provides: "node --test: SET→invalidateCache→GET-nil on the canonical key + the audit-row write"
      contains: "ralphe:entitlement:"
    - path: "scripts/test-phase19.sh"
      provides: "Local harness: ephemeral PG + ephemeral redis-server, runs the node-test, tears down"
      contains: "redis-server"
    - path: ".github/workflows/phase-19-assertions.yml"
      provides: "CI gate: PG audit job + redis:7-alpine invalidation job + structural hook/key job"
      contains: "redis:7-alpine"
  key_links:
    - from: ".github/workflows/phase-19-assertions.yml"
      to: "db/ci-assertions/19-entitlement-audit.sql + audit-hook.test.mjs"
      via: "psql -f against ephemeral postgres + node --test against redis:7-alpine"
      pattern: "19-entitlement-audit.sql|audit-hook.test.mjs"
    - from: "audit-hook.test.mjs"
      to: "ralphe:entitlement:{tenant_id}:{module_key}"
      via: "SET key -> invalidateCache(redis, tenant_id, module_key) -> GET returns nil"
      pattern: "ralphe:entitlement:"
    - from: "scripts/test-phase19.sh"
      to: "ephemeral redis-server + ephemeral postgres"
      via: "redis-server --port 7390 + initdb/pg_ctl as system postgres user"
      pattern: "redis-server|pg_ctl"
---

<objective>
Create the Phase 19 validation infrastructure (the Wave-0 gate named in `19-VALIDATION.md`): a CI fixture
+ DO-block assertions proving an `entitlement_audit_log` row is written per op (and an invalid tenant_id
is rejected), a `node --test` file that drives the PURE `audit-hook.ts` helper against ephemeral Postgres
+ Redis (SET → `invalidateCache()` → GET nil on the canonical `ralphe:entitlement:{tenant_id}:{module_key}`
key, proving no stale grant survives — AUD-02), a local harness `scripts/test-phase19.sh` that boots both
ephemeral services with docker DOWN, and a GitHub Actions gate mirroring `phase-18-assertions.yml` with an
added `redis:7-alpine` service.

Purpose: This is the machine-checkable proof of AUD-01 (a row per op) and AUD-02 (no stale grant) without
a live VPS and without booting Strapi (whose TS compile is already red). The pure-helper seam is what
makes the test fast; this plan builds the seam's harness so 19-02's helper has a target to satisfy.

Output: `db/ci-fixtures/19-entitlement-audit-seed.sql`, `db/ci-assertions/19-entitlement-audit.sql`,
`inventory-cms/src/api/tenant-entitlement/content-types/tenant-entitlement/__tests__/audit-hook.test.mjs`,
`scripts/test-phase19.sh`, `.github/workflows/phase-19-assertions.yml`. These are DISJOINT from 19-01 (the
ADR + uuid migration) and 19-02 (the `lifecycles.ts` + `audit-hook.ts`), so this runs as an independent
executor pass; its node-test/structural checks pass only once 19-02 lands `audit-hook.ts` — that is by
design (the gate is what proves 19-02).
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-entitlement-audit-and-cache-invalidation-lifecycle-hook/19-RESEARCH.md
@.planning/phases/19-entitlement-audit-and-cache-invalidation-lifecycle-hook/19-VALIDATION.md

<read_first>
- `19-RESEARCH.md` section 7 (Validation Architecture — the pure-helper seam, the test framework table,
  the local ephemeral PG + redis mechanisms, the Phase-Requirements→Test map, the Wave-0 gaps list),
  section 1 (the exact `entitlement_audit_log` columns), section 5 (the canonical cache key + the
  proven SET→DEL→GET-nil round-trip).
- `.github/workflows/phase-18-assertions.yml` — the EXACT pattern to mirror: pinned checkout SHA
  `actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2`, `permissions: contents: read`
  + `pull-requests: read`, `postgres:15-alpine` service (health-cmd, `ports: 5432:5432`), the inline
  "Seed FK parents" psql step, `PGPASSWORD`/`-v ON_ERROR_STOP=1`, `::group::`/`::endgroup::` wrapping,
  the structural `jq`/`grep` job.
- `db/ci-assertions/18-cross-tenant-isolation.sql` — the DO-block / nested `BEGIN..EXCEPTION` /
  `RAISE EXCEPTION`/`RAISE NOTICE` style (and Pitfall 5: NEVER SAVEPOINT/ROLLBACK TO inside a DO block).
- `db/ci-fixtures/18-two-tenant-seed.sql` — the fixture header + `ON CONFLICT DO NOTHING` idempotency style.
- `scripts/test-redis.sh` — the local `redis-server`/`redis-cli` invocation + `ralphe:` key convention.
- `db/migrations-strapi/2026-04-06_saas_modules_entitlements.sql:114-126` — the `entitlement_audit_log`
  column list the seed DDL must reproduce (in the post-19-01 **uuid** shape for `tenant_id`).
</read_first>

<interfaces>
<!-- The audit-hook.ts contract the node-test drives (19-02 implements it; this test is its spec). -->

audit-hook.ts exports (pure, no @strapi/strapi import — only knex/ioredis/zod types):
  deriveAction(oldValue, newValue) -> 'created'|'config_changed'|'enabled'|'disabled'|'expired'|'deleted'
  validateTenantId(tenant_id: string) -> string   (zod z.string().uuid(); throws on a non-canonical value)
  writeAuditRow(knex, { tenant_id, module_key, action, changed_by, old_value, new_value }) -> Promise<void>
  invalidateCache(redis, tenant_id, module_key) -> Promise<void>   (DEL ralphe:entitlement:{t}:{m})

Canonical CI identifiers (parity with Phase 18 + the migration backfill):
  tenant_id (canonical UUID)  00000000-0000-0000-0000-000000000001
  module_key                  channel_whatsapp
  canonical cache key         ralphe:entitlement:00000000-0000-0000-0000-000000000001:channel_whatsapp

entitlement_audit_log columns (from research §1; tenant_id is uuid POST-19-01):
  id SERIAL PK, tenant_id uuid NOT NULL, module_key VARCHAR(255) NOT NULL, action VARCHAR(50) NOT NULL,
  changed_by VARCHAR(255), old_value JSONB, new_value JSONB, created_at TIMESTAMPTZ DEFAULT NOW()

Local ephemeral services (verified 2026-06-20):
  redis-server --port 7390 --daemonize yes --save "" --appendonly no --dir /tmp  (then redis-cli -p 7390)
  postgres via system `postgres` user: /usr/lib/postgresql/16/bin/{initdb -A trust, pg_ctl start -o '-p 55433 -k <sock>'}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the entitlement-audit seed fixture (DDL already-uuid + tenants + entitlement seed)</name>
  <files>db/ci-fixtures/19-entitlement-audit-seed.sql</files>
  <read_first>19-RESEARCH.md §1 (entitlement_audit_log columns); §7 Wave-0 gaps; db/ci-fixtures/18-two-tenant-seed.sql
  (header + ON CONFLICT style); db/migrations-strapi/2026-04-06_saas_modules_entitlements.sql:114-126.</read_first>
  <action>
Create `db/ci-fixtures/19-entitlement-audit-seed.sql` (target: an ephemeral strapi-shaped DB; runnable
under `ON_ERROR_STOP=1`). Header comment matching `18-two-tenant-seed.sql`. It must:
  - `CREATE TABLE IF NOT EXISTS tenants (tenant_id uuid PRIMARY KEY, name text);` and insert the canonical
    tenant `00000000-0000-0000-0000-000000000001` `ON CONFLICT DO NOTHING` (so the FK in 19-01's migration
    AND the audit row's tenant_id resolve).
  - `CREATE TABLE IF NOT EXISTS entitlement_audit_log (...)` in the **post-19-01 uuid shape** — copy the
    column list from `2026-04-06_…sql:114-126` BUT with `tenant_id uuid NOT NULL` (not VARCHAR), plus
    `CREATE INDEX IF NOT EXISTS idx_entitlement_audit_tenant ON entitlement_audit_log (tenant_id, created_at DESC);`
    (This is the post-migration shape the helper writes against — the migration in 19-01 is what brings a
    legacy table to this shape; the fixture starts here so the helper test exercises the target shape.)
  - `CREATE TABLE IF NOT EXISTS tenant_entitlements (id SERIAL PRIMARY KEY, tenant_id uuid NOT NULL,
    module_key VARCHAR(255) NOT NULL, enabled boolean DEFAULT true, expires_at timestamptz);` and insert
    one row: tenant `…0001`, `module_key='channel_whatsapp'`, `enabled=true` — `ON CONFLICT DO NOTHING`
    (the row the helper's create/update/delete audit operates on).
  - Be idempotent (re-running is a no-op). Keep it a single file.
  </action>
  <verify>
    <automated>f=db/ci-fixtures/19-entitlement-audit-seed.sql; test -f "$f" || { echo "FAIL: seed missing"; exit 1; }; grep -qi "entitlement_audit_log" "$f" || { echo "FAIL: audit table DDL missing"; exit 1; }; grep -qi "tenant_id uuid" "$f" || { echo "FAIL: tenant_id not uuid (post-19-01 shape)"; exit 1; }; grep -qi "tenant_entitlements" "$f" || { echo "FAIL: entitlement seed table missing"; exit 1; }; grep -q "channel_whatsapp" "$f" || { echo "FAIL: module_key seed missing"; exit 1; }; grep -q "00000000-0000-0000-0000-000000000001" "$f" || { echo "FAIL: canonical tenant missing"; exit 1; }; grep -qi "ON CONFLICT" "$f" || { echo "FAIL: not idempotent"; exit 1; }; echo "PASS entitlement-audit seed present (uuid shape, canonical tenant, entitlement row)"</automated>
  </verify>
  <done>
`db/ci-fixtures/19-entitlement-audit-seed.sql` creates `tenants` (+ canonical `…0001`),
`entitlement_audit_log` in the post-19-01 **uuid** shape (+ index), and `tenant_entitlements` with one
`channel_whatsapp` row for the canonical tenant — all idempotent (`IF NOT EXISTS`/`ON CONFLICT`).
  </done>
</task>

<task type="auto">
  <name>Task 2: Write the audit-row + invalid-uuid DO-block assertions</name>
  <files>db/ci-assertions/19-entitlement-audit.sql</files>
  <read_first>19-RESEARCH.md §7 (Requirements→Test map: a row per op, invalid-uuid negative) + §6
  (validate-before-insert); db/ci-assertions/18-cross-tenant-isolation.sql (DO-block style + Pitfall 5:
  NEVER SAVEPOINT inside a DO block).</read_first>
  <action>
Create `db/ci-assertions/19-entitlement-audit.sql` (target: the ephemeral DB; run AFTER the seed AND
after the node-test has written its audit rows — see the CI job in Task 5; for the SQL-only DO-block
assertions that don't depend on the helper, INSERT representative rows inline). Header comment matching
the Phase-18 assertion file. DO-blocks, each `RAISE EXCEPTION` on failure (non-zero psql exit under
`ON_ERROR_STOP=1`), `RAISE NOTICE` on pass:

(1) Row-per-op: inline-INSERT three rows simulating the three hooks (action `created` with
    `new_value` set + `old_value` NULL; `config_changed` with both set; `deleted` with `old_value` set +
    `new_value` NULL) for tenant `…0001`, module `channel_whatsapp`, then assert
    `SELECT COUNT(*) FROM entitlement_audit_log WHERE tenant_id='…0001' AND module_key='channel_whatsapp'`
    is `>= 3`; else `RAISE EXCEPTION 'FAIL: not one audit row per op'`. Also assert the `created` row has
    `old_value IS NULL AND new_value IS NOT NULL`, and the `deleted` row has
    `new_value IS NULL AND old_value IS NOT NULL` (old→new captured correctly).

(2) Canonical-UUID enforcement (the DB plane, post-19-01): a nested
    `BEGIN ... EXCEPTION WHEN invalid_text_representation OR others THEN v_failed := true; END;` wrapping
    `INSERT INTO entitlement_audit_log (tenant_id, module_key, action) VALUES ('default','channel_whatsapp','created');`
    (a non-UUID `tenant_id` must be rejected by the `uuid` column type). After the block,
    `IF NOT v_failed THEN RAISE EXCEPTION 'FAIL: non-UUID tenant_id accepted into entitlement_audit_log'`.
    Do NOT use SAVEPOINT/ROLLBACK TO SAVEPOINT (Pitfall 5).

(3) Action vocabulary present: assert at least one row with `action IN ('created','config_changed','deleted')`
    exists; else FAIL (ties the assertion to the deriveAction() output the helper produces).

Use the canonical identifiers from <interfaces>. Each block declares its own vars. Single file under
`ON_ERROR_STOP=1`.
  </action>
  <verify>
    <automated>f=db/ci-assertions/19-entitlement-audit.sql; test -f "$f" || { echo "FAIL: assertions missing"; exit 1; }; grep -qi "entitlement_audit_log" "$f" || { echo "FAIL: audit table not referenced"; exit 1; }; grep -qi "created\|config_changed\|deleted" "$f" || { echo "FAIL: per-op action assertion missing"; exit 1; }; grep -qiE "invalid_text_representation|EXCEPTION WHEN" "$f" || { echo "FAIL: invalid-uuid negative assertion missing"; exit 1; }; grep -qi "old_value IS NULL\|new_value IS NULL" "$f" || { echo "FAIL: old->new capture assertion missing"; exit 1; }; if grep -qiE "SAVEPOINT|ROLLBACK TO" "$f"; then echo "FAIL: SAVEPOINT inside DO block (Pitfall 5)"; exit 1; fi; n=$(grep -c "DO \$\$" "$f"); [ "$n" -ge 2 ] || { echo "FAIL: expected >=2 DO blocks, got $n"; exit 1; }; echo "PASS audit assertions present (row-per-op + invalid-uuid negative, no SAVEPOINT)"</automated>
  </verify>
  <done>
`db/ci-assertions/19-entitlement-audit.sql` has DO-blocks proving a row is written per op (with old→new
captured: created has new-only, deleted has old-only), and a non-UUID `tenant_id` INSERT is rejected via
a nested `BEGIN..EXCEPTION` (NO SAVEPOINT). Each failure raises an EXCEPTION (non-zero exit).
  </done>
</task>

<task type="auto">
  <name>Task 3: Write the node --test helper test + the local ephemeral-services harness</name>
  <files>inventory-cms/src/api/tenant-entitlement/content-types/tenant-entitlement/__tests__/audit-hook.test.mjs, scripts/test-phase19.sh</files>
  <read_first>19-RESEARCH.md §7 (the pure-helper seam + the proven redis SET→DEL→GET-nil round-trip + the
  local ephemeral PG/redis mechanisms); §5 (the canonical key); scripts/test-redis.sh (redis-server/cli
  invocation); the <interfaces> block (the audit-hook.ts export signatures + canonical identifiers).</read_first>
  <action>
Create `inventory-cms/src/api/tenant-entitlement/content-types/tenant-entitlement/__tests__/audit-hook.test.mjs`
— a `node --test` (ESM) file that imports the PURE helper from `../audit-hook.ts` (19-02 will create it;
import via the runtime path the harness uses — since `node --test` cannot import `.ts` directly without a
loader, import from a JS-compiled or use `--experimental-strip-types`/`--import` per the harness; the
SIMPLEST robust path: have the harness invoke `node --test --experimental-strip-types …` (Node 22.22.2
supports type-stripping) OR have the test import named exports and SKIP gracefully (`test.skip`) if
`audit-hook.ts` is not yet present, so this file is committable BEFORE 19-02 lands). The test cases:

  - `deriveAction`: `(null, {enabled:true})` -> 'created'; `({enabled:true},{enabled:false})` -> 'disabled';
    `({enabled:false},{enabled:true})` -> 'enabled'; `({...},null)` -> 'deleted'; a benign field change
    -> 'config_changed'. (Pure, no IO.)
  - `validateTenantId`: `'00000000-0000-0000-0000-000000000001'` returns it; `'default'` THROWS;
    `''` THROWS. (zod, pure.)
  - `invalidateCache` (AUD-02, the load-bearing one): connect to the ephemeral redis on the harness port
    (read `process.env.REDIS_PORT`), `SET ralphe:entitlement:00000000-0000-0000-0000-000000000001:channel_whatsapp 1`,
    call `invalidateCache(redis, '…0001', 'channel_whatsapp')`, then `GET` the key and assert it is `null`
    (no stale grant). Assert the key shape is EXACTLY `ralphe:entitlement:{tenant_id}:{module_key}`.
  - `writeAuditRow` (AUD-01): connect to the ephemeral PG (read `process.env.PGPORT`/`PGHOST`), call
    `writeAuditRow(knex, {...created...})`, then `SELECT COUNT(*)` from `entitlement_audit_log` for the
    canonical tenant/module and assert it incremented by 1 with `action='created'`, `old_value` null,
    `new_value` not null.
  - Fail-loud: inject a knex/redis that REJECTS and assert `writeAuditRow`/`invalidateCache` surface the
    error (the helper must not silently swallow — the test asserts the rejection propagates OR a counter
    is incremented, per the 19-02 contract).
Use `ioredis` + `knex`/`pg` already in `inventory-cms/node_modules`. Keep imports lazy/guarded so the
file does not crash the test runner if `audit-hook.ts` is absent (use a top-level dynamic import in a
`before()` and `test.skip` the IO cases when the module or a service env var is missing).

Create `scripts/test-phase19.sh` — a bash harness (mirror `scripts/test-redis.sh` + the Phase-18
ephemeral-PG mechanism in 19-VALIDATION.md):
  - `set -euo pipefail`; trap-cleanup that stops PG + `redis-cli -p $RPORT shutdown nosave` + removes the
    temp dirs on EXIT.
  - Boot ephemeral redis: `redis-server --port 7390 --daemonize yes --save "" --appendonly no --dir /tmp`
    (pick a free high port; export `REDIS_PORT=7390 REDIS_HOST=127.0.0.1`).
  - Boot ephemeral PG as the system `postgres` user via `/usr/lib/postgresql/16/bin` (`initdb -A trust`,
    `pg_ctl start -o '-p 55433 -k <sock>'`), `createdb`/connect, apply
    `db/ci-fixtures/19-entitlement-audit-seed.sql`; export `PGHOST`/`PGPORT`/`PGUSER`/`PGDATABASE`.
  - Run `node --test --experimental-strip-types
    inventory-cms/src/api/tenant-entitlement/content-types/tenant-entitlement/__tests__/audit-hook.test.mjs`
    (so the `.ts` helper imports without a separate build), then optionally
    `psql … -f db/ci-assertions/19-entitlement-audit.sql`.
  - Echo a clear PASS/FAIL summary; exit non-zero on any failure.
  </action>
  <verify>
    <automated>t=inventory-cms/src/api/tenant-entitlement/content-types/tenant-entitlement/__tests__/audit-hook.test.mjs; h=scripts/test-phase19.sh; test -f "$t" || { echo "FAIL: node-test missing"; exit 1; }; node --check "$t" || { echo "FAIL: node-test not valid JS"; exit 1; }; grep -q "ralphe:entitlement:00000000-0000-0000-0000-000000000001:channel_whatsapp" "$t" || { echo "FAIL: canonical key not asserted in test"; exit 1; }; grep -qi "invalidateCache\|deriveAction\|validateTenantId\|writeAuditRow" "$t" || { echo "FAIL: helper exports not exercised"; exit 1; }; test -f "$h" || { echo "FAIL: harness missing"; exit 1; }; bash -n "$h" || { echo "FAIL: harness not valid bash"; exit 1; }; grep -q "redis-server" "$h" || { echo "FAIL: harness does not boot ephemeral redis"; exit 1; }; grep -qi "initdb\|pg_ctl\|/usr/lib/postgresql/16/bin" "$h" || { echo "FAIL: harness does not boot ephemeral PG"; exit 1; }; grep -q "audit-hook.test.mjs" "$h" || { echo "FAIL: harness does not run the node-test"; exit 1; }; echo "PASS node-test + harness present (canonical key, both services, helper exports)"</automated>
  </verify>
  <done>
`audit-hook.test.mjs` is valid JS (`node --check`), asserts the exact canonical key
`ralphe:entitlement:…0001:channel_whatsapp` and exercises `deriveAction`/`validateTenantId`/
`writeAuditRow`/`invalidateCache` (with IO cases gracefully skipped if the helper/service is absent), and
`scripts/test-phase19.sh` is valid bash that boots ephemeral redis + ephemeral PG, applies the seed, and
runs the node-test (tearing both down on exit).
  </done>
</task>

<task type="auto">
  <name>Task 4: Write the phase-19 CI gate (PG audit job + redis:7-alpine invalidation job + structural job)</name>
  <files>.github/workflows/phase-19-assertions.yml</files>
  <read_first>.github/workflows/phase-18-assertions.yml (the EXACT service + seed + psql + structural
  pattern + pinned SHA + log groups to mirror); 19-RESEARCH.md §7 (the CI job structure: PG + redis
  services + the structural greps).</read_first>
  <action>
Create `.github/workflows/phase-19-assertions.yml` mirroring `phase-18-assertions.yml` conventions
(pinned `actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2`,
`permissions: contents: read` + `pull-requests: read`, `timeout-minutes`, `::group::` wrapping).
Trigger on `pull_request` with `paths:` covering:
  `inventory-cms/src/api/tenant-entitlement/content-types/tenant-entitlement/audit-hook.ts`,
  `inventory-cms/src/api/tenant-entitlement/content-types/tenant-entitlement/lifecycles.ts`,
  `inventory-cms/src/api/product-module/content-types/product-module/lifecycles.ts`,
  `inventory-cms/src/api/tenant-entitlement/content-types/tenant-entitlement/__tests__/audit-hook.test.mjs`,
  `db/ci-fixtures/19-entitlement-audit-seed.sql`, `db/ci-assertions/19-entitlement-audit.sql`,
  `db/migrations-strapi/2026-06-20_entitlement_audit_uuid.sql`,
  `scripts/test-phase19.sh`, and `.github/workflows/phase-19-assertions.yml` itself.

JOB A — `audit-row-sql` (`postgres:15-alpine` service exactly like phase-18, `POSTGRES_USER: n8n` etc.):
  - Step "Apply entitlement-audit seed": `psql -h localhost -p 5432 -U n8n -d n8n -v ON_ERROR_STOP=1
    -f db/ci-fixtures/19-entitlement-audit-seed.sql` (the seed self-creates tenants + the uuid-shaped
    entitlement_audit_log + tenant_entitlements).
  - Step "Audit-row + invalid-uuid assertions": `psql … -v ON_ERROR_STOP=1
    -f db/ci-assertions/19-entitlement-audit.sql` (the DO-blocks inline-INSERT the per-op rows then
    assert; the invalid-uuid negative fires via nested BEGIN..EXCEPTION).
  - Use `env: PGPASSWORD: n8npass`, `-h localhost -p 5432 -U n8n -d n8n` like phase-18.

JOB B — `cache-invalidation-redis` (ADD a `redis:7-alpine` service with a health-cmd
  `redis-cli ping`, `ports: 6379:6379`; also the `postgres:15-alpine` service so the node-test's
  writeAuditRow case has a DB):
  - `services: redis: { image: redis:7-alpine, options: --health-cmd "redis-cli ping" --health-interval
    10s --health-timeout 5s --health-retries 5, ports: [6379:6379] }` + the postgres service.
  - Step "Install CMS deps for the helper" (only if needed): the helper imports ioredis/knex/pg already
    in `inventory-cms`; run `cd inventory-cms && npm ci` (or rely on a cached install) so `audit-hook.ts`'s
    imports resolve.
  - Step "Apply entitlement-audit seed (n8n DB)": as in Job A (so writeAuditRow has the table).
  - Step "Run the audit-hook node-test": set `REDIS_HOST=localhost REDIS_PORT=6379 PGHOST=localhost
    PGPORT=5432 PGUSER=n8n PGPASSWORD=n8npass PGDATABASE=n8n` and run
    `node --test --experimental-strip-types
    inventory-cms/src/api/tenant-entitlement/content-types/tenant-entitlement/__tests__/audit-hook.test.mjs`.
    The SET→invalidateCache→GET-nil round-trip on the canonical key proves AUD-02 in CI.

JOB C — `hook-structural` (no services, `runs-on: ubuntu-latest`), `jq`/`grep`/`node --check`:
  - Step "Audit-hook uses the canonical cache key":
    `grep -q "ralphe:entitlement:" inventory-cms/src/api/tenant-entitlement/content-types/tenant-entitlement/audit-hook.ts`
    else FAIL.
  - Step "Both lifecycles.ts exist":
    `test -f inventory-cms/src/api/tenant-entitlement/content-types/tenant-entitlement/lifecycles.ts &&
     test -f inventory-cms/src/api/product-module/content-types/product-module/lifecycles.ts` else FAIL.
  - Step "Audit-hook is a PURE helper (no @strapi/strapi import on the test seam)":
    `if grep -q "@strapi/strapi" inventory-cms/src/api/tenant-entitlement/content-types/tenant-entitlement/audit-hook.ts;
     then echo FAIL; exit 1; fi`.
  - Step "No bare swallow in the hook":
    `if grep -Eq "continueOnFail|catch *\( *\) *=> *\{ *\}|\.catch\(\(\)=>\{\}\)"
     inventory-cms/src/api/tenant-entitlement/content-types/tenant-entitlement/audit-hook.ts; then FAIL; fi`.
  - Step "node --check the test file": `node --check
    inventory-cms/src/api/tenant-entitlement/content-types/tenant-entitlement/__tests__/audit-hook.test.mjs`.
  Wrap each step in `::group::`/`::endgroup::`. The YAML must be valid.

The gate must fail the build (non-zero exit) on a missing audit row, a stale-cache survival, a missing
canonical key, a missing lifecycle file, or a `@strapi/strapi` import in the pure helper.
  </action>
  <verify>
    <automated>y=.github/workflows/phase-19-assertions.yml; python3 -c "import yaml; yaml.safe_load(open('$y'))" || { echo "FAIL: invalid YAML"; exit 1; }; grep -q "19-entitlement-audit.sql" "$y" || { echo "FAIL: assertion file not referenced"; exit 1; }; grep -q "19-entitlement-audit-seed.sql" "$y" || { echo "FAIL: seed not referenced"; exit 1; }; grep -q "audit-hook.test.mjs" "$y" || { echo "FAIL: node-test not referenced"; exit 1; }; grep -q "postgres:15-alpine" "$y" || { echo "FAIL: no PG service"; exit 1; }; grep -q "redis:7-alpine" "$y" || { echo "FAIL: no redis service"; exit 1; }; grep -q "11bd71901bbe5b1630ceea73d27597364c9af683" "$y" || { echo "FAIL: checkout not pinned to phase-18 SHA"; exit 1; }; grep -q "ralphe:entitlement:" "$y" || { echo "FAIL: structural canonical-key grep missing"; exit 1; }; grep -q "lifecycles.ts" "$y" || { echo "FAIL: lifecycle-presence check missing"; exit 1; }; echo "PASS phase-19 ci gate well-formed (PG + redis:7-alpine + structural)"</automated>
  </verify>
  <done>
`.github/workflows/phase-19-assertions.yml` is valid YAML with three jobs: a `postgres:15-alpine` audit-row
SQL job (seed + `19-entitlement-audit.sql`), a Redis job with a `redis:7-alpine` service running the
`audit-hook.test.mjs` invalidation round-trip on the canonical key, and a structural job asserting
`audit-hook.ts` uses `ralphe:entitlement:`, both `lifecycles.ts` exist, the helper has no `@strapi/strapi`
import, and no bare swallow. Pinned checkout SHA; any failure exits non-zero.
  </done>
</task>

</tasks>

<verification>
- `python3 yaml.safe_load` parses `.github/workflows/phase-19-assertions.yml` (PG + `redis:7-alpine`).
- `db/ci-fixtures/19-entitlement-audit-seed.sql` creates the uuid-shaped `entitlement_audit_log` + tenants
  + a `channel_whatsapp` entitlement under `…0001` (idempotent).
- `db/ci-assertions/19-entitlement-audit.sql` proves a row per op + rejects a non-UUID tenant_id via
  nested `BEGIN..EXCEPTION`, no SAVEPOINT.
- `audit-hook.test.mjs` (`node --check` clean) asserts the canonical key + the helper exports; runs green
  via `scripts/test-phase19.sh` (ephemeral PG + redis) once 19-02 lands `audit-hook.ts`.
- Once 19-02 lands, the gate (or `act pull_request -W .github/workflows/phase-19-assertions.yml`) is green
  end-to-end; local run via `bash scripts/test-phase19.sh` (19-VALIDATION.md mechanism).
</verification>

<success_criteria>
A CI gate proves, without a live VPS and without booting Strapi, that (a) an `entitlement_audit_log` row
is written per op with old→new captured and a non-UUID tenant_id rejected (AUD-01), and (b) a key present
before an entitlement mutation is GONE after `invalidateCache()` on the exact canonical
`ralphe:entitlement:{tenant_id}:{module_key}` key (AUD-02, no stale grant) — failing the build on any
miss, satisfying Phase 19 success-criteria 1 and 3.
</success_criteria>

<output>
After completion, create
`.planning/phases/19-entitlement-audit-and-cache-invalidation-lifecycle-hook/19-03-SUMMARY.md`.
</output>
