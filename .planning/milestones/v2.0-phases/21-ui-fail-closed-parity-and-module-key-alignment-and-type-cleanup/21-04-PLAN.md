---
phase: 21-ui-fail-closed-parity-and-module-key-alignment-and-type-cleanup
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - inventory-cms/src/api/product-module/controllers/product-module.ts
  - inventory-cms/src/api/product-module/routes/product-module.ts
  - inventory-cms/src/api/tenant-entitlement/controllers/tenant-entitlement.ts
  - inventory-cms/src/api/tenant-entitlement/routes/tenant-entitlement.ts
  - inventory-cms/src/middlewares/auth-ratelimit.ts
  - .github/workflows/phase-21-assertions.yml
autonomous: true
requirements: [TYP-01]
user_setup: []

must_haves:
  truths:
    - "The 4 SaaS-ContentType TS2345 sites get the repo's OWN established `// @ts-ignore - UID registered at runtime; type generator skips this custom type` comment (byte-identical to the 12-file precedent at e.g. dispatch-log/controllers/dispatch-log.ts:5) directly above the factories.create… call: product-module/controllers/product-module.ts:2, product-module/routes/product-module.ts:2, tenant-entitlement/controllers/tenant-entitlement.ts:2, tenant-entitlement/routes/tenant-entitlement.ts:2."
    - "The 5th CMS error (auth-ratelimit.ts:37 ioredis TS2351 'not constructable') is fixed by the SAFE ≤2-line import change PROVEN during planning: replace the dynamic `const Redis = (await import('ioredis')).default;` (inside getRedisClient) with a top-of-file static `import Redis from 'ioredis';` — the EXACT constructable pattern realtime.ts:2 uses (`new Redis(port, host)` works there). Verified during planning: this alone removes the TS2351 (4 errors remain), and with the 4 ts-ignores `npx tsc --noEmit` reaches 0 errors. Runtime behavior is preserved: ioredis is already a static CMS dep elsewhere; the construct still happens lazily inside getRedisClient only when USE_REDIS, with lazyConnect:true."
    - "DECISION = 4 + ioredis → FULLY-GREEN CMS TS. With all 5 sites fixed, `cd inventory-cms && npx tsc --noEmit` exits 0 (0 errors) — the CMS TypeScript Compilation CI job (ci.yml cms-ts-compile) goes FULLY green. Confirmed safe by a planning dry-run (patched → 0 errors → reverted to clean baseline). This is the milestone's clean-finish phase, so the residual-error compromise is NOT taken."
    - "A cms-ts-compile job is APPENDED to .github/workflows/phase-21-assertions.yml (created by 21-02; 21-04 appends — same-file append ordering, runs after 21-02 within Wave 1): Node 20 (parity with ci.yml's NODE_VERSION=20.20.0 cms-ts-compile job), `cd inventory-cms && npm ci --legacy-peer-deps && npx tsc --noEmit`, asserting 0 errors. Pinned actions/checkout + actions/setup-node SHAs matching the rest of the file."
    - "NO VPS deferral: pure source-comment + import-form edits in already-shipped CMS files; tsc runs locally + in CI. Confirmed: Phase 21 carries NO 🔴 VPS execution sub-step. The CMS-TS green is a bonus gated independently in the cms-ts-compile job; it does NOT gate the admin-dashboard lint (the phase's primary success criterion)."
  artifacts:
    - path: "inventory-cms/src/api/product-module/controllers/product-module.ts"
      provides: "@ts-ignore UID line on the product-module controller factory"
      contains: "@ts-ignore - UID registered at runtime"
    - path: "inventory-cms/src/api/product-module/routes/product-module.ts"
      provides: "@ts-ignore UID line on the product-module router factory"
      contains: "@ts-ignore - UID registered at runtime"
    - path: "inventory-cms/src/api/tenant-entitlement/controllers/tenant-entitlement.ts"
      provides: "@ts-ignore UID line on the tenant-entitlement controller factory"
      contains: "@ts-ignore - UID registered at runtime"
    - path: "inventory-cms/src/api/tenant-entitlement/routes/tenant-entitlement.ts"
      provides: "@ts-ignore UID line on the tenant-entitlement router factory"
      contains: "@ts-ignore - UID registered at runtime"
    - path: "inventory-cms/src/middlewares/auth-ratelimit.ts"
      provides: "Static `import Redis from 'ioredis'` (constructable) replacing the dynamic-import default destructure"
      contains: "import Redis from 'ioredis'"
    - path: ".github/workflows/phase-21-assertions.yml"
      provides: "Appended cms-ts-compile job (Node 20, npx tsc --noEmit → 0 errors)"
      contains: "cms-ts-compile"
  key_links:
    - from: "inventory-cms/src/api/{product-module,tenant-entitlement}/{controllers,routes}/*.ts"
      to: "tsc ContentType union (which omits these runtime-registered custom types)"
      via: "// @ts-ignore - UID registered at runtime (the 12-file precedent)"
      pattern: "@ts-ignore - UID registered at runtime"
    - from: "inventory-cms/src/middlewares/auth-ratelimit.ts new Redis(...) at L37"
      to: "the constructable default import"
      via: "top-of-file `import Redis from 'ioredis'` (realtime.ts:2 proven pattern)"
      pattern: "import Redis from 'ioredis'"
    - from: ".github/workflows/phase-21-assertions.yml cms-ts-compile job"
      to: "inventory-cms npx tsc --noEmit == 0 errors"
      via: "cd inventory-cms && npm ci --legacy-peer-deps && npx tsc --noEmit"
      pattern: "tsc --noEmit"
---

<objective>
Clear the standing CMS TypeScript Compilation red — the OTHER milestone gate — by applying the repo's own
established `// @ts-ignore - UID registered at runtime` one-liner (used by 12 other api files) to the 4
SaaS-ContentType TS2345 sites this milestone created, AND fixing the 5th error (`auth-ratelimit.ts:37`
ioredis TS2351) with the SAFE, planning-verified ≤2-line import change (dynamic-import default destructure
→ a static `import Redis from 'ioredis'`, the constructable pattern `realtime.ts` already uses). With all 5
fixed, `cd inventory-cms && npx tsc --noEmit` reaches **0 errors** — the `cms-ts-compile` CI job goes FULLY
green. Append a `cms-ts-compile` job to the phase-21 CI gate.

Purpose: TYP-01 (the CMS-side type debt about THIS milestone's SaaS content types). This is the milestone's
clean-finish phase — so the full-green path is taken (not the 4-only + 1-residual compromise). The 4 SaaS
files simply missed the `// @ts-ignore` comment when created; the ioredis fix was verified during planning
to be a one-line constructable-import alignment that removes the TS2351 without changing runtime behavior.

Output: the 4 `// @ts-ignore` additions, the 1 ioredis import-form fix, and an appended `cms-ts-compile`
job. DISJOINT from 21-01 (`useEntitlements.ts` + test + banner), 21-02 (`App.tsx` + workflows + scripts;
21-02 CREATES `phase-21-assertions.yml`), and 21-03 (`src/types/entitlements.ts` + `AIChatBubble.tsx`).
**The only shared file is `.github/workflows/phase-21-assertions.yml`: 21-02 creates it, 21-04 appends the
cms-ts-compile job (same-file append ordering — run 21-02 before 21-04 within Wave 1).**

🔴 VPS DEFERRED: NONE. Pure source edits in already-shipped CMS files; tsc runs locally + in CI.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-ui-fail-closed-parity-and-module-key-alignment-and-type-cleanup/21-RESEARCH.md
@.planning/phases/21-ui-fail-closed-parity-and-module-key-alignment-and-type-cleanup/21-VALIDATION.md

<read_first>
- `21-RESEARCH.md` §8 (the CMS-TS scope decision — the EXACT 5 errors, the root cause = the type generator
  omits the custom UIDs, the 12-file `// @ts-ignore` precedent, and the 5th error's ioredis ESM-interop
  nature), "Open Questions" O-4, "Don't Hand-Roll" (the `// @ts-ignore` precedent — do NOT redesign the
  content types / typegen).
- `inventory-cms/src/api/dispatch-log/controllers/dispatch-log.ts:5` — the EXACT precedent comment to copy
  byte-for-byte: `// @ts-ignore - UID registered at runtime; type generator skips this custom type`.
- The 4 target files (each is 2 lines today — `import { factories } …` then `export default
  factories.create…`): `inventory-cms/src/api/product-module/controllers/product-module.ts`,
  `…/product-module/routes/product-module.ts`,
  `…/tenant-entitlement/controllers/tenant-entitlement.ts`,
  `…/tenant-entitlement/routes/tenant-entitlement.ts`.
- `inventory-cms/src/middlewares/auth-ratelimit.ts:10,28-44` — the `import type { Core }` at the top, the
  `let redisClient: any = null;` (leave as-is — out of TYP-01 admin scope), the `getRedisClient()` body
  with the offending `const Redis = (await import('ioredis')).default;` at L35 + `new Redis(url, {...})`
  at L37.
- `inventory-cms/src/api/realtime/services/realtime.ts:2,13` — the PROVEN constructable pattern:
  `import Redis from 'ioredis';` (static default) + `new Redis(port, host)`. Align auth-ratelimit to this.
- `.github/workflows/ci.yml:498-536` — the REAL `cms-ts-compile` job: Node `${{ env.NODE_VERSION }}`
  (=20.20.0), `npm ci --legacy-peer-deps`, `npx tsc --noEmit`. Mirror for the appended job (Node 20).
- `.github/workflows/phase-21-assertions.yml` (created by 21-02) — append the new job into it; match its
  pinned SHAs (`actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2`,
  `actions/setup-node@39370e3970a6d050c480ffad4ff0ed4d3fdee5af # v4.1.0`), `permissions`, `::group::`.
</read_first>

<interfaces>
<!-- The exact edits. All 5 verified during planning to yield `npx tsc --noEmit` == 0 errors. -->

The 4 SaaS files — insert the precedent comment directly above the `export default factories.…` line:
  // @ts-ignore - UID registered at runtime; type generator skips this custom type
  export default factories.createCoreController('api::product-module.product-module');   // controllers
  export default factories.createCoreRouter('api::product-module.product-module');       // routes
  // (and the tenant-entitlement controller + router analogously)

auth-ratelimit.ts (the ioredis fix — ≤2 lines):
  // add near the top imports (after `import type { Core } from '@strapi/strapi';`):
  import Redis from 'ioredis';
  // and DELETE the dynamic-import line inside getRedisClient():
  -   const Redis = (await import('ioredis')).default;
  // `new Redis(url, { maxRetriesPerRequest: 1, lazyConnect: true })` at L37 then constructs against the
  //  static (constructable) default import — exactly as realtime.ts does.

Appended CI job (into phase-21-assertions.yml):
  cms-ts-compile:
    name: Phase 21 CMS TypeScript Compilation (0 errors)
    runs-on: ubuntu-latest
    steps:
      - checkout (pinned SHA)
      - setup-node (pinned SHA) node-version: '20'
      - working-directory: inventory-cms  →  npm ci --legacy-peer-deps  →  npx tsc --noEmit
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the @ts-ignore UID line to the 4 SaaS api files + fix the ioredis import</name>
  <files>inventory-cms/src/api/product-module/controllers/product-module.ts, inventory-cms/src/api/product-module/routes/product-module.ts, inventory-cms/src/api/tenant-entitlement/controllers/tenant-entitlement.ts, inventory-cms/src/api/tenant-entitlement/routes/tenant-entitlement.ts, inventory-cms/src/middlewares/auth-ratelimit.ts</files>
  <read_first>21-RESEARCH.md §8 (the 5 errors + root cause + the precedent); dispatch-log/controllers/dispatch-log.ts:5
  (the byte-for-byte comment); the 4 target files (current 2-line shape); auth-ratelimit.ts:10,35,37 (the
  import to fix); realtime.ts:2,13 (the constructable pattern); the <interfaces> block.</read_first>
  <action>
For EACH of the 4 SaaS files, insert the precedent comment line
`// @ts-ignore - UID registered at runtime; type generator skips this custom type` on its OWN line
directly above the `export default factories.create…` line (byte-identical to
`dispatch-log/controllers/dispatch-log.ts:5`). Do NOT change the import line or the factory call.

In `inventory-cms/src/middlewares/auth-ratelimit.ts`:
  1. Add a static `import Redis from 'ioredis';` near the top imports (e.g. immediately after
     `import type { Core } from '@strapi/strapi';`).
  2. DELETE the dynamic-import line inside `getRedisClient()`: `const Redis = (await import('ioredis')).default;`
  Leave everything else (the `let redisClient: any`, the `new Redis(url, { maxRetriesPerRequest: 1,
  lazyConnect: true })` call, the lazy/conditional `USE_REDIS` gating) untouched — the construct still
  happens lazily only when `USE_REDIS`, now against the constructable static default import.
  </action>
  <verify>
    <automated>set -e; for f in inventory-cms/src/api/product-module/controllers/product-module.ts inventory-cms/src/api/product-module/routes/product-module.ts inventory-cms/src/api/tenant-entitlement/controllers/tenant-entitlement.ts inventory-cms/src/api/tenant-entitlement/routes/tenant-entitlement.ts; do grep -q "@ts-ignore - UID registered at runtime" "$f" || { echo "FAIL: @ts-ignore missing in $f"; exit 1; }; done; a=inventory-cms/src/middlewares/auth-ratelimit.ts; grep -q "^import Redis from 'ioredis';" "$a" || { echo "FAIL: static ioredis import missing"; exit 1; }; if grep -q "await import('ioredis')" "$a"; then echo "FAIL: dynamic ioredis import still present"; exit 1; fi; cd inventory-cms; timeout 180 npx tsc --noEmit || { echo "FAIL: tsc --noEmit not 0 errors"; exit 1; }; echo "PASS: 4 @ts-ignore added + ioredis static import; npx tsc --noEmit == 0 errors (CMS TS fully green)"</automated>
  </verify>
  <done>
The 4 SaaS controller/route files each carry the precedent `// @ts-ignore - UID registered at runtime…`
comment above their `factories.create…` call; `auth-ratelimit.ts` uses a static `import Redis from
'ioredis'` (no dynamic-import destructure); and `cd inventory-cms && npx tsc --noEmit` exits 0 — the CMS
TypeScript Compilation is fully green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Append the cms-ts-compile job to phase-21-assertions.yml</name>
  <files>.github/workflows/phase-21-assertions.yml</files>
  <read_first>`.github/workflows/phase-21-assertions.yml` (created by 21-02 — read its current jobs/paths;
  this task APPENDS, leaving the 21-02 jobs intact); `.github/workflows/ci.yml:498-536` (the real
  `cms-ts-compile` job to mirror — Node 20, `npm ci --legacy-peer-deps`, `npx tsc --noEmit`); the
  <interfaces> block (the job shape).</read_first>
  <action>
Append a `cms-ts-compile` job to `.github/workflows/phase-21-assertions.yml` (do NOT modify the existing
`admin-dashboard-lint` / `admin-dashboard-vitest` / `module-key-consistency` jobs created by 21-02; only
add this job and, if needed, ensure the `paths:` already include the CMS api/middleware files — 21-02 added
them; if absent, add `inventory-cms/src/api/product-module/**`,
`inventory-cms/src/api/tenant-entitlement/**`, `inventory-cms/src/middlewares/auth-ratelimit.ts`).

The job (mirroring ci.yml's `cms-ts-compile`, parity Node 20):
  - `runs-on: ubuntu-latest`, `timeout-minutes`.
  - `actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2`.
  - `actions/setup-node@39370e3970a6d050c480ffad4ff0ed4d3fdee5af # v4.1.0` with `node-version: '20'`.
  - `working-directory: inventory-cms`: `npm ci --legacy-peer-deps` (CMS uses plain `--legacy-peer-deps`
    per ci.yml:532, not the `||` fallback).
  - `npx tsc --noEmit` (MUST exit 0 — fully green). `::group::`-wrap the step.
The YAML MUST still parse with `python3 -c "import yaml; yaml.safe_load(open('...'))"` and retain the 21-02
jobs.
  </action>
  <verify>
    <automated>set -e; y=.github/workflows/phase-21-assertions.yml; test -f "$y" || { echo "FAIL: CI gate missing (21-02 must run first)"; exit 1; }; python3 -c "import yaml; yaml.safe_load(open('$y'))" || { echo "FAIL: invalid YAML"; exit 1; }; grep -q "cms-ts-compile" "$y" || { echo "FAIL: cms-ts-compile job not appended"; exit 1; }; grep -q "tsc --noEmit" "$y" || { echo "FAIL: tsc --noEmit step missing"; exit 1; }; grep -q "inventory-cms" "$y" || { echo "FAIL: inventory-cms working-dir missing"; exit 1; }; grep -q "admin-dashboard-lint" "$y" || { echo "FAIL: 21-02 lint job clobbered"; exit 1; }; grep -q "module-key-consistency" "$y" || { echo "FAIL: 21-02 module-key job clobbered"; exit 1; }; n=$(python3 -c "import yaml; d=yaml.safe_load(open('$y')); print(len(d.get('jobs',{})))"); test "$n" -ge 4 || { echo "FAIL: expected >=4 jobs, got $n"; exit 1; }; echo "PASS: cms-ts-compile appended (Node 20, tsc --noEmit), 21-02 jobs intact, >=4 jobs, valid YAML"</automated>
  </verify>
  <done>
`.github/workflows/phase-21-assertions.yml` gains a `cms-ts-compile` job (pinned SHAs, Node 20,
`working-directory: inventory-cms`, `npm ci --legacy-peer-deps` → `npx tsc --noEmit`) WITHOUT disturbing
the 21-02 jobs (`admin-dashboard-lint`, `admin-dashboard-vitest`, `module-key-consistency` all intact);
the file has ≥4 jobs and parses as valid YAML.
  </done>
</task>

</tasks>

<verification>
- The 4 SaaS api files carry the `// @ts-ignore - UID registered at runtime…` precedent comment;
  `auth-ratelimit.ts` uses a static `import Redis from 'ioredis'` (no dynamic-import destructure).
- `cd inventory-cms && npx tsc --noEmit` exits 0 (fully green — verified safe during planning).
- `phase-21-assertions.yml` has the appended `cms-ts-compile` job (Node 20, `npx tsc --noEmit`) with the
  21-02 jobs intact; valid YAML; ≥4 jobs.
- (Cross-plan) 21-02 CREATES `phase-21-assertions.yml`; this plan APPENDS — run 21-02 before 21-04 within
  Wave 1 (a same-file append ordering, not a behavioral dependency).
</verification>

<success_criteria>
The CMS TypeScript Compilation is FULLY green: the 4 SaaS-ContentType TS2345 sites carry the repo's
established `// @ts-ignore` UID comment and the ioredis TS2351 is fixed by the proven static-import
alignment, so `npx tsc --noEmit` reaches 0 errors and a `cms-ts-compile` CI job (appended to
`phase-21-assertions.yml`) gates it. A bonus that greens the other standing red about this milestone's SaaS
content types. NO 🔴 VPS sub-step.
</success_criteria>

<output>
After completion, create
`.planning/phases/21-ui-fail-closed-parity-and-module-key-alignment-and-type-cleanup/21-04-SUMMARY.md`.
</output>
