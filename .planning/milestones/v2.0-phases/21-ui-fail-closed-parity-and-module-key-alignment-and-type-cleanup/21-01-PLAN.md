---
phase: 21-ui-fail-closed-parity-and-module-key-alignment-and-type-cleanup
plan: 01
type: execute
wave: 2
depends_on: ["21-03"]
files_modified:
  - admin-dashboard/src/hooks/useEntitlements.ts
  - admin-dashboard/src/hooks/useEntitlements.test.tsx
  - admin-dashboard/src/components/EntitlementErrorBanner.tsx
autonomous: true
requirements: [ENT-01, TYP-01]
user_setup: []

must_haves:
  truths:
    - "useEntitlements.hasModule(key) FAILS CLOSED: returns FALSE while loading AND on fetch error — EXCEPT keys in a static SHARED_CORE allowlist (derived from config/product_modules.json tier==='shared_core'||tier==='product_core' → ['platform_runtime','order_bot_core']) which stay visible during loading/error so CORE admin nav is never locked out. After a successful fetch, hasModule returns modules.includes(key) as today. This reverses the current `if (loading) return true` fail-OPEN bug — parity with W0_MODULE_GUARD's fail-closed posture."
    - "The hook exposes an explicit error surface: it adds an `error: boolean` (and a `status: 'loading'|'error'|'ready'`) to its return, set TRUE in the fetch catch block (the current catch only console.errors — the silent-fail half of the bug). A self-contained EntitlementErrorBanner component reads the hook and renders a visible locked/error indicator when error===true, so hidden modules read as 'error' not silently 'unentitled' — the criterion-1 'surfaces an explicit error/locked state instead of silently rendering all modules'."
    - "All 6 no-explicit-any in useEntitlements.ts (L23,24,30,31,39,40) are replaced with the 21-03 DTOs: strapi.find<ProductModuleRaw>('product-modules') / strapi.find<TenantEntitlementRaw>('tenant-entitlements') (no <any>), (modRes.data ?? []) / (entRes.data ?? []) (no `as any` — find already returns StrapiResponse<T[]>), and .forEach((m: ProductModuleRaw)=>{ const mData = unwrap(m); ... }) / unwrap(e). This clears the 6 errors → the admin-dashboard `npm run lint` job goes GREEN (TYP-01 criterion-4 gate)."
    - "The v4/v5 response-shape tolerance is PRESERVED via the imported unwrap<T>() (the typed equivalent of the old `m.attributes || m` / `e.attributes || e`) — the typed rewrite must NOT drop the attributes||self tolerance (Pitfall 5)."
    - "useEntitlements.test.tsx (Vitest + @testing-library/react renderHook, jsdom — already configured) asserts: (1) hasModule(non-core)===false while loading; (2) shared_core key (platform_runtime) ===true while loading (allowlist visible); (3) hasModule===false AND error===true after a fetch rejection; (4) hasModule(entitled)===true after a successful fetch — tested against BOTH the v5 flat shape AND the v4 {attributes} shape (two mock payloads); (5) hasModule(non-entitled)===false after success. strapiClient + authService are vi.mock'd; authService.isAuthenticated→true."
    - "Fallback #5 handling (ADR-0002 occurrence #5 — the tenantId='default' default param, the LAST annotated 'default' fallback): the hook is updated to accept a real tenant id when an authenticated tenant context is available, and to FAIL CLOSED on the RESULT when it is not — i.e. with no real tenant the query may still run but a zero-row / error result hides all GATED modules while the SHARED_CORE allowlist keeps core nav usable (NO total admin lockout). The plan DOCUMENTS in the SUMMARY whether #5 was fully closed (real auth-tenant wired) or kept-but-fail-closed-on-result (with the reason it can't be fully removed yet, e.g. no authenticated tenant UUID is exposed to the UI), and updates the INVENTORY-15 comment accordingly."
    - "NO total admin lockout: with tenantId='default' yielding zero rows, fail-closed hides only NON-CORE modules; the SHARED_CORE allowlist (platform_runtime, order_bot_core) plus the un-gated nav items (Dashboard, Stock, Customers, the isFullAdmin Advanced block) keep the operator productive. NO VPS deferral — pure frontend; locally + CI verifiable."
  artifacts:
    - path: "admin-dashboard/src/hooks/useEntitlements.ts"
      provides: "Fail-closed hasModule (false on loading/error except shared_core allowlist) + explicit error/status + DTO-typed (0 any) + preserved v4/v5 tolerance"
      contains: "SHARED_CORE"
      min_lines: 50
    - path: "admin-dashboard/src/hooks/useEntitlements.test.tsx"
      provides: "Vitest ENT-01 proof: false-while-loading, shared_core-visible, false+error-on-reject, entitled-true (v4 AND v5)"
      contains: "renderHook"
    - path: "admin-dashboard/src/components/EntitlementErrorBanner.tsx"
      provides: "Explicit locked/error UI surface when the hook's error===true"
      contains: "useEntitlements"
  key_links:
    - from: "admin-dashboard/src/hooks/useEntitlements.ts"
      to: "admin-dashboard/src/types/entitlements.ts (21-03)"
      via: "import { ProductModuleRaw, TenantEntitlementRaw, unwrap } from '../types/entitlements'"
      pattern: "from '../types/entitlements'"
    - from: "useEntitlements.hasModule"
      to: "fail-closed default + SHARED_CORE allowlist"
      via: "if (loading || error) return SHARED_CORE.has(key); return modules.includes(key)"
      pattern: "SHARED_CORE"
    - from: "useEntitlements catch block"
      to: "explicit error state"
      via: "catch (err) { setError(true); console.error(...) }  // no longer silent"
      pattern: "setError"
    - from: "admin-dashboard/src/components/EntitlementErrorBanner.tsx"
      to: "useEntitlements().error"
      via: "const { error } = useEntitlements(); if (!error) return null; render the banner"
      pattern: "error"
---

<objective>
Flip `admin-dashboard/src/hooks/useEntitlements.ts` from fail-OPEN (`if (loading) return true`) to
fail-CLOSED in parity with `W0_MODULE_GUARD`: `hasModule(key)` returns **false while loading and on fetch
error**, EXCEPT a static `SHARED_CORE` allowlist (`platform_runtime`, `order_bot_core` — the
`shared_core`/`product_core` tiers) that stays visible so the admin is never locked out of core nav. Add an
explicit `error`/`status` to the hook return (the current `catch` is silent — the second half of the bug)
and ship a small self-contained `EntitlementErrorBanner` that surfaces a visible locked/error state. In the
same rewrite, replace all 6 `no-explicit-any` (L23,24,30,31,39,40) with the 21-03 DTOs
(`ProductModuleRaw`/`TenantEntitlementRaw`/`unwrap`) — clearing the only load-bearing lint errors so the
admin-dashboard `npm run lint` job goes GREEN. Prove it with a new Vitest test.

Purpose: ENT-01 (+ the load-bearing half of TYP-01). The guard fails closed; the UI must mirror it. Today
every gate renders during the loading window, and on a wrong-tenant/empty result the UI silently hides
everything with no error indication. Fail-closed + an explicit error surface + a shared_core allowlist
fixes all three failure modes without locking the operator out.

Output: the rewritten `useEntitlements.ts` (fail-closed, typed, error-stateful, v4/v5-tolerant), its new
Vitest test `useEntitlements.test.tsx`, and a small `EntitlementErrorBanner.tsx`. DISJOINT from 21-02
(`App.tsx` + workflows + scripts + the CI yml), 21-03 (`src/types/entitlements.ts` + `AIChatBubble.tsx`),
and 21-04 (the CMS files). **This plan is the SOLE owner of `useEntitlements.ts`.**

DEPENDS ON 21-03 (Wave 1): imports `ProductModuleRaw`/`TenantEntitlementRaw`/`unwrap` from
`src/types/entitlements.ts` — so this plan is Wave 2.

🔴 VPS DEFERRED: NONE. Pure frontend; locally + CI verifiable. This is the milestone's clean-finish phase.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-ui-fail-closed-parity-and-module-key-alignment-and-type-cleanup/21-RESEARCH.md
@.planning/phases/21-ui-fail-closed-parity-and-module-key-alignment-and-type-cleanup/21-VALIDATION.md

<read_first>
- `21-RESEARCH.md` §1 (the EXACT current fail-open behavior + the v4/v5 response parsing to preserve + the
  fallback-#5 / tenantId='default' zero-row reality), §3 (the fail-closed contract + the SHARED_CORE
  allowlist SOURCE decision — static `['platform_runtime','order_bot_core']`, the structurally-always-on
  tiers; the explicit error/locked UI requirement), §4/§5 (the 6 `any` to clear + the DTO usage), §7 (the
  Vitest test design — vi.mock strapiClient+authService, renderHook, the two v4/v5 payloads; Vitest is
  ALREADY configured + green), "Common Pitfalls" 1 (error state), 2 (allowlist width), 5 (keep v4/v5
  tolerance), "Open Questions" O-3 (the error-UI scope — a small persistent indicator).
- `admin-dashboard/src/hooks/useEntitlements.ts` (current, 63 lines) — the file you REWRITE: the
  `if (loading) return true` fail-open, the silent `catch`, the `m.attributes || m`/`e.attributes || e`
  tolerance, the `tier==='shared_core'||tier==='product_core'||enabled_globally` global-allow rule, the
  `tenantId='default'` default param + the INVENTORY-15 comment (ADR-0002 #5).
- `admin-dashboard/src/types/entitlements.ts` (21-03, Wave 1) — `ProductModuleRaw`/`TenantEntitlementRaw`/
  `unwrap<T>()` to import (this is the dependency).
- `admin-dashboard/src/services/strapiClient.ts:143-147` — `find<T>()` returns `StrapiResponse<T[]>` =
  `{ data: T[] }` (so `find<ProductModuleRaw>(...)` is typed; `.data` needs no cast).
- `admin-dashboard/src/services/authService.ts:52,69` — `isAuthenticated(): boolean`, `getUser()` (no
  tenant UUID is exposed on the user shape — relevant to the fallback-#5 decision: the UI has no
  authenticated tenant UUID today, so #5 likely stays kept-but-fail-closed-on-result).
- `admin-dashboard/src/App.lazy.test.tsx` + `vite.config.ts` (`test:{environment:'jsdom',globals:true}`) —
  the Vitest setup the new test uses (renderHook from @testing-library/react 16, vi.mock).
- `admin-dashboard/src/components/ToastProvider.tsx:1-30` — the visual idiom for an error surface (red
  AlertTriangle, `bg-red-500/5 border-red-500/20`) the banner can mirror.
- `docs/adr/0002-tenant-id-fallback-inventory.md` (occurrence #5) — the fallback this phase addresses.
</read_first>

<interfaces>
<!-- The rewritten hook contract. The test is its spec; EntitlementErrorBanner is its consumer. -->

useEntitlements.ts (rewrite) — exports:
  // static allowlist derived from product_modules.json tier shared_core/product_core (always-on tiers,
  // the seeder never gates them). Hardcode (research §3 recommendation #1 — no fetch dependency).
  const SHARED_CORE = new Set(['platform_runtime', 'order_bot_core']);

  export function useEntitlements(tenantId?: string): {
    modules: string[];
    loading: boolean;
    error: boolean;                         // NEW — true when the fetch failed (catch sets it)
    status: 'loading' | 'error' | 'ready';  // NEW — convenience for the banner
    hasModule: (key: string) => boolean;    // FAIL-CLOSED: (loading||error) ? SHARED_CORE.has(key) : modules.includes(key)
  }

  // typed fetch (clears the 6 any):
  //   const [modRes, entRes] = await Promise.all([
  //     strapi.find<ProductModuleRaw>('product-modules'),
  //     strapi.find<TenantEntitlementRaw>('tenant-entitlements', { filters: {...} }),
  //   ]);
  //   const allMods = modRes.data ?? [];
  //   allMods.forEach((m: ProductModuleRaw) => { const mData = unwrap(m); if (mData.enabled_globally || mData.tier==='shared_core' || mData.tier==='product_core') enabledKeys.add(mData.key); });
  //   const ents = entRes.data ?? [];
  //   ents.forEach((e: TenantEntitlementRaw) => { const eData = unwrap(e); enabledKeys.add(eData.module_key); });
  //   catch (err) { setError(true); console.error('Failed to fetch entitlements', err); }

EntitlementErrorBanner.tsx — a self-contained component:
  export function EntitlementErrorBanner(): JSX.Element | null {
    const { error } = useEntitlements();
    if (!error) return null;
    // fixed/visible red banner: "Entitlements unavailable — some modules are hidden. Retry or check
    // connection." (mirror ToastProvider's error styling). This is the explicit locked/error surface.
  }

Test mock shapes (research §7):
  v5 flat:   { data: [ { id:1, key:'platform_runtime', tier:'shared_core', enabled_globally:true },
                        { id:2, key:'kiosk_instore', tier:'addon' } ] }
  v4 wrapped:{ data: [ { id:1, attributes:{ key:'kiosk_instore', tier:'addon' } } ] }
  entitlements: { data: [ { id:9, module_key:'kiosk_instore', enabled:true } ] }   // v5
                { data: [ { id:9, attributes:{ module_key:'kiosk_instore', enabled:true } } ] } // v4
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the fail-closed ENT-01 Vitest test (the executable spec)</name>
  <files>admin-dashboard/src/hooks/useEntitlements.test.tsx</files>
  <read_first>21-RESEARCH.md §7 (the Vitest test design — vi.mock strapiClient+authService, renderHook, the
  two v4/v5 payloads); the <interfaces> block (the rewritten contract + the exact mock shapes);
  App.lazy.test.tsx (the Vitest import idiom); strapiClient.ts (the `strapi.find` shape to mock);
  authService.ts:52 (`isAuthenticated`).</read_first>
  <behavior>
    - hasModule('kiosk_instore') === false WHILE loading (non-core gate hidden during the loading window — the fail-OPEN reversal) [criterion 1]
    - hasModule('platform_runtime') === true WHILE loading (SHARED_CORE allowlist visible → no core lockout) [criterion 1 / no-lockout]
    - after the fetch REJECTS: error === true AND hasModule('kiosk_instore') === false AND hasModule('platform_runtime') === true (fail-closed + explicit error, core still visible) [criterion 1 / Pitfall 1]
    - after a SUCCESSFUL v5-flat fetch (kiosk_instore entitled): hasModule('kiosk_instore') === true, hasModule('voice') === false, error === false [criterion 1 / Pitfall 5]
    - after a SUCCESSFUL v4-attributes fetch (kiosk_instore entitled): hasModule('kiosk_instore') === true (unwrap tolerance proven against BOTH shapes) [Pitfall 5]
    - when authService.isAuthenticated() is false: loading resolves false, no fetch, hasModule(non-core)===false, shared_core still visible
  </behavior>
  <action>
Create `admin-dashboard/src/hooks/useEntitlements.test.tsx` (Vitest, jsdom — globals are on, no extra
setup). `vi.mock('../services/strapiClient', ...)` exposing a `strapi.find` mock and
`vi.mock('../services/authService', ...)` with `isAuthenticated: () => true`. Use `renderHook` +
`waitFor`/`act` from `@testing-library/react`.

Implement every <behavior> bullet:
  - "while loading": make `strapi.find` return a never-resolving Promise (or a deferred you resolve later);
    assert the initial render's `hasModule('kiosk_instore') === false` and `hasModule('platform_runtime')
    === true`.
  - "fetch rejects": `strapi.find` mock rejects; `await waitFor(() => expect(result.current.error).toBe(true))`;
    assert `hasModule('kiosk_instore') === false`, `hasModule('platform_runtime') === true`.
  - "v5 success" / "v4 success": resolve the two mock payloads from the <interfaces> block (modules +
    entitlements); `await waitFor(() => expect(result.current.loading).toBe(false))`; assert the entitled
    key true, a non-entitled key false, `error === false`. Run the v4 variant with the `{attributes:{...}}`
    payloads to prove `unwrap` tolerance.
  - "unauthenticated": isAuthenticated mock → false; assert no fetch + fail-closed defaults.
This test is RED until Task 2 (the hook still returns `if (loading) return true`). It is the spec.
  </action>
  <verify>
    <automated>set -e; t=admin-dashboard/src/hooks/useEntitlements.test.tsx; test -f "$t" || { echo "FAIL: test missing"; exit 1; }; grep -q "renderHook" "$t" || { echo "FAIL: not using renderHook"; exit 1; }; grep -q "vi.mock" "$t" || { echo "FAIL: strapiClient/authService not mocked"; exit 1; }; grep -q "platform_runtime" "$t" || { echo "FAIL: shared_core allowlist case missing"; exit 1; }; grep -q "attributes" "$t" || { echo "FAIL: v4 attributes-shape case missing"; exit 1; }; grep -qE "error" "$t" || { echo "FAIL: error-state assertion missing"; exit 1; }; (cd admin-dashboard && npx eslint src/hooks/useEntitlements.test.tsx) || { echo "FAIL: test not lint-clean"; exit 1; }; echo "PASS: ENT-01 Vitest spec present (loading-false, shared_core-visible, error-on-reject, v4+v5 entitled), lint-clean"</automated>
  </verify>
  <done>
`admin-dashboard/src/hooks/useEntitlements.test.tsx` exists (Vitest + `renderHook` + `vi.mock` of
strapiClient & authService), encoding every <behavior> bullet — false-while-loading for non-core,
shared_core-visible-while-loading, false+error-on-reject (core still visible), entitled-true on success
for BOTH v5-flat and v4-attributes payloads — and is lint-clean. (RED until Task 2.)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite useEntitlements.ts fail-closed + DTO-typed + error state (GREEN)</name>
  <files>admin-dashboard/src/hooks/useEntitlements.ts</files>
  <read_first>21-RESEARCH.md §1 (current behavior to reverse), §3 (fail-closed contract + SHARED_CORE
  static allowlist + error surface), §4/§5 (the 6 `any` + the DTO usage + unwrap), "Pitfalls" 1/2/5;
  the <interfaces> block (the exact rewritten contract); src/types/entitlements.ts (the imports);
  strapiClient.ts:143-147 (`find<T>` shape); authService.ts:52 (`isAuthenticated`); the current
  useEntitlements.ts (the INVENTORY-15 comment + the tenantId='default' default to address).</read_first>
  <behavior>
    - Makes Task 1's useEntitlements.test.tsx pass GREEN (all bullets).
    - hasModule: `if (loading || error) return SHARED_CORE.has(key); return modules.includes(key);` (fail-closed; shared_core allowlist the only exception)
    - catch sets error=true (no longer silent); finally sets loading=false; status derives 'loading'|'error'|'ready'
    - typed: strapi.find<ProductModuleRaw>/<TenantEntitlementRaw>, modRes.data ?? [], unwrap(m)/unwrap(e) — ZERO `any`, v4/v5 tolerance preserved
  </behavior>
  <action>
Rewrite `admin-dashboard/src/hooks/useEntitlements.ts`:
  1. `import { ProductModuleRaw, TenantEntitlementRaw, unwrap } from '../types/entitlements';` (21-03).
  2. Add `const SHARED_CORE = new Set(['platform_runtime', 'order_bot_core']);` (module-level const; the
     structurally-always-on tiers — research §3 #1). A short comment cites product_modules.json
     tier shared_core/product_core.
  3. Add state: `const [error, setError] = useState(false);` (keep `modules`, `loading`).
  4. In `fetchModules`: replace `strapi.find<any>(...)` ×2 with `strapi.find<ProductModuleRaw>('product-modules')`
     and `strapi.find<TenantEntitlementRaw>('tenant-entitlements', { filters: {...} })`. Replace
     `(modRes as any).data || []` → `modRes.data ?? []`, `.forEach((m: any)=>{ const mData = m.attributes||m; ...})`
     → `.forEach((m: ProductModuleRaw) => { const mData = unwrap(m); if (mData.enabled_globally ||
     mData.tier === 'shared_core' || mData.tier === 'product_core') enabledKeys.add(mData.key); })`.
     Same for entitlements with `unwrap(e)` and `eData.module_key`. This clears ALL 6 `no-explicit-any`.
  5. `catch (err) { setError(true); console.error('Failed to fetch entitlements', err); }` — the explicit
     error state (no longer silent). Reset `setError(false)` at the START of a fetch attempt.
  6. `hasModule = (key: string) => { if (loading || error) return SHARED_CORE.has(key); return
     modules.includes(key); };` — fail-closed with the allowlist exception. REMOVE the `if (loading) return
     true` fail-open.
  7. Derive `const status: 'loading'|'error'|'ready' = loading ? 'loading' : error ? 'error' : 'ready';`
     Return `{ modules, loading, error, status, hasModule }`.
  8. FALLBACK #5 (tenantId='default', ADR-0002 #5): authService exposes NO tenant UUID on the user shape
     (confirmed authService.ts:69), so a real authenticated-tenant context is NOT available to wire today.
     Therefore: KEEP the query working (so the seeded canonical tenant's data can be returned once a real
     tenant id IS provided) but make the RESULT fail-closed — a zero-row / error result hides all GATED
     modules while SHARED_CORE keeps core nav usable (steps 5-6 already guarantee this). Update the
     INVENTORY-15 comment to record: "#5 kept-but-fail-closed-on-result — the UI has no authenticated
     tenant UUID to substitute; fail-closed on the RESULT (gated hidden, shared_core shown) prevents both
     the fail-open bug and a total lockout. Full removal needs an authenticated tenant context exposed to
     the UI (future work)." If the executor finds an authenticated tenant UUID IS in fact available
     (e.g. on the user/role), prefer closing #5 cleanly by defaulting `tenantId` to that real UUID and
     drop the 'default' literal — and document THAT path in the SUMMARY instead.
  9. Create `admin-dashboard/src/components/EntitlementErrorBanner.tsx` (a NEW file owned by this plan): a
     self-contained component that calls `useEntitlements()`, returns `null` when `!error`, and otherwise
     renders a small fixed/visible red banner (mirror ToastProvider's error styling: AlertTriangle,
     `bg-red-500/5 border-red-500/20`) reading e.g. "Entitlements unavailable — some modules are hidden."
     This is the explicit locked/error surface (criterion 1). It must be lint-clean and import-clean (no
     unused symbols — noUnusedLocals is on).
The file MUST be lint-clean (0 `no-explicit-any`) and tsc-clean.
  </action>
  <verify>
    <automated>set -e; h=admin-dashboard/src/hooks/useEntitlements.ts; b=admin-dashboard/src/components/EntitlementErrorBanner.tsx; grep -q "from '../types/entitlements'" "$h" || { echo "FAIL: DTO import missing"; exit 1; }; grep -q "SHARED_CORE" "$h" || { echo "FAIL: shared_core allowlist missing"; exit 1; }; grep -q "platform_runtime" "$h" || { echo "FAIL: allowlist not seeded with platform_runtime"; exit 1; }; grep -q "setError" "$h" || { echo "FAIL: explicit error state missing"; exit 1; }; grep -q "unwrap(" "$h" || { echo "FAIL: v4/v5 unwrap tolerance dropped"; exit 1; }; if grep -qE "if \(loading\) return true|return true;" "$h"; then echo "FAIL: fail-OPEN branch still present"; exit 1; fi; if grep -qE "<any>|as any|:\s*any\b" "$h"; then echo "FAIL: any still present in the hook"; exit 1; fi; test -f "$b" || { echo "FAIL: EntitlementErrorBanner missing"; exit 1; }; grep -q "useEntitlements" "$b" || { echo "FAIL: banner does not consume the hook"; exit 1; }; cd admin-dashboard; npx eslint src/hooks/useEntitlements.ts src/components/EntitlementErrorBanner.tsx || { echo "FAIL: hook/banner not lint-clean"; exit 1; }; npx vitest run src/hooks/useEntitlements.test.tsx || { echo "FAIL: ENT-01 vitest red"; exit 1; }; echo "PASS: fail-closed hook (no fail-open, no any, shared_core allowlist, explicit error, v4/v5 unwrap), banner present, ENT-01 vitest GREEN, lint-clean"</automated>
  </verify>
  <done>
`useEntitlements.ts` is rewritten fail-closed (`hasModule` returns `SHARED_CORE.has(key)` while
`loading||error`, else `modules.includes(key)`; the `if (loading) return true` removed), sets an explicit
`error` state in `catch`, returns `{modules,loading,error,status,hasModule}`, imports + uses the 21-03 DTOs
(`ProductModuleRaw`/`TenantEntitlementRaw`/`unwrap`) clearing all 6 `no-explicit-any` while preserving the
v4/v5 tolerance, and records the fallback-#5 decision in the INVENTORY-15 comment.
`EntitlementErrorBanner.tsx` surfaces the error state. `npx eslint` on both is clean and `npx vitest run
src/hooks/useEntitlements.test.tsx` is GREEN.
  </done>
</task>

<task type="auto">
  <name>Task 3: Prove the tree-wide green-lint gate + full vitest suite</name>
  <files>admin-dashboard/src/hooks/useEntitlements.ts</files>
  <read_first>21-RESEARCH.md §4 (the 6 `any` were the ENTIRE lint failure — clearing them greens the tree),
  §7 (the full-suite commands + the `npm install --legacy-peer-deps` reality — plain `npm ci` ERESOLVEs on
  lucide-react@react-19).</read_first>
  <action>
This is a VERIFICATION-only task (no new file — it re-asserts the tree-wide gates the rewrite unlocks; it
"touches" `useEntitlements.ts` only in the sense of confirming its effect). Run, from `admin-dashboard/`:
  1. `npm run lint` (= `eslint .`) over the WHOLE tree → MUST exit 0 (the 6 `useEntitlements.ts` errors
     were the only failures per research §4; 21-03's AIChatBubble change is independently clean). This is
     the TYP-01 criterion-4 milestone-closing gate.
  2. `npm test` (= `npx vitest run`) → the existing `setup.test.ts` + `App.lazy.test.tsx` (5 tests) AND the
     new `useEntitlements.test.tsx` all GREEN.
If `npm run lint` reports any residual error, fix it within `useEntitlements.ts`/`EntitlementErrorBanner.tsx`
ownership ONLY (do not touch files owned by 21-02/21-03/21-04 — if a non-owned file is implicated, STOP and
report it as a cross-plan blocker). Use `npm install --legacy-peer-deps` if node_modules needs a refresh
(plain `npm ci` ERESOLVEs).
  </action>
  <verify>
    <automated>cd admin-dashboard; test -d node_modules || npm install --legacy-peer-deps >/dev/null 2>&1 || true; npm run lint || { echo "FAIL: tree-wide npm run lint not green (TYP-01 criterion-4 gate)"; exit 1; }; npx vitest run || { echo "FAIL: vitest suite not green"; exit 1; }; echo "PASS: npm run lint GREEN tree-wide + full vitest suite GREEN (ENT-01 + existing)"</automated>
  </verify>
  <done>
`npm run lint` exits 0 across `admin-dashboard` (the standing Frontend Lint failure is cleared — TYP-01
criterion 4) and `npx vitest run` is green for the full suite (`setup.test.ts`, `App.lazy.test.tsx`, and the
new `useEntitlements.test.tsx`).
  </done>
</task>

</tasks>

<verification>
- `useEntitlements.ts`: no `if (loading) return true`; `hasModule` returns `SHARED_CORE.has(key)` while
  `loading||error` else `modules.includes(key)`; `setError(true)` in catch; imports + uses
  `ProductModuleRaw`/`TenantEntitlementRaw`/`unwrap`; ZERO `any`; v4/v5 tolerance preserved.
- `npx vitest run src/hooks/useEntitlements.test.tsx`: false-while-loading (non-core), shared_core-visible,
  false+error-on-reject, entitled-true for v5 AND v4 payloads.
- `EntitlementErrorBanner.tsx` renders only when `error===true` (explicit locked/error surface).
- `npm run lint` GREEN tree-wide (the milestone-closing TYP-01 gate); `npx vitest run` GREEN full suite.
- (Cross-plan) imports the 21-03 `src/types/entitlements.ts` (Wave 1 → this is Wave 2). The tree-wide lint
  + vitest are also gated by 21-02's `phase-21-assertions.yml` (`admin-dashboard-lint` + `admin-dashboard-vitest`).
</verification>

<success_criteria>
`useEntitlements.hasModule` defaults to **false** while loading and on fetch error (except the
`SHARED_CORE` allowlist that keeps core admin nav usable — no total lockout), the hook surfaces an explicit
`error` state rendered by `EntitlementErrorBanner` instead of silently rendering all modules (parity with
`W0_MODULE_GUARD`), asserted by a Vitest test; the 6 `no-explicit-any` are cleared via the 21-03 DTOs
(`npm run lint` GREEN) while the v4/v5 tolerance is preserved; and fallback #5 is handled fail-closed on the
result (documented). Satisfies ENT-01 (criterion 1) + the load-bearing half of TYP-01 (criterion 4). NO
🔴 VPS sub-step.
</success_criteria>

<output>
After completion, create
`.planning/phases/21-ui-fail-closed-parity-and-module-key-alignment-and-type-cleanup/21-01-SUMMARY.md`.
</output>
