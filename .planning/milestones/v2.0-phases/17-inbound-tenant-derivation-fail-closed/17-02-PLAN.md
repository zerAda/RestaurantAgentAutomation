---
phase: 17-inbound-tenant-derivation-fail-closed
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - workflows/W0_MODULE_GUARD.json
  - workflows/W_DRIVER_ONBOARDING.json
  - docs/adr/0002-tenant-id-fallback-inventory.md
autonomous: true
requirements: [TEN-03]
user_setup: []

must_haves:
  truths:
    - "W0_MODULE_GUARD no longer substitutes DEFAULT_TENANT_ID or 'default' for a missing tenant_id; it fails closed (allowed:false) when the caller supplies no real UUID"
    - "W_DRIVER_ONBOARDING no longer falls back to a hardcoded UUID / DEFAULT_TENANT_ID for tenant_id/restaurant_id; a missing tenant is a loud NOT NULL error, not a silent wrong-tenant write"
    - "No __inventory_15 / INVENTORY-17 annotation markers remain on the fixed nodes"
    - "ADR 0002 records occurrences #2, #3, #4 as REMOVED (Phase 17)"
  artifacts:
    - path: "workflows/W0_MODULE_GUARD.json"
      provides: "Fail-closed guard tenant_id handling (no default substitution)"
      contains: "GUARD_ERROR"
    - path: "workflows/W_DRIVER_ONBOARDING.json"
      provides: "No-fallback tenant/restaurant queryParams (NOT NULL enforces correctness)"
    - path: "docs/adr/0002-tenant-id-fallback-inventory.md"
      provides: "Inventory updated: #2/#3/#4 REMOVED (Phase 17)"
      contains: "REMOVED (Phase 17)"
  key_links:
    - from: "W0_MODULE_GUARD Module Guard node"
      to: "fail-closed deny"
      via: "empty-tenant check returns allowed:false (no || default)"
      pattern: "allowed: false"
    - from: "docs/adr/0002"
      to: "Phase 17 disposition"
      via: "status column #2/#3/#4 -> REMOVED (Phase 17)"
      pattern: "REMOVED \\(Phase 17\\)"
---

<objective>
Remove the remaining tenant-fallback constructs that live OUTSIDE the three inbound adapters, and
finalize the fallback ADR. Specifically: delete `|| $env.DEFAULT_TENANT_ID || 'default'` from
`W0_MODULE_GUARD.json` (replace with a fail-closed empty-tenant guard) and the hardcoded UUID /
`DEFAULT_TENANT_ID` fallback from `W_DRIVER_ONBOARDING.json`'s INSERT params; strip the
`__inventory_15` annotation keys on those nodes; and mark occurrences #2/#3/#4 in
`docs/adr/0002-tenant-id-fallback-inventory.md` as `REMOVED (Phase 17)`.

Purpose: After Plan 17-01 makes callers always supply a real `channel_identities` UUID, the
guard's own default and the driver-onboarding default become both unnecessary and dangerous (they
re-open the fail-open hole). Removing them gives defense-in-depth and makes the post-Phase-17 grep
for workflow tenant fallbacks return zero matches.

Output: Two modified workflow JSONs + the updated ADR. Disjoint from Plan 17-01's files, so this
runs in parallel.

🔴 VPS DEFERRED: importing the updated W0_MODULE_GUARD / W_DRIVER_ONBOARDING on production n8n is
deferred to a prod-connected session.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-inbound-tenant-derivation-fail-closed/17-RESEARCH.md
@.claude/skills/11_workflow_governance/SKILL.md

<read_first>
- `17-RESEARCH.md` section 4 ("The 3 INVENTORY-17 Sites and What to Remove") — exact lines for
  W0_MODULE_GUARD site 1 and W_DRIVER_ONBOARDING site 3, the fail-closed replacement, and the ADR
  update instruction. Also Pitfall 5 (W_DRIVER has no inbound identity signal — NOT NULL is the
  correct fail-closed).
- `docs/adr/0002-tenant-id-fallback-inventory.md` — the 5-occurrence inventory table; rows #2, #3, #4
  are this plan's targets.
- The two target node bodies before editing: `workflows/W0_MODULE_GUARD.json` node "Module Guard",
  `workflows/W_DRIVER_ONBOARDING.json` node "Ensure Customer Profile".
</read_first>

<interfaces>
<!-- Verified by direct read. -->

W0_MODULE_GUARD.json node "Module Guard" (id "guard", n8n-nodes-base.code v2), current line:
  const tenantId = $input.first().json.tenant_id || $env.DEFAULT_TENANT_ID || 'default';
Node carries a "__inventory_15" key (remove it after the fix).

W_DRIVER_ONBOARDING.json node "Ensure Customer Profile"
  (id e6a4b12c-3d4f-4e5a-8b6c-7d8e9f0a1b2c, n8n-nodes-base.postgres), current queryParams:
  ={{[$json.phone, $json.tenant_id || $env.DEFAULT_TENANT_ID || '00000000-0000-0000-0000-000000000001', $json.restaurant_id || $env.DEFAULT_RESTAURANT_ID || '00000000-0000-0000-0000-000000000000']}}
  The INSERT targets restaurant_users (tenant_id, restaurant_id, channel, user_id, role) with
  $2=tenant_id, $3=restaurant_id, $1=phone. Node carries a "__inventory_15" key (remove it).

ADR table rows to update (status -> "REMOVED (Phase 17)", owning phase stays 17):
  # 2  W0_MODULE_GUARD.json   node "Module Guard"
  # 3  W1_IN_WA.json          node "B0 - Apply Auth Context"   (removed by Plan 17-01)
  # 4  W_DRIVER_ONBOARDING.json node "Ensure Customer Profile"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove fail-open fallbacks from W0_MODULE_GUARD and W_DRIVER_ONBOARDING</name>
  <files>workflows/W0_MODULE_GUARD.json, workflows/W_DRIVER_ONBOARDING.json</files>
  <read_first>17-RESEARCH.md section 4 sites 1 and 3; the &lt;interfaces&gt; block above for the exact
  current lines.</read_first>
  <action>
W0_MODULE_GUARD.json, node "Module Guard" (jsCode):
  Replace:
    const tenantId = $input.first().json.tenant_id || $env.DEFAULT_TENANT_ID || 'default';
  With (fail closed on a missing/blank tenant — defense-in-depth now that callers always pass a
  real UUID from channel_identities):
    const tenantId = ($input.first().json.tenant_id || '').toString().trim();
    if (!tenantId) {
      return [{ json: { allowed: false, reason: 'GUARD_ERROR: tenant_id not provided (UNKNOWN_CHANNEL_IDENTITY)' } }];
    }
  Place the `if (!tenantId)` guard right after the existing `if (!moduleKey) { ... }` block so both
  early returns sit together. Do NOT introduce any new `|| 'default'`.
  Then DELETE the `"__inventory_15"` key from that node object.

W_DRIVER_ONBOARDING.json, node "Ensure Customer Profile" (parameters.additionalFields.queryParams):
  Replace the queryParams expression with the no-fallback form (a missing tenant becomes a NOT NULL
  violation — the correct loud failure per Pitfall 5):
    ={{[$json.phone, $json.tenant_id, $json.restaurant_id]}}
  Then DELETE the `"__inventory_15"` key from that node object.

Both files must remain valid JSON.
  </action>
  <verify>
    <automated>python3 -c "import json; json.load(open('workflows/W0_MODULE_GUARD.json'))" && python3 -c "import json; json.load(open('workflows/W_DRIVER_ONBOARDING.json'))" || exit 1; jq -r '.nodes[] | select(.name=="Module Guard") | .parameters.jsCode' workflows/W0_MODULE_GUARD.json > /tmp/guard.js; node --check /tmp/guard.js || exit 1; if grep -Eq "\|\| *'default'|DEFAULT_TENANT_ID" /tmp/guard.js; then echo "FAIL guard fallback remains"; exit 1; fi; grep -q "allowed: false" /tmp/guard.js || exit 1; dp=$(jq -r '.nodes[] | select(.name=="Ensure Customer Profile") | .parameters.additionalFields.queryParams' workflows/W_DRIVER_ONBOARDING.json); echo "$dp" | grep -Eq "DEFAULT_TENANT_ID|00000000-0000-0000-0000-000000000001|DEFAULT_RESTAURANT_ID" && { echo "FAIL driver fallback remains"; exit 1; } || true; if grep -rq "__inventory_15" workflows/W0_MODULE_GUARD.json workflows/W_DRIVER_ONBOARDING.json; then echo "FAIL inventory marker remains"; exit 1; fi; echo "PASS fallbacks removed + markers stripped"</automated>
  </verify>
  <done>
Both files are valid JSON; the guard jsCode passes `node --check`, fails closed (`allowed: false`)
on a blank tenant, and contains no `|| 'default'` / `DEFAULT_TENANT_ID`; the driver queryParams is
`[$json.phone, $json.tenant_id, $json.restaurant_id]` with no UUID/env fallback; neither node retains
an `__inventory_15` key.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update ADR 0002 — mark occurrences #2, #3, #4 REMOVED (Phase 17)</name>
  <files>docs/adr/0002-tenant-id-fallback-inventory.md</files>
  <read_first>The full ADR, especially the "Complete Fallback Inventory" table and the
  "Phase 17 Remaining Work" + "Post-Phase 15 State" sections.</read_first>
  <action>
Edit `docs/adr/0002-tenant-id-fallback-inventory.md`:

1. In the "Complete Fallback Inventory" table, change the Disposition cell for rows #2, #3, #4 from
   `**ANNOTATED** (...)` to `**REMOVED (Phase 17)** — <one-line note>`:
     - #2 W0_MODULE_GUARD.json — "fallback deleted; guard now fails closed (allowed:false) on blank tenant_id"
     - #3 W1_IN_WA.json — "defaultTenantId/fallback construct removed; tenant comes from channel_identities lookup (Plan 17-01)"
     - #4 W_DRIVER_ONBOARDING.json — "UUID/env fallback removed from queryParams; NOT NULL enforces correctness"
   Leave row #1 (Phase 15, already REMOVED) and row #5 (Phase 21, useEntitlements.ts) unchanged.

2. Rewrite the "## Phase 17 Remaining Work (Occurrences #2, #3, #4)" heading/body to a
   "## Phase 17 Action Taken (Occurrences #2, #3, #4)" past-tense record describing the three removals
   and that the `__inventory_15` annotation keys were stripped from those nodes.

3. Update the "## Post-Phase 15 State" grep note (or add a "## Post-Phase 17 State" section) so it
   states that a post-Phase-17 repo-wide grep for `|| 'default'` / `DEFAULT_TENANT_ID` over
   `workflows/` returns ZERO matches, and that exactly ONE annotated occurrence remains repo-wide —
   #5 (`admin-dashboard/src/hooks/useEntitlements.ts`, Phase 21 scope).

Keep the References section intact; you may add `17-RESEARCH.md` and `phase-17-assertions.yml` to it.
  </action>
  <verify>
    <automated>grep -c "REMOVED (Phase 17)" docs/adr/0002-tenant-id-fallback-inventory.md | { read n; [ "$n" -ge 3 ] || { echo "FAIL: expected >=3 'REMOVED (Phase 17)', got $n"; exit 1; }; }; grep -qi "Post-Phase 17" docs/adr/0002-tenant-id-fallback-inventory.md || { echo "FAIL: no Post-Phase 17 state section"; exit 1; }; echo "PASS ADR updated"</automated>
  </verify>
  <done>
ADR 0002 marks occurrences #2/#3/#4 as `REMOVED (Phase 17)` with notes, records the Phase 17 action
taken, and documents that the post-Phase-17 workflow grep is clean (only #5 remains, Phase 21).
  </done>
</task>

</tasks>

<verification>
- `python3 json.load` passes for W0_MODULE_GUARD.json and W_DRIVER_ONBOARDING.json.
- Guard jsCode passes `node --check`, fails closed, no `|| 'default'` / `DEFAULT_TENANT_ID`.
- Driver queryParams has no UUID/env fallback.
- No `__inventory_15` markers remain in the two workflows.
- ADR 0002 has >=3 `REMOVED (Phase 17)` entries and a Post-Phase-17 state note.
- (Cross-plan) Plan 17-03 structural CI asserts no `DEFAULT_TENANT_ID`/`'default'` across
  W0_MODULE_GUARD + W_DRIVER_ONBOARDING and no `INVENTORY-17` markers anywhere in `workflows/`.
</verification>

<success_criteria>
Every workflow tenant-fallback outside the inbound adapters is removed and fails closed; the
fallback ADR reflects the Phase 17 removals; only the Phase-21 UI occurrence remains repo-wide.
</success_criteria>

<output>
After completion, create
`.planning/phases/17-inbound-tenant-derivation-fail-closed/17-02-SUMMARY.md`.
</output>
