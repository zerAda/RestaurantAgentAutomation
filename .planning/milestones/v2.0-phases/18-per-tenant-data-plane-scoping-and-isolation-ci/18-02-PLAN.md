---
phase: 18-per-tenant-data-plane-scoping-and-isolation-ci
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - workflows/W_ORDER_FINALIZER.json
  - workflows/W51_VIP_WIN_BACK.json
  - workflows/W53_DYNAMIC_KITCHEN_LOAD.json
  - workflows/W_THE_USUAL.json
  - workflows/W_ADMIN_PROACTIVE_AGENT.json
  - workflows/W14_ADMIN_WA_SUPPORT_CONSOLE.json
  - workflows/W4_CORE.json
  - inventory-cms/src/api/order/content-types/order/schema.json
  - inventory-cms/src/api/order/content-types/order/lifecycles.ts
  - inventory-cms/src/api/customer/content-types/customer/schema.json
  - inventory-cms/src/api/customer/content-types/customer/lifecycles.ts
  - db/migrations-strapi/2026-06-20_strapi_order_customer_tenant.sql
autonomous: true
requirements: [TEN-04]
user_setup: []

must_haves:
  truths:
    - "W_ORDER_FINALIZER no longer writes a raw INSERT that omits tenant_id — its order write carries a non-defaultable tenant_id (routed through create_order() or stamped from the sealed body.tenantId), and the order_items INSERT uses the real columns qty/unit_price_cents"
    - "Every previously-unscoped n8n direct-SQL order read/write (W51, W53, W_THE_USUAL, W_ADMIN_PROACTIVE, W14 cancel) now carries a tenant_id filter; no order query reads/writes across tenants; the W4_CORE self-ref UPDATE is tenant-scoped for defense-in-depth"
    - "The Strapi order and customer content types gain required tenant_id (+ restaurant_id) attributes, a beforeCreate lifecycle that throws if tenant_id is absent (non-defaultable, no || 'default'), and the customer phone unique is relaxed to a per-tenant composite"
    - "A db/migrations-strapi/ migration adds tenant_id/restaurant_id columns to the strapi-DB orders/customers tables, backfills to the canonical tenant, sets NOT NULL, and replaces the global phone unique with (tenant_id, phone) — idempotent, with the live apply marked 🔴 VPS-deferred"
    - "No || 'default' / DEFAULT_TENANT_ID fallback is introduced on any fixed order/customer write path"
  artifacts:
    - path: "workflows/W_ORDER_FINALIZER.json"
      provides: "Non-defaultable order write (create_order() or sealed tenant_id stamp) + fixed order_items columns"
      contains: "tenant_id"
    - path: "inventory-cms/src/api/order/content-types/order/schema.json"
      provides: "tenant_id/restaurant_id required attributes on the Strapi order content type"
      contains: "tenant_id"
    - path: "inventory-cms/src/api/customer/content-types/customer/lifecycles.ts"
      provides: "beforeCreate tenant_id injection/validation (throw if absent) for customers"
      contains: "tenant_id"
    - path: "db/migrations-strapi/2026-06-20_strapi_order_customer_tenant.sql"
      provides: "Strapi-DB tenant_id/restaurant_id columns + backfill + NOT NULL + (tenant_id,phone) unique"
      contains: "tenant_id"
  key_links:
    - from: "Strapi order/customer beforeCreate lifecycle"
      to: "event.params.data.tenant_id"
      via: "throw on absent tenant_id (non-defaultable, parity with n8n NOT NULL)"
      pattern: "tenant_id"
    - from: "db/migrations-strapi/2026-06-20_strapi_order_customer_tenant.sql"
      to: "strapi-DB orders/customers"
      via: "ALTER TABLE ADD tenant_id + backfill + SET NOT NULL + (tenant_id,phone) unique"
      pattern: "ALTER TABLE"
    - from: "W51/W53/W_THE_USUAL/W_ADMIN_PROACTIVE/W14 order queries"
      to: "tenant scoping"
      via: "WHERE/AND tenant_id filter on the orders query"
      pattern: "tenant_id"
---

<objective>
Execute the scoping fixes enumerated in `18-SCOPING-CHECKLIST.md` (Plan 18-01): close every unscoped
n8n direct-SQL order path, fix the broken `W_ORDER_FINALIZER` order write so `tenant_id` is
non-defaultable, and close the Strapi sub-plane by adding `tenant_id`/`restaurant_id` to the
`order`/`customer` content types with fail-loud lifecycle injection and a per-tenant `phone` unique —
plus the `db/migrations-strapi/` migration that lands those columns on the strapi DB.

Purpose: This is the core TEN-04 surgery. After 18-01 confirmed O-1 (the Strapi `orders`/`customers`
live in a separate strapi DB with no tenant field), this plan makes both planes non-defaultable: the
n8n plane via `WHERE tenant_id`/`create_order()` (the column is already NOT NULL), and the Strapi plane
via required attributes + a `beforeCreate` throw + a backfilled NOT NULL column + composite unique.

Output: 7 modified workflow JSONs + 2 Strapi schema.json + 2 Strapi lifecycles.ts + 1 new
`db/migrations-strapi/` SQL file. All files are DISJOINT from 18-01 (the checklist) and 18-03 (CI/SQL
assertion files), so this runs as an independent executor pass.

🔴 VPS DEFERRED: applying the `db/migrations-strapi/2026-06-20_strapi_order_customer_tenant.sql`
migration to production Postgres (column add + backfill + NOT NULL + composite-unique index), rebuilding
the CMS, and importing the updated workflows on prod n8n are deferred to a prod-connected session.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-per-tenant-data-plane-scoping-and-isolation-ci/18-RESEARCH.md
@.planning/phases/18-per-tenant-data-plane-scoping-and-isolation-ci/18-SCOPING-CHECKLIST.md
@.claude/skills/11_workflow_governance/SKILL.md

<read_first>
- `18-SCOPING-CHECKLIST.md` (Plan 18-01 output) — the per-path Action column is the spec for this plan.
  If it is not yet written, read `18-RESEARCH.md` §2/§3 as the fallback spec.
- `18-RESEARCH.md` section 2 (exact current SQL per workflow), section 3 (create_order() vs raw INSERT,
  the Strapi lifecycle injection pattern), Pitfall 2 (W_ORDER_FINALIZER drift), Pitfall 3 (phone
  unique), Pitfall 6 (CONCURRENTLY targets postgres:5432 not pgbouncer — VPS only).
- `db/bootstrap.sql:1242-1364` — `public.create_order(p_conversation_key text)` signature/behavior.
- `db/bootstrap.sql:179-205` — n8n `orders` DDL (tenant_id/restaurant_id/channel/customer_userId NOT NULL).
- The target node bodies BEFORE editing: read each of the 7 workflow JSONs' order query node, the two
  Strapi `schema.json`, and `inventory-cms/src/api/order/content-types/order/lifecycles.ts` (the
  existing beforeCreate throw pattern to mirror).
- `db/migrations-strapi/2026-04-06_saas_modules_entitlements.sql` — the strapi-DB migration style/header
  to mirror (this dir targets PGDATABASE=strapi, applied direct to postgres:5432).
</read_first>

<interfaces>
<!-- Exact current SQL (verified by direct read). Preserve params; add only the tenant filter. -->

W_ORDER_FINALIZER.json:38 (BROKEN write — omits tenant_id):
  INSERT INTO orders (customer_phone, "customer_userId", restaurant_id, total_amount, items_summary,
    status, source, metadata) VALUES ($1,$2,$3,$4,$5,'confirmed',$6,$7) RETURNING id;
  :67 (column-drifted): INSERT INTO order_items (order_id, item_code, quantity, price_cents) VALUES ($1,$2,$3,$4);
  Sealed tenant available at: $node["Webhook - Finalize Order"].json.body.tenantId (already used at :200).
  Real order_items columns are `qty` and `unit_price_cents` (NOT quantity/price_cents).

W51_VIP_WIN_BACK.json:28 (cross-tenant aggregate):
  SELECT customer_phone, COUNT(*) as total_orders, MAX(created_at) as last_order_date
  FROM orders GROUP BY customer_phone HAVING COUNT(*) > 5 AND MAX(created_at) < NOW() - INTERVAL '30 days';

W53_DYNAMIC_KITCHEN_LOAD.json:32:
  SELECT COUNT(*) as current_workload FROM orders WHERE status IN ('confirmed','preparing');

W_THE_USUAL.json:21:
  SELECT oi.label, count(*) as freq FROM orders o JOIN order_items oi ON o.order_id = oi.order_id
  WHERE o."customer_userId" = $1 AND EXTRACT(HOUR FROM o.created_at) BETWEEN ... GROUP BY oi.label ...;

W_ADMIN_PROACTIVE_AGENT.json:32:
  WITH sales AS (SELECT SUM(total_amount) as revenue, COUNT(*) as volume FROM orders
    WHERE created_at > now() - interval '4 hours'), abandoned AS (SELECT COUNT(*) ... FROM carts
    WHERE updated_at > now() - interval '4 hours') SELECT ...;

W14_ADMIN_WA_SUPPORT_CONSOLE.json:1463:
  UPDATE orders SET status='CANCELLED', payment_status='CANCELLED', updated_at=now()
  WHERE order_id=$1::uuid AND status NOT IN ('DONE','CANCELLED') RETURNING order_id;

W4_CORE.json:395 (defense-in-depth self-ref UPDATE):
  WITH ord AS (SELECT * FROM create_order($1)), upd AS (UPDATE orders SET attributed_campaign=(...)
  WHERE id=(SELECT COALESCE(order_id,id) FROM ord LIMIT 1) RETURNING 1) SELECT * FROM ord;

Strapi order/schema.json: collectionName "orders", attributes have NO tenant_id/restaurant_id.
Strapi customer/schema.json: collectionName "customers", `phone` {type:string, unique:true, required:true}, NO tenant_id.
order/lifecycles.ts beforeCreate already does `throw new Error(...)` on empty items — mirror that throw style.

Canonical CI tenant/restaurant (for backfill default + assertions parity):
  tenant   00000000-0000-0000-0000-000000000001
  restaurant 00000000-0000-0000-0000-000000000000
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scope the n8n direct-SQL order paths + fix the W_ORDER_FINALIZER write</name>
  <files>workflows/W_ORDER_FINALIZER.json, workflows/W51_VIP_WIN_BACK.json, workflows/W53_DYNAMIC_KITCHEN_LOAD.json, workflows/W_THE_USUAL.json, workflows/W_ADMIN_PROACTIVE_AGENT.json, workflows/W14_ADMIN_WA_SUPPORT_CONSOLE.json, workflows/W4_CORE.json</files>
  <read_first>18-SCOPING-CHECKLIST.md Inventory + Sweep Decisions; the &lt;interfaces&gt; block above
  for the exact current SQL; 18-RESEARCH.md §3 (create_order) + Pitfall 2 (finalizer drift) + Pitfall 4
  (sweeps).</read_first>
  <action>
Edit each workflow's order query node (preserve everything else — node ids, positions, credentials,
other params). Apply EXACTLY the fix recorded in `18-SCOPING-CHECKLIST.md` for each path. If the
checklist is unavailable, use these (its source spec):

W_ORDER_FINALIZER.json — fix the broken write (non-defaultable tenant_id, no `|| 'default'`):
  - Since the finalizer has no `conversation_key`, the minimal-correct form is to ADD
    `tenant_id, restaurant_id` to the column list and source them from the sealed body, NOT a default:
      INSERT INTO orders (tenant_id, restaurant_id, customer_phone, "customer_userId", total_amount,
        items_summary, status, source, metadata)
      VALUES ($1, $2, $3, $4, $5, $6, 'confirmed', $7, $8) RETURNING id;
    Update its `additionalFields.queryParams` so $1=tenantId, $2=restaurant_id come from
    `$node["Webhook - Finalize Order"].json.body.tenantId` / `.restaurant_id` (NO `|| 'default'`,
    NO env fallback — an absent tenant must hit the NOT NULL violation loudly), and renumber the
    remaining params. (If you prefer routing through `public.create_order()`, document why in the
    SUMMARY and keep it non-defaultable.)
  - Fix the order_items INSERT column drift: `INSERT INTO order_items (order_id, item_code, quantity,
    price_cents)` -> `INSERT INTO order_items (order_id, item_code, qty, unit_price_cents)`.

W51_VIP_WIN_BACK.json — per the Sweep Decision (O-2). The `orders` aggregate MUST carry a `tenant_id`
  filter so it no longer aggregates across all tenants (per-tenant scoping or per-tenant loop, whichever
  the checklist chose).

W53_DYNAMIC_KITCHEN_LOAD.json — add `AND tenant_id = $1` (i.e. `WHERE status IN ('confirmed','preparing')
  AND tenant_id = $1`) with the tenant param supplied.

W_THE_USUAL.json — add `AND o.tenant_id = $2` (param 2) to the WHERE clause; supply the tenant from the
  sealed context alongside the existing `$1` customer_userId.

W_ADMIN_PROACTIVE_AGENT.json — add `AND tenant_id = $1` to the `sales` CTE `FROM orders`. The
  `abandoned` CTE reads `carts` (no tenant_id column) — scope it via the chosen checklist approach
  (e.g. join to conversation_state, or per-tenant loop); at minimum the orders aggregate MUST carry the
  tenant filter. Document the carts approach in the SUMMARY.

W14_ADMIN_WA_SUPPORT_CONSOLE.json — add `AND tenant_id = $2::uuid` to the cancel UPDATE WHERE clause
  (param 2), supplying the admin's tenant context so an admin cannot cancel another tenant's order.

W4_CORE.json — defense-in-depth: add `AND tenant_id = (SELECT tenant_id FROM ord)` to the self-ref
  `UPDATE orders ... WHERE id=(...)`.

Use `tenant_id = $N` parameterized placeholders (never string-interpolate the tenant). Do NOT add any
`|| 'default'`, `DEFAULT_TENANT_ID`, or hardcoded `'00000000-...'` on any tenant path. Each file must
remain valid JSON.
  </action>
  <verify>
    <automated>set -e; for f in W_ORDER_FINALIZER W51_VIP_WIN_BACK W53_DYNAMIC_KITCHEN_LOAD W_THE_USUAL W_ADMIN_PROACTIVE_AGENT W14_ADMIN_WA_SUPPORT_CONSOLE W4_CORE; do python3 -c "import json; json.load(open('workflows/$f.json'))" || { echo "FAIL invalid JSON $f"; exit 1; }; done; FQ=$(jq -r '.nodes[]|select(.parameters.query)|.parameters.query' workflows/W_ORDER_FINALIZER.json); echo "$FQ" | grep -q "INSERT INTO orders" && echo "$FQ" | grep -q "tenant_id" || { echo "FAIL: finalizer orders INSERT missing tenant_id"; exit 1; }; echo "$FQ" | grep -Eq "qty|unit_price_cents" || { echo "FAIL: order_items column drift not fixed"; exit 1; }; if echo "$FQ" | grep -Eq "quantity, price_cents"; then echo "FAIL: old order_items columns remain"; exit 1; fi; for f in W51_VIP_WIN_BACK W53_DYNAMIC_KITCHEN_LOAD W_THE_USUAL W_ADMIN_PROACTIVE_AGENT W14_ADMIN_WA_SUPPORT_CONSOLE; do jq -r '.nodes[]|select(.parameters.query)|.parameters.query' workflows/$f.json | grep -q "tenant_id" || { echo "FAIL: $f order query not tenant-scoped"; exit 1; }; done; for f in W_ORDER_FINALIZER W51_VIP_WIN_BACK W53_DYNAMIC_KITCHEN_LOAD W_THE_USUAL W_ADMIN_PROACTIVE_AGENT W14_ADMIN_WA_SUPPORT_CONSOLE W4_CORE; do if grep -Eq "\|\| *'default'|DEFAULT_TENANT_ID" workflows/$f.json; then echo "FAIL: fallback in $f"; exit 1; fi; done; echo "PASS n8n order paths scoped + finalizer fixed"</automated>
  </verify>
  <done>
All 7 workflows are valid JSON; `W_ORDER_FINALIZER`'s order INSERT lists `tenant_id` (sourced from the
sealed body, no default) and its order_items INSERT uses `qty`/`unit_price_cents`; W51/W53/W_THE_USUAL/
W_ADMIN_PROACTIVE/W14 order queries each carry a `tenant_id` filter; the W4_CORE self-ref UPDATE is
tenant-scoped; no `|| 'default'`/`DEFAULT_TENANT_ID` remains in any of the 7 files.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add tenant_id to the Strapi order/customer content types + fail-loud lifecycle injection</name>
  <files>inventory-cms/src/api/order/content-types/order/schema.json, inventory-cms/src/api/order/content-types/order/lifecycles.ts, inventory-cms/src/api/customer/content-types/customer/schema.json, inventory-cms/src/api/customer/content-types/customer/lifecycles.ts</files>
  <behavior>
    - order/schema.json gains `tenant_id` (string, required:true) and `restaurant_id` (string, required:true) attributes; no `default`.
    - customer/schema.json gains `tenant_id` (string, required:true); its `phone` is no longer `unique:true` at the schema level (uniqueness moves to the DB composite `(tenant_id, phone)`).
    - order/lifecycles.ts beforeCreate throws if `event.params.data.tenant_id` is absent/blank (non-defaultable; mirrors the existing empty-items throw) and does NOT default it.
    - customer/lifecycles.ts beforeCreate throws if `event.params.data.tenant_id` is absent/blank.
    - No `|| 'default'` or hardcoded tenant UUID anywhere in either lifecycle.
  </behavior>
  <action>
order/schema.json: add to `attributes` (keep all existing attrs):
  "tenant_id": { "type": "string", "required": true },
  "restaurant_id": { "type": "string", "required": true }
(Use `"type": "string"` to store the canonical UUID as text — Strapi has no native uuid scalar; this
parallels how the strapi DB stores the n8n canonical UUID. Do NOT add a `default`.)

customer/schema.json: add `"tenant_id": { "type": "string", "required": true }`; CHANGE the `phone`
attribute from `{ "type":"string", "unique":true, "required":true }` to
`{ "type":"string", "required":true }` (drop the schema-level global `unique`; per-tenant uniqueness is
enforced by the DB composite index in Task 3). Keep `required:true`.

order/lifecycles.ts: in `beforeCreate(event)`, BEFORE the existing items logic, add a non-defaultable
guard mirroring the existing `throw new Error(...)` style:
  const tenantId = (event.params.data.tenant_id || '').toString().trim();
  if (!tenantId) {
    throw new Error('tenant_id is required for an order (non-defaultable, no default tenant).');
  }
Do NOT substitute a default. Leave the price-recompute / idempotence logic intact.

customer/lifecycles.ts: this file does NOT exist yet — CREATE it as a Strapi lifecycles module
(`export default { async beforeCreate(event: any) { ... } }`) with the same non-defaultable tenant_id
guard:
  const tenantId = (event.params.data.tenant_id || '').toString().trim();
  if (!tenantId) {
    throw new Error('tenant_id is required for a customer (non-defaultable, no default tenant).');
  }

ANTI-PATTERN GUARD: no `|| 'default'`, no `DEFAULT_TENANT_ID`, no hardcoded `'00000000-...'` in either
lifecycle — an absent tenant MUST throw, parity with the n8n NOT NULL constraint.
  </action>
  <verify>
    <automated>set -e; for s in inventory-cms/src/api/order/content-types/order/schema.json inventory-cms/src/api/customer/content-types/customer/schema.json; do python3 -c "import json; json.load(open('$s'))" || { echo "FAIL invalid JSON $s"; exit 1; }; done; jq -e '.attributes.tenant_id' inventory-cms/src/api/order/content-types/order/schema.json >/dev/null || { echo "FAIL: order schema missing tenant_id"; exit 1; }; jq -e '.attributes.restaurant_id' inventory-cms/src/api/order/content-types/order/schema.json >/dev/null || { echo "FAIL: order schema missing restaurant_id"; exit 1; }; jq -e '.attributes.tenant_id' inventory-cms/src/api/customer/content-types/customer/schema.json >/dev/null || { echo "FAIL: customer schema missing tenant_id"; exit 1; }; if jq -e '.attributes.phone.unique == true' inventory-cms/src/api/customer/content-types/customer/schema.json >/dev/null; then echo "FAIL: customer.phone still globally unique"; exit 1; fi; test -f inventory-cms/src/api/customer/content-types/customer/lifecycles.ts || { echo "FAIL: customer lifecycles.ts missing"; exit 1; }; for l in inventory-cms/src/api/order/content-types/order/lifecycles.ts inventory-cms/src/api/customer/content-types/customer/lifecycles.ts; do grep -q "tenant_id" "$l" || { echo "FAIL: $l missing tenant_id guard"; exit 1; }; grep -qi "throw" "$l" || { echo "FAIL: $l missing throw"; exit 1; }; if grep -Eq "\|\| *'default'|DEFAULT_TENANT_ID|00000000-0000-0000-0000-000000000001" "$l"; then echo "FAIL: fallback in $l"; exit 1; fi; done; echo "PASS Strapi tenant attributes + fail-loud lifecycles"</automated>
  </verify>
  <done>
Both Strapi schemas are valid JSON; `order/schema.json` has required `tenant_id` + `restaurant_id`;
`customer/schema.json` has required `tenant_id` and `phone` is no longer schema-level `unique`;
`order/lifecycles.ts` and the new `customer/lifecycles.ts` each throw on a blank `tenant_id` with no
default/fallback.
  </done>
</task>

<task type="auto">
  <name>Task 3: Strapi-DB migration — tenant_id/restaurant_id columns + backfill + composite phone unique</name>
  <files>db/migrations-strapi/2026-06-20_strapi_order_customer_tenant.sql</files>
  <read_first>db/migrations-strapi/2026-04-06_saas_modules_entitlements.sql for the header/idempotency
  style (PGDATABASE=strapi, direct to postgres:5432); 18-RESEARCH.md Pitfall 6 (CONCURRENTLY targets
  postgres:5432 not pgbouncer — VPS-only note); the canonical CI UUIDs in &lt;interfaces&gt;.</read_first>
  <action>
Create `db/migrations-strapi/2026-06-20_strapi_order_customer_tenant.sql` (target: the **strapi** DB —
the kiosk/CMS plane confirmed by 18-01 O-1). Header comment matching the existing migrations-strapi
file. Make it fully idempotent (re-running is a no-op). Steps, in order:

1. Add the columns idempotently:
   `ALTER TABLE orders ADD COLUMN IF NOT EXISTS tenant_id uuid;`
   `ALTER TABLE orders ADD COLUMN IF NOT EXISTS restaurant_id uuid;`
   `ALTER TABLE customers ADD COLUMN IF NOT EXISTS tenant_id uuid;`

2. Backfill existing rows to the canonical tenant/restaurant BEFORE flipping NOT NULL:
   `UPDATE orders SET tenant_id='00000000-0000-0000-0000-000000000001', restaurant_id='00000000-0000-0000-0000-000000000000' WHERE tenant_id IS NULL;`
   `UPDATE customers SET tenant_id='00000000-0000-0000-0000-000000000001' WHERE tenant_id IS NULL;`

3. Flip NOT NULL (re-running SET NOT NULL on an already-NOT NULL column is a no-op):
   `ALTER TABLE orders ALTER COLUMN tenant_id SET NOT NULL;`
   `ALTER TABLE orders ALTER COLUMN restaurant_id SET NOT NULL;`
   `ALTER TABLE customers ALTER COLUMN tenant_id SET NOT NULL;`

4. Replace the global phone unique with a per-tenant composite:
   `ALTER TABLE customers DROP CONSTRAINT IF EXISTS customers_phone_unique;`
   `ALTER TABLE customers DROP CONSTRAINT IF EXISTS customers_phone_key;`
   `DROP INDEX IF EXISTS customers_phone_unique;`
   then the CI/local form (plain PG, no pgbouncer):
   `CREATE UNIQUE INDEX IF NOT EXISTS uq_customers_tenant_phone ON customers (tenant_id, phone);`
   and a COMMENTED 🔴 VPS form for the live table:
   `-- 🔴 VPS: CREATE UNIQUE INDEX CONCURRENTLY uq_customers_tenant_phone ON customers (tenant_id, phone);`
   `-- NOTE: CONCURRENTLY must run direct to postgres:5432, NOT pgbouncer:6432 (Pitfall 6).`

5. Header note block: "🔴 VPS DEFERRED — this migration alters the LIVE strapi-DB orders/customers
   tables; apply it in a prod-connected session via the migrations-strapi PGDATABASE=strapi pass direct
   to postgres:5432. Backfill (step 2) MUST complete before the NOT NULL flip (step 3)."

The file must be valid SQL that applies cleanly TWICE against a fresh ephemeral Postgres `orders`/
`customers` table (idempotency — see verify).
  </action>
  <verify>
    <automated>f=db/migrations-strapi/2026-06-20_strapi_order_customer_tenant.sql; test -f "$f" || { echo "FAIL missing"; exit 1; }; grep -q "ADD COLUMN IF NOT EXISTS tenant_id" "$f" || { echo "FAIL no idempotent tenant_id add"; exit 1; }; grep -q "restaurant_id" "$f" || { echo "FAIL no restaurant_id"; exit 1; }; grep -qi "SET NOT NULL" "$f" || { echo "FAIL no NOT NULL flip"; exit 1; }; grep -qi "uq_customers_tenant_phone" "$f" || { echo "FAIL no composite unique"; exit 1; }; grep -qi "VPS" "$f" || { echo "FAIL no VPS note"; exit 1; }; grep -q "00000000-0000-0000-0000-000000000001" "$f" || { echo "FAIL no canonical backfill"; exit 1; }; echo "PASS migration structural checks (run the ephemeral-PG apply-twice idempotency check from 18-VALIDATION.md Test Infrastructure to prove re-runnability)"</automated>
  </verify>
  <done>
`db/migrations-strapi/2026-06-20_strapi_order_customer_tenant.sql` exists, adds `tenant_id`/`restaurant_id`
to orders + `tenant_id` to customers idempotently (`IF NOT EXISTS`), backfills to the canonical tenant
`00000000-0000-0000-0000-000000000001`, flips NOT NULL, drops the global phone unique and creates the
`(tenant_id, phone)` composite, and carries the 🔴 VPS-deferred + pgbouncer note. It applies cleanly
twice on an ephemeral Postgres (see 18-VALIDATION.md local-run mechanism).
  </done>
</task>

</tasks>

<verification>
- `python3 json.load` passes for all 7 workflows and both Strapi schemas.
- `W_ORDER_FINALIZER` order INSERT lists `tenant_id`; order_items uses `qty`/`unit_price_cents`.
- W51/W53/W_THE_USUAL/W_ADMIN_PROACTIVE/W14 order queries each carry a `tenant_id` filter.
- Order + customer Strapi schemas have required `tenant_id`; customer `phone` no longer schema-`unique`.
- Both lifecycles throw on blank `tenant_id`; no `|| 'default'`/`DEFAULT_TENANT_ID`/hardcoded UUID.
- The migrations-strapi SQL adds columns + backfill + NOT NULL + `(tenant_id,phone)` unique, idempotent.
- (Cross-plan) Plan 18-03's `workflow-structural` job asserts the same scoping/attributes once this lands.
</verification>

<success_criteria>
Every previously-unscoped order/customer read and write is closed: the n8n direct-SQL paths carry a
`tenant_id` filter, the broken `W_ORDER_FINALIZER` write is non-defaultable and column-correct, and the
Strapi sub-plane gains required `tenant_id`/`restaurant_id` + fail-loud lifecycle injection + a
per-tenant `phone` unique — with the live apply 🔴 VPS-deferred. `tenant_id` is non-defaultable on every
write path (NOT NULL / required + throw, no `|| 'default'`), satisfying Phase 18 success-criterion 2.
</success_criteria>

<output>
After completion, create
`.planning/phases/18-per-tenant-data-plane-scoping-and-isolation-ci/18-02-SUMMARY.md`.
</output>
