---
phase: 05-test-coverage-n8n-workflow-e2e
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements: [TEST-11]

must_haves:
  truths:
    - "Workflow E2E tests run in CI on main and release branches without a live VPS"
    - "The CI job uses the existing docker-compose.test.yml stack"
    - "CI failure in E2E tests uploads Docker logs as artifacts for debugging"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "n8n-workflow-e2e CI job"
      contains: "n8n-workflow-e2e"
  key_links:
    - from: ".github/workflows/ci.yml (n8n-workflow-e2e job)"
      to: "scripts/test-n8n-e2e.sh"
      via: "bash scripts/test-n8n-e2e.sh invocation"
      pattern: "bash scripts/test-n8n-e2e.sh"
    - from: ".github/workflows/ci.yml (n8n-workflow-e2e job)"
      to: "test-harness job"
      via: "needs dependency (runs after test-harness)"
      pattern: "needs:.*test-harness"
---

<objective>
Wire the E2E test script into CI as a new `n8n-workflow-e2e` job in ci.yml.

Purpose: TEST-11 requires workflow smoke tests to run in CI without a live VPS. The new job runs `scripts/test-n8n-e2e.sh` (created in Plan 01) after the existing test-harness job, reusing the same compose stack pattern. This ensures inbound adapter and outbox retry tests run automatically on every main/release push.

Output: Updated `.github/workflows/ci.yml` with a new `n8n-workflow-e2e` job.
</objective>

<execution_context>
@C:/Users/mon pc/Desktop/ralphé_final_patch/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/mon pc/Desktop/ralphé_final_patch/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-test-coverage-n8n-workflow-e2e/05-RESEARCH.md
@.planning/phases/05-test-coverage-n8n-workflow-e2e/05-01-SUMMARY.md

<interfaces>
<!-- Existing CI job structure from .github/workflows/ci.yml -->

Job dependency graph (from ci.yml header):
```
integrity-gate --> test-harness (main/release only, needs: integration-tests)
                       |
                  ci-summary (needs: all, if: always())
```

Existing test-harness job pattern (lines 696-725):
```yaml
  test-harness:
    name: Test Harness (Full Stack)
    runs-on: ubuntu-latest
    needs: [integrity-gate, integration-tests]
    if: github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/heads/release/')
    timeout-minutes: 30
    steps:
      - name: Checkout code
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
      - name: Run test harness
        run: |
          chmod +x scripts/test_harness.sh
          bash scripts/test_harness.sh
      - name: Upload Docker logs on failure
        if: failure()
        uses: actions/upload-artifact@65462800fd760344b1a7b4382951275a0abb4808 # v4.6.0
        ...
```

ci-summary job (needs all other jobs):
```yaml
  ci-summary:
    needs:
      - ...
      - test-harness
    if: always()
```

SHA-pinned actions used in CI:
- actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
- actions/upload-artifact@65462800fd760344b1a7b4382951275a0abb4808 # v4.6.0
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add n8n-workflow-e2e job to ci.yml and update ci-summary dependencies</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml
    - scripts/test-n8n-e2e.sh
  </read_first>
  <action>
Add a new job `n8n-workflow-e2e` to `.github/workflows/ci.yml`. Insert it BETWEEN the `test-harness` job and the `ci-summary` job. The new job follows the exact same pattern as `test-harness` but runs `test-n8n-e2e.sh` instead.

**New job to insert (after the test-harness job closing, before ci-summary):**

```yaml
  # ==========================================================================
  # JOB 8b: n8n Workflow E2E Tests (main/release only, after test-harness)
  # ==========================================================================
  n8n-workflow-e2e:
    name: n8n Workflow E2E
    runs-on: ubuntu-latest
    needs: [integrity-gate, test-harness]
    if: github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/heads/release/')
    timeout-minutes: 30

    steps:
      - name: Checkout code
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Run n8n E2E tests
        run: |
          chmod +x scripts/test-n8n-e2e.sh
          bash scripts/test-n8n-e2e.sh

      - name: Upload Docker logs on failure
        if: failure()
        run: |
          mkdir -p n8n-e2e-logs
          docker compose -f docker/docker-compose.test.yml logs --no-color > n8n-e2e-logs/compose.log 2>&1 || true
          docker ps -a > n8n-e2e-logs/containers.log 2>&1 || true
          docker compose -f docker/docker-compose.test.yml exec -T redis redis-cli LRANGE ralphe:outbox:pending 0 -1 > n8n-e2e-logs/redis-outbox.log 2>&1 || true
          docker compose -f docker/docker-compose.test.yml exec -T redis redis-cli LRANGE ralphe:outbox:dlq 0 -1 > n8n-e2e-logs/redis-dlq.log 2>&1 || true

      - name: Upload failure artifacts
        if: failure()
        uses: actions/upload-artifact@65462800fd760344b1a7b4382951275a0abb4808 # v4.6.0
        with:
          name: n8n-e2e-logs
          path: n8n-e2e-logs/
          retention-days: 14
```

**Also update the ci-summary job** to add `n8n-workflow-e2e` to its `needs` list. Find the `ci-summary` job's `needs:` array and add `- n8n-workflow-e2e` after `- test-harness`.

**Also update the ci-summary step** that generates the markdown summary table. Add a new row after the `| Test Harness |` row:
```
          | n8n Workflow E2E | ${{ needs.n8n-workflow-e2e.result == 'success' && 'PASS' || needs.n8n-workflow-e2e.result }} |
```

**Also update the dependency graph comment** at the top of ci.yml (lines 5-13). Add `n8n-workflow-e2e` after test-harness in the diagram:
```
#                    └──► test-harness (main/release only, needs: integration-tests)
#                              └──► n8n-workflow-e2e (main/release only, needs: test-harness)
#                                        │
#                         ci-summary (needs: all, if: always())
```

**Key constraints:**
- All actions MUST be SHA-pinned (same SHAs as existing jobs — see interfaces section above)
- The `if:` condition MUST match test-harness: `github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/heads/release/')`
- The job needs `test-harness` because test-n8n-e2e.sh spins up its own stack (it's self-contained) but logically depends on the harness passing first
- timeout-minutes: 30 (same as test-harness — E2E tests need up to 120s for compose + 35s for CRON wait)
  </action>
  <verify>
    <automated>grep -c "n8n-workflow-e2e" .github/workflows/ci.yml | grep -qE "^[4-9]|^[1-9][0-9]" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - .github/workflows/ci.yml contains job `n8n-workflow-e2e:` with `name: n8n Workflow E2E`
    - Job has `needs: [integrity-gate, test-harness]`
    - Job has `if: github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/heads/release/')`
    - Job runs `bash scripts/test-n8n-e2e.sh`
    - Job uses SHA-pinned checkout action `actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683`
    - Job uses SHA-pinned upload-artifact action `actions/upload-artifact@65462800fd760344b1a7b4382951275a0abb4808`
    - Job uploads `n8n-e2e-logs/` on failure (includes compose.log, containers.log, redis-outbox.log, redis-dlq.log)
    - ci-summary job `needs:` array includes `n8n-workflow-e2e`
    - ci-summary markdown table includes `| n8n Workflow E2E |` row
    - Dependency graph comment at top of file mentions `n8n-workflow-e2e`
  </acceptance_criteria>
  <done>ci.yml has a new n8n-workflow-e2e job that runs test-n8n-e2e.sh on main/release pushes, with failure artifact upload and ci-summary integration</done>
</task>

</tasks>

<verification>
1. `grep -c "n8n-workflow-e2e" .github/workflows/ci.yml` returns >= 4 (job name, needs ref in ci-summary, summary table, comment)
2. `grep "bash scripts/test-n8n-e2e.sh" .github/workflows/ci.yml` returns a match
3. `grep "needs:.*test-harness" .github/workflows/ci.yml` confirms the dependency chain
4. `grep "n8n-e2e-logs" .github/workflows/ci.yml` confirms artifact upload path
5. YAML syntax is valid (no tab characters, consistent indentation)
</verification>

<success_criteria>
- ci.yml has a new n8n-workflow-e2e job that runs after test-harness on main/release branches
- The job runs scripts/test-n8n-e2e.sh and uploads Docker + Redis logs on failure
- ci-summary includes the new job in its needs and summary table
- All actions are SHA-pinned (supply chain security)
- The dependency graph comment at the top of ci.yml is updated
</success_criteria>

<output>
After completion, create `.planning/phases/05-test-coverage-n8n-workflow-e2e/05-02-SUMMARY.md`
</output>
