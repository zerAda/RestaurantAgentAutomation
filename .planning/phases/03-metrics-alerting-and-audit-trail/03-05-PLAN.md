---
phase: 03-metrics-alerting-and-audit-trail
plan: "05"
type: execute
wave: 4
depends_on:
  - "03-03"
  - "03-04"
files_modified:
  - admin-dashboard/src/pages/AuditLogView.tsx
  - admin-dashboard/src/App.tsx
autonomous: false
requirements:
  - AUDIT-03
must_haves:
  truths:
    - "Authenticated admin can navigate to /audit-log in the dashboard sidebar"
    - "The audit log table shows workflow_name, channel, status, started_at, duration_ms columns"
    - "Admin can filter by date range (start date + end date pickers)"
    - "Admin can filter by workflow name (text input, partial match)"
    - "The table paginates at 50 rows per page with Previous/Next controls"
    - "Loading and empty states are handled gracefully (skeleton or 'No entries' message)"
    - "nginx.conf contains a location block matching /v1/internal/ that proxies to n8n, and GET /v1/internal/audit-log returns non-502 before the dashboard component is deployed"
  artifacts:
    - path: "admin-dashboard/src/pages/AuditLogView.tsx"
      provides: "Audit log React page with filters and paginated table"
      min_lines: 120
    - path: "admin-dashboard/src/App.tsx"
      provides: "Route /audit-log and sidebar nav item under Advanced section"
      contains: "audit-log"
  key_links:
    - from: "admin-dashboard/src/pages/AuditLogView.tsx"
      to: "/v1/internal/audit-log webhook"
      via: "fetch via strapiClient.rawGet or direct fetch to n8n internal API"
      pattern: "audit-log"
    - from: "admin-dashboard/src/App.tsx"
      to: "AuditLogView.tsx"
      via: "React Router <Route path='/audit-log' />"
      pattern: "AuditLogView"
    - from: "infra/gateway/nginx.conf"
      to: "n8n-main:5678"
      via: "location /v1/internal/ proxy_pass block"
      pattern: "location.*v1/internal"
user_setup: []
---

<objective>
Add the audit log view to the admin dashboard: a page where operators can search the
workflow_audit table by date range and workflow name.

Purpose: AUDIT-03 requires the admin dashboard to expose the audit trail. This is the operator
interface to the data written by the three inbound adapters and queryable via W_AUDIT_QUERY.

Output:
- `admin-dashboard/src/pages/AuditLogView.tsx` — new React page
- `admin-dashboard/src/App.tsx` — route + sidebar nav item registered
</objective>

<execution_context>
@C:/Users/mon pc/Desktop/ralphé_final_patch/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/mon pc/Desktop/ralphé_final_patch/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-metrics-alerting-and-audit-trail/03-CONTEXT.md

<interfaces>
<!-- strapiClient.ts rawGet pattern (already in codebase): -->
rawGet: <T>(path: string, options?: RequestOptions) => request<T>(path, options)
// path is appended to VITE_STRAPI_URL. But audit-log is served by n8n, not Strapi.
// Use direct fetch() or a dedicated n8n fetch utility.

<!-- Existing API call pattern in the dashboard (from DashboardHome.tsx / AnalyticsView.tsx): -->
// Most pages use strapi.find() or strapi.rawGet() via strapiClient.
// For n8n internal webhook, we need a different base URL.
// The n8n internal API is NOT accessible from the browser (n8n is on the internal network).
// SOLUTION: Route the audit-log query THROUGH the gateway:
//   GET https://api.srv1258231.hstgr.cloud/v1/internal/audit-log?...params
//   This works because /v1/internal/ proxies to n8n (see nginx.conf line 329-332).
//   The gateway requires X-Api-Token header for /v1/internal/ (internal_token rate limit zone).
//   However, the admin dashboard does NOT currently send X-Api-Token to n8n internal routes.
//   SIMPLER ALTERNATIVE: Use the gateway's /v1/portal/ proxy to a new Strapi endpoint that
//   queries the ops.workflow_audit table directly... but that adds Strapi complexity.
//   SIMPLEST: Make W_AUDIT_QUERY accessible via /v1/internal/audit-log but require the
//   Strapi JWT (Bearer token) as auth. The gateway /v1/internal/ accepts internal_token zone
//   but does NOT enforce specific token format. The existing dashboard token can be forwarded.
//   Actually: the gateway rate-limits /v1/internal/ by $http_x_api_token. If X-Api-Token is
//   absent, all requests share one bucket (empty string key). This is fine for low-volume
//   admin dashboard use.

// FINAL DECISION: AuditLogView fetches directly from the public API gateway:
//   const API_BASE = import.meta.env.VITE_API_BASE || 'https://api.srv1258231.hstgr.cloud';
//   fetch(`${API_BASE}/v1/internal/audit-log?${params}`, {
//     headers: { 'Authorization': `Bearer ${getToken()}` }
//   })
// This passes the Strapi JWT as Bearer. W_AUDIT_QUERY does not validate auth
// (it's an internal webhook, already protected by the gateway's IP allowlist at Traefik level).
// The admin dashboard is already behind Traefik BasicAuth + IP allowlist.

<!-- Existing page pattern (from AnalyticsView.tsx, CustomerView.tsx): -->
// Pages are functional React components exported as named exports
// Use useState, useEffect, useCallback
// Use Tailwind classes from the quantum design system
// Use quantum-card, diamond-card class names for card containers
// Use text-zinc-400 for labels, text-white for values, text-brand-primary for accents
// Table rows: divide-y divide-white/5, hover:bg-white/5
// Status badges: rounded-full px-2 py-0.5 text-[10px] font-bold uppercase
//   success=text-success bg-success/10, error=text-red-400 bg-red-400/10

<!-- App.tsx route registration pattern: -->
// Import at top: import { AuditLogView } from './pages/AuditLogView';
// Route inside isFullAdmin block:
//   <Route path="/audit-log" element={<ViewWrapper activeKey="audit-log" component={<AuditLogView />} />} />
// Sidebar NavItem in "Advanced" section:
//   <NavItem active={activeTab === 'audit-log'} onClick={() => { navigate('/audit-log'); setIsMobileMenuOpen(false); }} label="Audit Log" icon={FileText} lang={lang} />
// Import FileText from lucide-react (already imported set, just add FileText).

<!-- W_AUDIT_QUERY response shape: -->
{
  "rows": [
    {
      "id": 1,
      "workflow_name": "W1 - IN WhatsApp Adapter",
      "workflow_id": "abc",
      "execution_id": "exec-123",
      "channel": "whatsapp",
      "status": "completed",
      "started_at": "2026-03-23T10:00:00Z",
      "completed_at": "2026-03-23T10:00:02Z",
      "duration_ms": 2000,
      "tenant_id": null,
      "correlation_id": "uuid",
      "error_message": null,
      "created_at": "2026-03-23T10:00:00Z"
    }
  ]
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AuditLogView.tsx — audit log page</name>
  <files>admin-dashboard/src/pages/AuditLogView.tsx</files>
  <action>
Create `admin-dashboard/src/pages/AuditLogView.tsx`. This is a React functional component.

The component must:
1. Have a date range filter: two `<input type="date">` fields (start_date, end_date).
   Default start_date = 7 days ago (ISO date string `YYYY-MM-DD`).
   Default end_date = today.
2. Have a workflow name filter: `<input type="text" placeholder="Filtrer par workflow...">`.
3. Have a page counter state (current page, default 1). Reset to page 1 when filters change.
4. Fetch from `${N8N_API_BASE}/v1/internal/audit-log?start_date=...&end_date=...&workflow_name=...&page=N&page_size=50`
   where `N8N_API_BASE = import.meta.env.VITE_API_BASE || 'https://api.' + (import.meta.env.VITE_DOMAIN || 'srv1258231.hstgr.cloud')`.
   Use `Authorization: Bearer ${getToken()}` header (import `getToken` from `../services/strapiClient`).
5. Show a loading skeleton (4-5 skeleton rows with animate-pulse) while fetching.
6. Show "Aucune entrée trouvée" (FR) when rows array is empty.
7. Show a table with columns: Workflow, Canal, Statut, Début, Durée, Correlation ID.
8. Statut badge colors: completed=green, started=blue/brand, failed=red.
9. Duration: show `${duration_ms}ms` or `—` if null.
10. Correlation ID: truncate to 8 chars with full value in `title` attribute.
11. Previous/Next pagination buttons. Show current page number. Disable Previous on page 1.
    Disable Next when `rows.length < 50` (less than a full page means last page).
12. Use a `useCallback` for the fetch function with `[startDate, endDate, workflowFilter, page]` deps.
13. Use a `useEffect` to call the fetch function on mount and when deps change.

Design tokens to use (consistent with dashboard):
- Container: `<div className="space-y-6 animate-in fade-in slide-in-from-bottom-8 duration-700">`
- Filter card: `<div className="quantum-card p-6 rounded-2xl">`
- Table wrapper: `<div className="quantum-card rounded-2xl overflow-hidden">`
- Table: `<table className="w-full text-sm">`
- Table header: `<th className="px-4 py-3 text-left text-[10px] font-bold text-zinc-500 uppercase tracking-widest border-b border-white/5">`
- Table rows: `<tr className="border-b border-white/5 hover:bg-white/5 transition-colors">`
- Table cells: `<td className="px-4 py-3 text-zinc-300">`

Export as a named export: `export function AuditLogView() { ... }`

The TypeScript interface for a row:
```typescript
interface AuditRow {
  id: number;
  workflow_name: string;
  workflow_id: string;
  execution_id: string;
  channel: string;
  status: 'started' | 'completed' | 'failed';
  started_at: string;
  completed_at: string | null;
  duration_ms: number | null;
  tenant_id: string | null;
  correlation_id: string | null;
  error_message: string | null;
  created_at: string;
}
```
  </action>
  <verify>
    <automated>node -e "const ts=require('fs').readFileSync('admin-dashboard/src/pages/AuditLogView.tsx','utf8'); console.log('lines:', ts.split('\n').length); console.log('has export:', ts.includes('export function AuditLogView')); console.log('has getToken:', ts.includes('getToken'))"</automated>
  </verify>
  <done>
`admin-dashboard/src/pages/AuditLogView.tsx` exists with >= 120 lines. Exports `AuditLogView`
named function. Imports `getToken` from `../services/strapiClient`. Contains filter inputs
for date range and workflow name. Contains table with status badge logic. Has pagination.
  </done>
</task>

<task type="auto">
  <name>Task 2: Register route and nav item in App.tsx</name>
  <files>admin-dashboard/src/App.tsx</files>
  <action>
Edit `admin-dashboard/src/App.tsx` to add the audit log route and sidebar navigation item.

**Change 1 — Import:**
Add to the existing lucide-react import (around line 30):
```typescript
  FileText,
```
(Add `FileText` to the destructured imports from `lucide-react`.)

**Change 2 — Import AuditLogView:**
Add after the last page import (near line 19, after `ControlPlaneView` import):
```typescript
import { AuditLogView } from './pages/AuditLogView';
```

**Change 3 — Add route:**
Inside the `{isFullAdmin && (` block that contains the admin routes (around line 277-286),
add after the `/brand` route:
```typescript
<Route path="/audit-log" element={<ViewWrapper activeKey="audit-log" component={<AuditLogView />} />} />
```

**Change 4 — Add header title:**
In the header section (around line 240-250), add:
```typescript
{activeTab === 'audit-log' && "Audit Trail"}
```
after the existing `{activeTab === 'growth' && 'Growth Intelligence'}` line.

**Change 5 — Add sidebar nav item:**
Inside the `{isFullAdmin && (` Advanced section (around line 173-179), after the "DNA Studio"
NavItem:
```typescript
<NavItem active={activeTab === 'audit-log'} onClick={() => { navigate('/audit-log'); setIsMobileMenuOpen(false); }} label="Audit Trail" icon={FileText} lang={lang} />
```
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('admin-dashboard/src/App.tsx','utf8'); console.log('has route:', s.includes('audit-log')); console.log('has import:', s.includes('AuditLogView')); console.log('has FileText:', s.includes('FileText'))"</automated>
  </verify>
  <done>
`admin-dashboard/src/App.tsx` contains `AuditLogView` import, `FileText` in lucide imports,
`/audit-log` route inside isFullAdmin block, and "Audit Trail" NavItem in the Advanced section.
  </done>
</task>

</tasks>

<verification>
0. PREREQUISITE — Confirm nginx /v1/internal/ proxy block exists and the audit-log path resolves
   before deploying the dashboard component:
   a. Check nginx.conf has a location block for /v1/internal/:
      `grep -n "v1/internal" infra/gateway/nginx.conf`
      Expected: at least one location block matching /v1/internal/ with proxy_pass to n8n.
   b. After gateway reload, confirm no 502 on audit-log path (W_AUDIT_QUERY must be active on VPS):
      `curl -s -o /dev/null -w "%{http_code}" https://api.srv1258231.hstgr.cloud/v1/internal/audit-log`
      Expected: 200 or 401/403, NOT 502 (502 means nginx proxy block is missing or n8n is unreachable).
   If this check fails (502), do NOT proceed to dashboard deployment — fix nginx.conf first by adding:
   ```nginx
   location /v1/internal/ {
     proxy_pass http://n8n-main:5678/webhook/;
     proxy_set_header Host $host;
     proxy_set_header X-Real-IP $remote_addr;
   }
   ```

1. AuditLogView.tsx exists and has > 120 lines:
   `wc -l admin-dashboard/src/pages/AuditLogView.tsx`

2. App.tsx has all required additions:
   `grep -c "audit-log\|AuditLogView\|FileText" admin-dashboard/src/App.tsx` returns >= 4

3. TypeScript compiles without errors (run in admin-dashboard directory):
   `cd admin-dashboard && npx tsc --noEmit 2>&1 | head -20`
   (should show no errors for these two files)

4. Vite build passes:
   `cd admin-dashboard && npm run build 2>&1 | tail -5`
   (should show build success)
</verification>

<success_criteria>
- nginx.conf contains a `location /v1/internal/` block proxying to n8n, confirmed by grep before
  dashboard deployment
- `curl GET /v1/internal/audit-log` returns non-502 (gateway can reach n8n webhook endpoint)
- After rebuilding and deploying the admin dashboard, navigating to
  `https://admin.srv1258231.hstgr.cloud` and logging in shows "Audit Trail" in the Advanced
  section of the sidebar
- Clicking it loads the /audit-log route showing the filter controls and table
- With real audit data in the DB (after Plan 04 inbound adapters are patched and active),
  the table shows rows with correct status badges and duration values
- Date range filter and workflow name filter both narrow the results
- Previous/Next pagination works
</success_criteria>

<output>
After completion, create `.planning/phases/03-metrics-alerting-and-audit-trail/03-05-SUMMARY.md`
following the summary template format.
</output>
