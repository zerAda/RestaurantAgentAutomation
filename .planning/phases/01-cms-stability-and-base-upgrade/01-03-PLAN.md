---
phase: 01-cms-stability-and-base-upgrade
plan: 03
type: execute
wave: 2
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - project/TEST_REPORT.md
autonomous: false
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03

must_haves:
  truths:
    - "admin-dashboard/Dockerfile line 1 reads FROM node:20-alpine (INFRA-01 verified)"
    - "kiosk-app/Dockerfile line 1 reads FROM node:20-alpine (INFRA-01 verified)"
    - "inventory-cms/Dockerfile lines 4 and 33 read FROM node:20-alpine (INFRA-02 verified)"
    - "Admin dashboard login returns JWT (rebuilt image works, INFRA-03 verified)"
    - "Kiosk product endpoint returns product data (rebuilt image works, INFRA-03 verified)"
    - "TEST_REPORT.md static check rows updated with PASS for all three Dockerfiles"
  artifacts:
    - path: "project/admin-dashboard/Dockerfile"
      provides: "Node.js 20-alpine base image for admin dashboard"
      contains: "node:20-alpine"
    - path: "project/kiosk-app/Dockerfile"
      provides: "Node.js 20-alpine base image for kiosk app"
      contains: "node:20-alpine"
    - path: "project/inventory-cms/Dockerfile"
      provides: "Node.js 20-alpine base image for CMS (both stages)"
      contains: "node:20-alpine"
    - path: "project/TEST_REPORT.md"
      provides: "Phase 1 static check results filled in"
      contains: "PASS"
  key_links:
    - from: "project/admin-dashboard/Dockerfile"
      to: "node:20-alpine"
      via: "FROM instruction line 1"
      pattern: "FROM node:20-alpine"
    - from: "project/kiosk-app/Dockerfile"
      to: "node:20-alpine"
      via: "FROM instruction line 1"
      pattern: "FROM node:20-alpine"
    - from: "project/inventory-cms/Dockerfile"
      to: "node:20-alpine"
      via: "FROM instruction lines 4 and 33"
      pattern: "FROM node:20-alpine"
---

<objective>
Verify the Node.js LTS upgrade is complete across all three service Dockerfiles (INFRA-01, INFRA-02) and confirm the rebuilt frontend images pass functional smoke checks (INFRA-03). Update TEST_REPORT.md with results.

Purpose: Research confirmed all three Dockerfiles already use node:20-alpine. This plan formally verifies and documents that fact, and runs the INFRA-03 functional check against the live stack (admin login + kiosk products).

Output:
- Static grep confirmation of node:20-alpine in all three Dockerfiles
- INFRA-03 functional verification via smoke-post-rebuild.sh
- TEST_REPORT.md static check rows updated with PASS
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-cms-stability-and-base-upgrade/01-RESEARCH.md
@.planning/phases/01-cms-stability-and-base-upgrade/01-01-SUMMARY.md

<interfaces>
<!-- Dockerfile locations and expected content. Source: RESEARCH.md "INFRA Status" section. -->

admin-dashboard/Dockerfile:
  Line 1: FROM node:20-alpine AS build
  (confirmed by direct file read 2026-03-18)

kiosk-app/Dockerfile:
  Line 1: FROM node:20-alpine AS build
  (confirmed by direct file read 2026-03-18)

inventory-cms/Dockerfile:
  Line 4: FROM node:20-alpine AS build    (build stage)
  Line 33: FROM node:20-alpine            (production stage)
  (confirmed by direct file read 2026-03-18)

Static check commands (run from repo root):
  grep "FROM node:20" project/admin-dashboard/Dockerfile
  grep "FROM node:20" project/kiosk-app/Dockerfile
  grep "FROM node:20" project/inventory-cms/Dockerfile

INFRA-03 functional checks (run after Plan 02 rebuild completes):
  bash project/scripts/smoke-post-rebuild.sh
  (covers CMS health, admin login via gateway, kiosk products via gateway)
  Kiosk: GET https://api.srv1258231.hstgr.cloud/v1/strapi/api/products  → 200 + data
  Admin login: POST https://api.srv1258231.hstgr.cloud/v1/portal/api/auth/local → 200 + jwt
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Static verification of Dockerfile base images</name>
  <files>project/TEST_REPORT.md</files>
  <action>
Run the static checks locally (no VPS needed):

```bash
grep "FROM node:20" project/admin-dashboard/Dockerfile
grep "FROM node:20" project/kiosk-app/Dockerfile
grep "FROM node:20" project/inventory-cms/Dockerfile
```

Each command must return at least one match. Record each result.

Then read project/TEST_REPORT.md. Find the "Static Checks (Node.js Base Images)" table in the Phase 1 section. Update each PENDING row with the actual grep output and mark as PASS. Write the updated file back with all prior content preserved.

Expected output for each grep:
- admin-dashboard: "FROM node:20-alpine AS build"
- kiosk-app: "FROM node:20-alpine AS build"
- inventory-cms: "FROM node:20-alpine AS build" (appears twice — build + production stages)

If any grep returns empty (no match), mark that row FAIL and note the actual FROM line found.
  </action>
  <verify>
    <automated>grep -c "FROM node:20-alpine" "$(pwd)/project/admin-dashboard/Dockerfile" && grep -c "FROM node:20-alpine" "$(pwd)/project/kiosk-app/Dockerfile" && grep -c "FROM node:20-alpine" "$(pwd)/project/inventory-cms/Dockerfile"</automated>
  </verify>
  <done>
    All three grep commands return at least one match. TEST_REPORT.md static check rows updated with PASS (no longer PENDING). INFRA-01 and INFRA-02 are confirmed satisfied.
  </done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: Verify INFRA-03 functional checks</name>
  <files>project/TEST_REPORT.md</files>
  <action>
Admin dashboard and kiosk-app images built from node:20-alpine. Plan 02 rebuilt the CMS. The smoke-post-rebuild.sh script (created in Plan 01) now needs to run against the live stack to confirm admin login and kiosk product display work end-to-end.
  

Verification steps:
Run the post-rebuild smoke script from the VPS (or locally if the services are accessible):

```bash
# From VPS at /opt/resto/current/
STRAPI_EMAIL=adel.zeriri@gmail.com \
STRAPI_PASSWORD=RestoBot2026 \
bash project/scripts/smoke-post-rebuild.sh

# Also verify directly from browser or curl:
# 1. Admin dashboard: https://admin.srv1258231.hstgr.cloud
#    - Traefik BasicAuth: admin / dev_local_password_not_for_prod_32c
#    - App login: adel.zeriri@gmail.com / RestoBot2026
#    - Expect: dashboard loads, products/orders visible in sidebar

# 2. Kiosk: https://kiosk.srv1258231.hstgr.cloud
#    - Expect: product cards visible (at least 6 items)

# 3. CMS health: curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:1337/_health
#    Expect: 204
```

Expected smoke output:
```
PASS  CMS health (204)
PASS  CMS login (JWT obtained)
PASS  kiosk products via gateway (200, data present)
PASS  admin login via gateway (200, JWT)
Results: 4/4 passed
```
  </action>
  <verify>
    <automated>bash project/scripts/smoke-post-rebuild.sh 2>/dev/null || echo "Run smoke-post-rebuild.sh on VPS"</automated>
  </verify>
  <done>Admin login returns JWT (200), kiosk products return data (200), CMS health returns 204. Operator confirms INFRA-03 pass.</done>
  <resume-signal>Type "infra-verified: pass" if all 4 checks pass, or describe any failures for investigation.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Mark INFRA-03 results in TEST_REPORT.md</name>
  <files>project/TEST_REPORT.md</files>
  <action>
Read project/TEST_REPORT.md. Find the Phase 1 section. Update the INFRA-03 / post-rebuild smoke row with the actual result from the checkpoint above.

If all checks passed: update the "Admin login + kiosk products" row to "PASS — 4/4 checks" with date 2026-03-18.

If any check failed: mark FAIL with the failure description, so the gap is visible for future investigation.

Write the updated file back with all prior content preserved.

Also add a "Phase 1 Completion" line at the end of the Phase 1 section:
```
### Phase 1 Summary
INFRA-01: PASS (admin-dashboard, kiosk-app Dockerfiles use node:20-alpine)
INFRA-02: PASS (inventory-cms Dockerfile uses node:20-alpine, both stages)
INFRA-03: [PASS/FAIL] (admin login, kiosk products, CMS health)
CMS-01: PASS (all 42 content type TS source files verified present)
CMS-02: [PASS/FAIL] (clean --no-cache rebuild completed, see Plan 02)
CMS-03: [PASS/FAIL] (all 15 routes respond correctly post-rebuild)
```
  </action>
  <verify>
    <automated>grep -c "Phase 1 Summary" "$(pwd)/project/TEST_REPORT.md"</automated>
  </verify>
  <done>
    TEST_REPORT.md Phase 1 section is complete — all PENDING rows filled in, Phase 1 Summary section added with per-requirement PASS/FAIL status.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. grep "FROM node:20-alpine" project/admin-dashboard/Dockerfile — returns match
2. grep "FROM node:20-alpine" project/kiosk-app/Dockerfile — returns match
3. grep "FROM node:20-alpine" project/inventory-cms/Dockerfile — returns 2 matches (build + prod stage)
4. grep "Phase 1 Summary" project/TEST_REPORT.md — returns match
5. grep "PENDING" project/TEST_REPORT.md — returns nothing (all rows resolved)
</verification>

<success_criteria>
- INFRA-01 confirmed: admin-dashboard and kiosk-app Dockerfiles use node:20-alpine (static grep)
- INFRA-02 confirmed: inventory-cms Dockerfile uses node:20-alpine in both build and production stages
- INFRA-03 confirmed: admin login + kiosk products functional after rebuild (smoke-post-rebuild.sh passes)
- TEST_REPORT.md Phase 1 section fully populated (no PENDING rows, summary added)
</success_criteria>

<output>
After completion, create `.planning/phases/01-cms-stability-and-base-upgrade/01-03-SUMMARY.md`
</output>
