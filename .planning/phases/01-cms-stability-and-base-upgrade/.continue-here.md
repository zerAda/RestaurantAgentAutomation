---
phase: 01-cms-stability-and-base-upgrade
plan: 04
task: 1
total_tasks: 3
status: in_progress
last_updated: 2026-03-23T17:57:20.176Z
---

<current_state>
Plan 01-04 Task 1 — VPS CMS rebuild. 6 rebuilds done (this session).
lodash/fp fixed (19 nested installs all patched via fix-lodash-fp.js).
New blocker: `fs-extra` named ESM import fails same way as lodash/fp.
</current_state>

<completed_work>

**All prior bugs fixed (committed to main):**
- auth: true/string invalid in Strapi 5 → auth: false
- allowJs: true added to tsconfig.json
- lodash named import in @strapi/core dist → sed fix kept
- lodash/fp: 19 nested installs patched with ESM wrapper + exports map (fix-lodash-fp.js script)

**This session commits:** 3025c89, 1c148dc, 10c34d6, 5f53f96, 5227e62

**Plans 01-01, 01-02, 01-03 have SUMMARY.md. Only 01-04 remains.**
</completed_work>

<remaining_work>

**BLOCKER — extend fix-lodash-fp.js to patch fs-extra:**

Same pattern as lodash fix. In `inventory-cms/fix-lodash-fp.js`, after the existing
`dirs.forEach(patchLodash)` section, add:

```js
// Also patch fs-extra (same CJS dynamic exports problem)
function patchFsExtra(dir) {
  var fse = require(dir + '/index.js');
  var keys = Object.keys(fse);
  var lines = ["import _fse from './index.js';", "export default _fse;"];
  keys.forEach(function(k) {
    if (/^[a-zA-Z_$][a-zA-Z0-9_$]*$/.test(k)) {
      lines.push('export const ' + k + ' = _fse.' + k + ';');
    }
  });
  fs.writeFileSync(path.join(dir, 'index.mjs'), lines.join('\n') + '\n');
  var pkgPath = path.join(dir, 'package.json');
  var pkg = JSON.parse(fs.readFileSync(pkgPath, 'utf8'));
  pkg.exports = {
    '.': { 'import': './index.mjs', 'require': './index.js' },
    './index': { 'import': './index.mjs', 'require': './index.js' },
    './index.js': { 'import': './index.mjs', 'require': './index.js' }
  };
  fs.writeFileSync(pkgPath, JSON.stringify(pkg, null, 2) + '\n');
  console.log('Patched fs-extra: ' + dir.replace('/app/node_modules/', '') + ' (' + keys.length + ' exports)');
}

function findFsExtraDirs(startNM) {
  var results = [];
  // Same scan approach as lodash
  function scanNM(nmDir, depth) {
    if (depth > 5) return;
    var entries;
    try { entries = fs.readdirSync(nmDir, { withFileTypes: true }); } catch(e) { return; }
    entries.forEach(function(e) {
      if (!e.isDirectory()) return;
      var pkgDir = path.join(nmDir, e.name);
      if (e.name === 'fs-extra') {
        if (fs.existsSync(path.join(pkgDir, 'index.js'))) results.push(pkgDir);
      } else if (e.name.charAt(0) === '@') {
        var se; try { se = fs.readdirSync(pkgDir, { withFileTypes: true }); } catch(er) { return; }
        se.forEach(function(s) {
          if (!s.isDirectory()) return;
          var nested = path.join(pkgDir, s.name, 'node_modules');
          if (fs.existsSync(nested)) scanNM(nested, depth + 1);
        });
      } else {
        var nested = path.join(pkgDir, 'node_modules');
        if (fs.existsSync(nested)) scanNM(nested, depth + 1);
      }
    });
  }
  scanNM(startNM, 0);
  return results;
}

var fsExtraDirs = findFsExtraDirs('/app/node_modules');
fsExtraDirs.forEach(patchFsExtra);
console.log('Total fs-extra installs patched: ' + fsExtraDirs.length);
```

Then: `git add + commit` → `scp fix-lodash-fp.js deploy@72.60.190.192:/opt/resto/current/inventory-cms/` → rebuild on VPS (fast, cached layers) → check health → if another package fails, apply same pattern.

**After CMS healthy (204):**

Task 2 — run smoke scripts on VPS:
```bash
ssh deploy@72.60.190.192
cd /opt/resto/current/
STRAPI_EMAIL=adel.zeriri@gmail.com STRAPI_PASSWORD=RestoBot2026 bash scripts/smoke-cms-routes.sh http://127.0.0.1:1337
STRAPI_EMAIL=adel.zeriri@gmail.com STRAPI_PASSWORD=RestoBot2026 bash scripts/smoke-post-rebuild.sh
```

Task 3 — Update `TEST_REPORT.md` with smoke results. Create `01-04-SUMMARY.md`.
</remaining_work>

<decisions_made>

- Node.js 20.20.0 loadESMFromCJS is root cause: dist/src/index.js (compiled TS, ESM syntax) is require()d by Strapi CJS core, loaded as ESM, chain-loads other ESM imports that fail on named CJS imports
- Fix pattern: create .mjs wrapper with explicit named exports + package.json exports map pointing to .mjs for ESM and original .js for CJS
- Applied to lodash (19 nested installs). Same pattern for fs-extra. May need more packages.
- fix-lodash-fp.js is the vehicle in Dockerfile (COPY + RUN). Extend it per-package.
</decisions_made>

<blockers>
- **fs-extra** named ESM import fails — extend fix-lodash-fp.js as above
- More packages may follow — check error log after each rebuild
</blockers>

<context>
VPS: deploy@72.60.190.192, path /opt/resto/current/
Compose file: docker-compose.hostinger.prod.yml
CMS logs: `docker logs current-cms-1 --tail 30 2>&1 | grep error`
Build: `nohup docker compose -f docker-compose.hostinger.prod.yml build cms > /tmp/cms-build8.log 2>&1 &`
Build is fast (~3 min) because npm ci layers are cached — only COPY fix-lodash-fp.js + RUN node + downstream layers re-run.
</context>

<next_action>
Edit inventory-cms/fix-lodash-fp.js to add fs-extra patching (code above). Commit. SCP to VPS. Rebuild. Check health. Repeat for any new package errors.
</next_action>
