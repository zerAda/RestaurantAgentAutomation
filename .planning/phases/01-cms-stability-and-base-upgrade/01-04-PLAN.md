---
phase: 01-cms-stability-and-base-upgrade
plan: 04
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
  - "01-03"
files_modified:
  - project/TEST_REPORT.md
autonomous: false
gap_closure: true
requirements:
  - CMS-02
  - CMS-03
  - INFRA-03

must_haves:
  truths:
    - "CMS image rebuilt from node:20.20.0-alpine Dockerfile starts successfully on VPS"
    - "CMS health endpoint returns HTTP 204 after rebuild"
    - "All 15 Strapi API routes return 200 when tested with smoke-cms-routes.sh"
    - "Admin login via gateway returns JWT (200)"
    - "Kiosk products via gateway returns data array (200)"
    - "TEST_REPORT.md Phase 1 rows updated from BLOCKED/DEFERRED to actual PASS/FAIL"
  artifacts:
    - path: "project/TEST_REPORT.md"
      provides: "Phase 1 smoke results with actual PASS/FAIL (no BLOCKED or DEFERRED rows)"
      contains: "Results:"
  key_links:
    - from: "docker compose build cms --no-cache"
      to: "project/inventory-cms/Dockerfile (node:20.20.0-alpine)"
      via: "Docker BuildKit reads pinned base image"
      pattern: "node:20.20.0-alpine"
    - from: "project/scripts/smoke-cms-routes.sh"
      to: "http://127.0.0.1:1337/api/*"
      via: "curl with Bearer JWT auth"
      pattern: "Authorization.*Bearer"
    - from: "project/scripts/smoke-post-rebuild.sh"
      to: "http://127.0.0.1:1337/_health"
      via: "curl expects HTTP 204"
      pattern: "_health"

user_setup:
  - service: vps-ssh
    why: "All tasks require SSH to production VPS"
    env_vars: []
    dashboard_config:
      - task: "Ensure SSH key is loaded for deploy@72.60.190.192"
        location: "Local SSH agent or ~/.ssh/config"
---

<objective>
Close the 3 remaining Phase 1 verification gaps by executing the corrected CMS rebuild on the VPS, running both smoke scripts, and recording results in TEST_REPORT.md.

Purpose: All code fixes are already committed (node:20.20.0-alpine pin in Dockerfile). The only remaining work is VPS execution: rebuild, verify, document. One SSH session closes CMS-02, CMS-03, and INFRA-03 simultaneously.

Output:
- CMS container running from rebuilt image (node:20.20.0-alpine) with all 15 routes responding
- smoke-cms-routes.sh results recorded in TEST_REPORT.md (replacing BLOCKED rows)
- smoke-post-rebuild.sh results recorded in TEST_REPORT.md (replacing DEFERRED INFRA-03 row)
- Phase 1 Summary updated with final PASS/FAIL per requirement
</objective>

<execution_context>
@C:/Users/mon pc/Desktop/ralphé_final_patch/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/mon pc/Desktop/ralphé_final_patch/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-cms-stability-and-base-upgrade/01-VERIFICATION.md
@.planning/phases/01-cms-stability-and-base-upgrade/01-01-SUMMARY.md
@.planning/phases/01-cms-stability-and-base-upgrade/01-02-SUMMARY.md
@.planning/phases/01-cms-stability-and-base-upgrade/01-03-SUMMARY.md
@project/TEST_REPORT.md

<interfaces>
<!-- VPS connection and commands. All tasks run on the VPS via SSH. -->

VPS:
  Host: 72.60.190.192
  User: deploy
  Project path: /opt/resto/current/
  Compose file: docker-compose.hostinger.prod.yml

CMS credentials (for smoke scripts):
  STRAPI_EMAIL=adel.zeriri@gmail.com
  STRAPI_PASSWORD=RestoBot2026

Disk safety threshold: 10GB free minimum before build.

Build command:
  cd /opt/resto/current/ && docker compose -f docker-compose.hostinger.prod.yml build cms --no-cache 2>&1 | tee /tmp/cms-build.log

Start command:
  docker compose -f docker-compose.hostinger.prod.yml up -d cms

Gateway DNS flush (REQUIRED after CMS restart):
  docker exec current-gateway-1 nginx -s reload

Health wait loop:
  until curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:1337/_health | grep -q "204"; do echo "Waiting for CMS... $(date)"; sleep 15; done

Smoke commands:
  STRAPI_EMAIL=adel.zeriri@gmail.com STRAPI_PASSWORD=RestoBot2026 bash /opt/resto/current/project/scripts/smoke-cms-routes.sh http://127.0.0.1:1337
  STRAPI_EMAIL=adel.zeriri@gmail.com STRAPI_PASSWORD=RestoBot2026 bash /opt/resto/current/project/scripts/smoke-post-rebuild.sh

Expected smoke-cms-routes.sh output (17 checks):
  PASS  products  (200)
  PASS  orders  (200)
  ... (13 collectionType + 2 singleType + 2 custom handlers)
  Results: 17/17 passed

Expected smoke-post-rebuild.sh output (4 checks):
  PASS  CMS health (204)
  PASS  CMS login (JWT obtained)
  PASS  kiosk products via gateway (200, data present)
  PASS  admin login via gateway (200, JWT)
  Results: 4/4 passed
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: VPS disk check, CMS rebuild, and health verification</name>
  <files></files>
  <read_first>
    - project/inventory-cms/Dockerfile (confirm node:20.20.0-alpine is present before building)
    - .planning/phases/01-cms-stability-and-base-upgrade/01-02-SUMMARY.md (understand prior failure)
  </read_first>
  <action>
SSH to VPS and execute the full rebuild sequence. This is a single uninterrupted session.

Step 1 — Disk check:
```bash
ssh deploy@72.60.190.192
df -h /
```
If less than 10GB free:
```bash
docker system prune -f
df -h /
# If still < 10GB:
docker image prune -a
df -h /
```
Do NOT proceed if less than 10GB free after pruning.

Step 2 — Verify Dockerfile is correct on VPS:
```bash
cd /opt/resto/current/
git -C project/ log --oneline -3
head -5 project/inventory-cms/Dockerfile
# MUST show: FROM node:20.20.0-alpine (NOT node:20-alpine)
# If it shows node:20-alpine, pull latest: git -C project/ pull origin main
```

Step 3 — Build CMS image (15-30 min):
```bash
docker compose -f docker-compose.hostinger.prod.yml build cms --no-cache 2>&1 | tee /tmp/cms-build.log
echo "Build exit code: $?"
```
If build fails: check `/tmp/cms-build.log` tail for error. Common failures:
- npm ci timeout -> retry the build command
- ENOSPC -> abort, free more disk, retry
- TypeScript error -> report exact error message

Step 4 — Start CMS container:
```bash
docker compose -f docker-compose.hostinger.prod.yml up -d cms
```

Step 5 — Flush nginx DNS cache (REQUIRED):
```bash
docker exec current-gateway-1 nginx -s reload
```

Step 6 — Wait for CMS health:
```bash
until curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:1337/_health | grep -q "204"; do
  echo "Waiting for CMS... $(date)"
  sleep 15
done
echo "CMS healthy at $(date)"
```
Cold start can take 5-8 minutes (DB migration check on first boot).

Step 7 — Confirm:
```bash
curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:1337/_health
# MUST output: 204
docker logs current-cms-1 --tail 20
# Should show Strapi startup complete, no errors
```
  </action>
  <verify>
    <automated>MISSING — VPS-only. Operator confirms CMS health 204.</automated>
  </verify>
  <acceptance_criteria>
    - `curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:1337/_health` returns `204`
    - `docker logs current-cms-1 --tail 5` shows Strapi startup with no ERR_UNSUPPORTED_DIR_IMPORT
    - `docker inspect current-cms-1 --format '{{.Config.Image}}'` shows an image built today (not the old 44cf772ff9b2)
    - Build log in `/tmp/cms-build.log` shows successful completion (no error exit)
  </acceptance_criteria>
  <done>CMS container rebuilt from node:20.20.0-alpine, started, and returning HTTP 204 on /_health. No ESM regression error in logs.</done>
  <resume-signal>Type "rebuild-done: healthy" with the health check output (204), or "rebuild-failed: [error]" if it failed.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Run smoke scripts and capture output</name>
  <files></files>
  <read_first>
    - project/scripts/smoke-cms-routes.sh (understand what it tests)
    - project/scripts/smoke-post-rebuild.sh (understand what it tests)
  </read_first>
  <action>
On the VPS (still in the SSH session from Task 1), run both smoke scripts and capture their full output.

Script 1 — CMS route smoke (CMS-03):
```bash
cd /opt/resto/current/
STRAPI_EMAIL=adel.zeriri@gmail.com \
STRAPI_PASSWORD=RestoBot2026 \
bash project/scripts/smoke-cms-routes.sh http://127.0.0.1:1337
echo "Exit code: $?"
```
Expected: All 15+ routes show PASS, exit code 0.

If any route shows FAIL:
- 404 = route not registered (content type missing from TS source — report which one)
- 401/403 = auth issue (check JWT was obtained in the first step of the script)
- 502 = gateway issue (already flushed nginx in Task 1, but retry: `docker exec current-gateway-1 nginx -s reload`)

Script 2 — Post-rebuild smoke (INFRA-03):
```bash
STRAPI_EMAIL=adel.zeriri@gmail.com \
STRAPI_PASSWORD=RestoBot2026 \
bash project/scripts/smoke-post-rebuild.sh
echo "Exit code: $?"
```
Expected:
```
PASS  CMS health (204)
PASS  CMS login (JWT obtained)
PASS  kiosk products via gateway (200, data present)
PASS  admin login via gateway (200, JWT)
Results: 4/4 passed
```

Copy and paste the FULL output of both scripts (every PASS/FAIL line and the Results summary line).
  </action>
  <verify>
    <automated>MISSING — VPS-only. Operator pastes smoke output.</automated>
  </verify>
  <acceptance_criteria>
    - smoke-cms-routes.sh output contains "Results:" line with pass count
    - smoke-cms-routes.sh exit code is 0 (all routes passed)
    - smoke-post-rebuild.sh output contains "Results: 4/4 passed"
    - smoke-post-rebuild.sh exit code is 0
    - No FAIL lines in either script output
  </acceptance_criteria>
  <done>Both smoke scripts ran successfully. Full output captured for TEST_REPORT.md update.</done>
  <resume-signal>Paste the full output of both smoke scripts. Claude will use this to update TEST_REPORT.md in Task 3.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Update TEST_REPORT.md with actual results and finalize Phase 1 Summary</name>
  <files>project/TEST_REPORT.md</files>
  <read_first>
    - project/TEST_REPORT.md (current state with BLOCKED/DEFERRED rows)
  </read_first>
  <action>
Read project/TEST_REPORT.md. Make these specific updates in the Phase 1 section:

1. Replace the BLOCKED row for "All 15 CMS routes return 200":
   Old: `| All 15 CMS routes return 200 | ... | BLOCKED — see note |`
   New: `| All 15 CMS routes return 200 | ... | PASS — {N}/{M} routes passed (exit 0) |`
   Use the actual counts from the smoke-cms-routes.sh output provided in Task 2.

2. Replace the BLOCKED row for "Admin login + kiosk products":
   Old: `| Admin login + kiosk products | ... | BLOCKED — see note |`
   New: `| Admin login + kiosk products | ... | PASS — 4/4 checks passed (exit 0) |`
   Use the actual results from smoke-post-rebuild.sh output provided in Task 2.

3. Update the "New CMS image built from TS source" row:
   Old: `| New CMS image built from TS source | ... | PASS — image 44cf772ff9b2 (3.36GB, 2026-03-18) |`
   New: `| New CMS image built from TS source | ... | PASS — image rebuilt with node:20.20.0-alpine ({date}), starts successfully |`

4. Remove or update the "BLOCKED note" paragraph since it no longer applies.

5. Update the Phase 1 Summary section:
   - CMS-02: change from "PARTIAL" to "PASS — clean rebuild with node:20.20.0-alpine, container starts and routes respond"
   - CMS-03: change from "DEFERRED" to "PASS — {N}/{M} routes verified via smoke-cms-routes.sh"
   - INFRA-03: change from "DEFERRED" to "PASS — admin login, kiosk products, CMS health all verified via smoke-post-rebuild.sh"

6. Add a gap closure note at the end of the Phase 1 section:
   ```
   ### Gap Closure (Plan 01-04, {date})
   - CMS-02: CLOSED — rebuild with node:20.20.0-alpine succeeded
   - CMS-03: CLOSED — all routes respond correctly post-rebuild
   - INFRA-03: CLOSED — admin login + kiosk products verified end-to-end
   ```

Write the full updated file preserving all content outside the Phase 1 section.
  </action>
  <verify>
    <automated>grep -c "BLOCKED" "C:/Users/mon pc/Desktop/ralphé_final_patch/project/TEST_REPORT.md" | grep -q "^0$" && grep -c "DEFERRED" "C:/Users/mon pc/Desktop/ralphé_final_patch/project/TEST_REPORT.md" | grep -q "^0$" && echo "No BLOCKED or DEFERRED rows remain"</automated>
  </verify>
  <acceptance_criteria>
    - Zero occurrences of "BLOCKED" in TEST_REPORT.md Phase 1 section
    - Zero occurrences of "DEFERRED" in TEST_REPORT.md Phase 1 section
    - CMS-02 row in Phase 1 Summary shows "PASS"
    - CMS-03 row in Phase 1 Summary shows "PASS"
    - INFRA-03 row in Phase 1 Summary shows "PASS"
    - "Gap Closure" subsection present with all 3 gaps marked CLOSED
    - All content below Phase 1 section (v3.4.4, v3.4.3, etc.) preserved unchanged
  </acceptance_criteria>
  <done>TEST_REPORT.md Phase 1 section fully populated with actual results. No BLOCKED, DEFERRED, or PENDING rows remain. Phase 1 Summary shows all 6 requirements as PASS.</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. On VPS: `curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:1337/_health` returns 204
2. On VPS: `STRAPI_EMAIL=adel.zeriri@gmail.com STRAPI_PASSWORD=RestoBot2026 bash project/scripts/smoke-cms-routes.sh http://127.0.0.1:1337` exits 0
3. On VPS: `STRAPI_EMAIL=adel.zeriri@gmail.com STRAPI_PASSWORD=RestoBot2026 bash project/scripts/smoke-post-rebuild.sh` exits 0
4. Locally: `grep "BLOCKED" project/TEST_REPORT.md` returns nothing
5. Locally: `grep "DEFERRED" project/TEST_REPORT.md` returns nothing
6. Locally: `grep "Gap Closure" project/TEST_REPORT.md` returns a match
</verification>

<success_criteria>
- CMS container rebuilt and healthy (204) on VPS using node:20.20.0-alpine
- All 15 Strapi API routes respond correctly (smoke-cms-routes.sh exits 0)
- Admin login + kiosk products work end-to-end (smoke-post-rebuild.sh exits 0)
- TEST_REPORT.md has no BLOCKED or DEFERRED rows — all replaced with actual PASS/FAIL
- Phase 1 Summary shows CMS-02: PASS, CMS-03: PASS, INFRA-03: PASS
</success_criteria>

<output>
After completion, create `.planning/phases/01-cms-stability-and-base-upgrade/01-04-SUMMARY.md`
</output>
