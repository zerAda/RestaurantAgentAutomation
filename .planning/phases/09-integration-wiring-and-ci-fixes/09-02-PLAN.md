---
phase: 09-integration-wiring-and-ci-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements: [TEST-03, TEST-04, AUDIT-01]
must_haves:
  truths:
    - "CI smoke-nginx-routing job calls smoke-nginx-routing.sh (the Docker-container burst variant), not smoke-nginx-routing-v2.sh"
    - "CI smoke-nginx-routing job fails with exit 1 when rate-limit burst test detects no 429 (no warning suppression)"
    - "CI smoke-nginx-routing job runs on PRs (not only main/release pushes)"
    - "CI integration-tests (PG15) verifies ops.workflow_audit exists in the ops schema"
    - "CI integration-tests-pg16 verifies ops.workflow_audit exists in the ops schema"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Corrected smoke-nginx-routing job + ops schema verification"
      contains: "smoke-nginx-routing.sh"
  key_links:
    - from: "smoke-nginx-routing job"
      to: "scripts/smoke-nginx-routing.sh"
      via: "bash scripts/smoke-nginx-routing.sh"
      pattern: "smoke-nginx-routing\\.sh"
    - from: "integration-tests Verify schema integrity"
      to: "ops.workflow_audit table"
      via: "psql query with table_schema='ops'"
      pattern: "table_schema='ops'"
---

<objective>
Fix three CI gaps: switch the smoke-nginx-routing job to call the correct burst-test script, enable it on PRs, and add ops.workflow_audit to the schema verification in both integration-tests jobs.

Purpose: The current CI has three blind spots: (1) TEST-03 rate-limit burst test never executes because ci.yml calls smoke-nginx-routing-v2.sh which lacks the 25-POST burst test; (2) TEST-04 smoke only runs on main/release, not on PRs touching nginx.conf; (3) AUDIT-01 ops.workflow_audit migration is not verified in CI because EXPECTED_TABLES only checks the public schema.

Output: Updated `.github/workflows/ci.yml` with all three fixes.
</objective>

<execution_context>
@C:/Users/mon pc/Desktop/ralphé_final_patch/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/mon pc/Desktop/ralphé_final_patch/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-integration-wiring-and-ci-fixes/09-RESEARCH.md

<interfaces>
<!-- Current ci.yml structure (line references) -->

Line 24-28: on: triggers
```yaml
on:
  push:
    branches: [main, develop, "release/*"]
  pull_request:
    branches: [main, develop]
```
No `paths:` filter on pull_request — all CI jobs run on all PRs.

Lines 696-724: smoke-nginx-routing job (CURRENT STATE TO MODIFY)
```yaml
smoke-nginx-routing:
  name: Nginx Routing Smoke
  runs-on: ubuntu-latest
  needs: [integrity-gate, lint-validate]
  if: github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/heads/release/')
  timeout-minutes: 10

  services:
    nginx:
      image: nginx:1.27-alpine
      ports:
        - 8080:8080
      options: >-
        --health-cmd "curl -sf http://localhost:8080/healthz || exit 1"
        --health-interval 10s
        --health-timeout 5s
        --health-retries 5

  steps:
    - name: Checkout code
      uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

    - name: Run nginx routing smoke tests
      run: |
        chmod +x scripts/smoke-nginx-routing-v2.sh
        bash scripts/smoke-nginx-routing-v2.sh "http://localhost:8080" || {
          echo "::warning::Some routing tests failed (expected without live backends)"
        }
```

Lines 342-363: integration-tests (PG15) "Verify schema integrity" step
```yaml
      - name: Verify schema integrity
        env:
          PGPASSWORD: n8npass
        run: |
          EXPECTED_TABLES="tenants restaurants api_clients menu_items orders inbound_messages outbound_messages security_events schema_migrations conversation_state webhook_replay_guard admin_wa_audit_log fraud_rules payment_intents faq_entries support_tickets delivery_zones"
          MISSING=0
          for table in $EXPECTED_TABLES; do
            EXISTS=$(psql -h localhost -U n8n -d n8n -t -c "SELECT 1 FROM information_schema.tables WHERE table_schema='public' AND table_name='$table';" | tr -d ' ')
            if [ "$EXISTS" != "1" ]; then
              echo "::error::Missing table: $table"
              MISSING=$((MISSING + 1))
            fi
          done

          TOTAL=$(psql -h localhost -U n8n -d n8n -t -c "SELECT COUNT(*) FROM information_schema.tables WHERE table_schema='public';" | tr -d ' ')
          echo "::notice::PG${{ matrix.pg_major }}: $TOTAL total tables, $(echo $EXPECTED_TABLES | wc -w) critical verified"
          echo "Migration records: $(psql -h localhost -U n8n -d n8n -t -c "SELECT COUNT(*) FROM schema_migrations;" | tr -d ' ')"

          if [ $MISSING -gt 0 ]; then
            echo "::error::$MISSING critical tables missing"
            exit 1
          fi
```

Lines 472-492: integration-tests-pg16 "Verify schema integrity" step (IDENTICAL structure, same EXPECTED_TABLES)
```yaml
      - name: Verify schema integrity
        env:
          PGPASSWORD: n8npass
        run: |
          EXPECTED_TABLES="tenants restaurants api_clients menu_items orders inbound_messages outbound_messages security_events schema_migrations conversation_state webhook_replay_guard admin_wa_audit_log fraud_rules payment_intents faq_entries support_tickets delivery_zones"
          MISSING=0
          ... (same loop as PG15)
          if [ $MISSING -gt 0 ]; then
            echo "::error::$MISSING critical tables missing"
            exit 1
          fi
```

<!-- SHA-pinned action (do NOT change) -->
actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix smoke-nginx-routing job to use burst-test script and run on PRs</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (full file — lines 696-724 for the smoke job, lines 24-28 for triggers)
    - scripts/smoke-nginx-routing.sh (confirm burst test exists — lines 237-280 per research)
    - .planning/phases/09-integration-wiring-and-ci-fixes/09-RESEARCH.md (Pattern 3 and Example 3)
  </read_first>
  <action>
Edit `.github/workflows/ci.yml` lines 696-724 to replace the `smoke-nginx-routing` job with the following changes:

**Change 1 — Update the `if:` condition (line 700):**

FROM:
```yaml
    if: github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/heads/release/')
```

TO:
```yaml
    if: >-
      github.ref == 'refs/heads/main' ||
      startsWith(github.ref, 'refs/heads/release/') ||
      github.event_name == 'pull_request'
```

This ensures the job runs on ALL PRs (not just main/release pushes). Since `smoke-nginx-routing.sh` uses its own Docker container with `nginx.smoke.conf`, it does not need live backends and is safe to run on any PR. The `paths:` filter cannot be applied at job level in GitHub Actions; running on all PRs is the correct trade-off per the research (the script completes in ~30 seconds).

**Change 2 — Remove the entire `services:` block (lines 703-712):**

DELETE these lines entirely:
```yaml
    services:
      nginx:
        image: nginx:1.27-alpine
        ports:
          - 8080:8080
        options: >-
          --health-cmd "curl -sf http://localhost:8080/healthz || exit 1"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
```

Reason: `smoke-nginx-routing.sh` starts and stops its own Docker container internally. The `services:` block would create a port conflict and is unnecessary.

**Change 3 — Replace the run step (lines 718-723):**

FROM:
```yaml
      - name: Run nginx routing smoke tests
        run: |
          chmod +x scripts/smoke-nginx-routing-v2.sh
          bash scripts/smoke-nginx-routing-v2.sh "http://localhost:8080" || {
            echo "::warning::Some routing tests failed (expected without live backends)"
          }
```

TO:
```yaml
      - name: Run nginx routing smoke tests (burst variant)
        run: |
          chmod +x scripts/smoke-nginx-routing.sh
          bash scripts/smoke-nginx-routing.sh
```

Key differences:
- Calls `smoke-nginx-routing.sh` (has the 25-POST burst test for TEST-03) instead of `smoke-nginx-routing-v2.sh` (no burst test)
- No URL argument needed — script starts its own nginx container
- No `|| { echo warning }` suppression — exit 1 from the script MUST fail CI
- Step name updated to indicate burst variant

The checkout step (line 715-716) stays unchanged: `actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2`
  </action>
  <verify>
    <automated>grep -c "smoke-nginx-routing-v2" .github/workflows/ci.yml | grep "^0$" && grep -c "smoke-nginx-routing.sh" .github/workflows/ci.yml | grep -v "^0$" && grep "pull_request" .github/workflows/ci.yml | grep -c "smoke-nginx" || echo "VERIFY: check grep results above"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "smoke-nginx-routing-v2" .github/workflows/ci.yml` returns 0 (no references to v2 script)
    - `grep "smoke-nginx-routing.sh" .github/workflows/ci.yml` returns at least one match containing `bash scripts/smoke-nginx-routing.sh`
    - `grep -A5 "smoke-nginx-routing:" .github/workflows/ci.yml` does NOT contain `services:` block
    - `grep -A5 "smoke-nginx-routing:" .github/workflows/ci.yml` contains `pull_request` in the `if:` condition
    - `grep "echo.*warning.*routing" .github/workflows/ci.yml` returns no matches (warning suppression removed)
    - The checkout step still uses SHA `11bd71901bbe5b1630ceea73d27597364c9af683` (unchanged)
  </acceptance_criteria>
  <done>CI smoke-nginx-routing job calls smoke-nginx-routing.sh (burst variant), runs on PRs, has no services block, and fails CI on test failure</done>
</task>

<task type="auto">
  <name>Task 2: Add ops.workflow_audit to CI schema verification in both integration-tests jobs</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (lines 342-363 for PG15 schema check, lines 472-492 for PG16 schema check)
    - db/migrations/2026-03-23_p3_workflow_audit.sql (confirm ops schema DDL — table name, schema name)
    - .planning/phases/09-integration-wiring-and-ci-fixes/09-RESEARCH.md (Pattern 4 and Example 4)
  </read_first>
  <action>
Edit `.github/workflows/ci.yml` in TWO places: the `integration-tests` (PG15) and `integration-tests-pg16` jobs' "Verify schema integrity" steps.

**For BOTH jobs, add the following block AFTER the existing `if [ $MISSING -gt 0 ]` exit check and BEFORE the closing of the run block:**

Insert this block after line 363 (PG15) and after line 492 (PG16):

```bash

          # Verify ops schema tables (Phase 3 migration — AUDIT-01)
          OPS_EXPECTED_TABLES="workflow_audit"
          for table in $OPS_EXPECTED_TABLES; do
            EXISTS=$(psql -h localhost -U n8n -d n8n -t -c "SELECT 1 FROM information_schema.tables WHERE table_schema='ops' AND table_name='$table';" | tr -d ' ')
            if [ "$EXISTS" != "1" ]; then
              echo "::error::Missing ops schema table: $table"
              MISSING=$((MISSING + 1))
            else
              echo "ops.$table: OK"
            fi
          done
          if [ $MISSING -gt 0 ]; then
            echo "::error::$MISSING ops schema tables missing"
            exit 1
          fi
```

IMPORTANT: The `MISSING` variable must be reset to 0 before the ops loop OR the ops block must use its own counter. Since the existing block already exits on MISSING > 0 before this code runs, the MISSING variable will be 0 at this point. However, to be safe and explicit, reset it:

```bash
          # Verify ops schema tables (Phase 3 migration — AUDIT-01)
          MISSING=0
          OPS_EXPECTED_TABLES="workflow_audit"
          for table in $OPS_EXPECTED_TABLES; do
            EXISTS=$(psql -h localhost -U n8n -d n8n -t -c "SELECT 1 FROM information_schema.tables WHERE table_schema='ops' AND table_name='$table';" | tr -d ' ')
            if [ "$EXISTS" != "1" ]; then
              echo "::error::Missing ops schema table: $table"
              MISSING=$((MISSING + 1))
            else
              echo "ops.$table: OK"
            fi
          done
          if [ $MISSING -gt 0 ]; then
            echo "::error::$MISSING ops schema tables missing"
            exit 1
          fi
```

This must be added to BOTH:
1. The `integration-tests` job "Verify schema integrity" step (around line 363)
2. The `integration-tests-pg16` job "Verify schema integrity" step (around line 492)

The query uses `table_schema='ops'` (not `'public'`) because `ops.workflow_audit` lives in the `ops` schema, not `public`. The existing `EXPECTED_TABLES` check only queries `table_schema='public'` and would never find it.
  </action>
  <verify>
    <automated>grep -c "table_schema='ops'" .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "table_schema='ops'" .github/workflows/ci.yml` returns exactly 2 (one in PG15 job, one in PG16 job)
    - `grep -c "OPS_EXPECTED_TABLES" .github/workflows/ci.yml` returns exactly 2 (one per job)
    - `grep -c "workflow_audit" .github/workflows/ci.yml` returns at least 2 (the OPS_EXPECTED_TABLES value in each job)
    - `grep "ops schema tables missing" .github/workflows/ci.yml` returns exactly 2 matches (error message in each job)
    - The existing `EXPECTED_TABLES` line (public schema tables) is UNCHANGED in both jobs
  </acceptance_criteria>
  <done>Both integration-tests (PG15) and integration-tests-pg16 jobs verify ops.workflow_audit exists in the ops schema; a failed Phase 3 migration is caught by CI</done>
</task>

</tasks>

<verification>
- `grep -c "smoke-nginx-routing-v2" .github/workflows/ci.yml` returns 0
- `grep -c "smoke-nginx-routing.sh" .github/workflows/ci.yml` returns >= 1
- `grep -c "table_schema='ops'" .github/workflows/ci.yml` returns 2
- `grep -c "OPS_EXPECTED_TABLES" .github/workflows/ci.yml` returns 2
- smoke-nginx-routing job `if:` includes `pull_request`
- smoke-nginx-routing job has no `services:` block
- No warning suppression (`|| { echo warning }`) on the smoke script call
</verification>

<success_criteria>
- CI smoke-nginx-routing job calls the correct script with burst test (TEST-03)
- CI smoke-nginx-routing job runs on PRs (TEST-04)
- CI detects missing ops.workflow_audit in both PG15 and PG16 paths (AUDIT-01)
- No existing CI jobs are broken by these changes
</success_criteria>

<output>
After completion, create `.planning/phases/09-integration-wiring-and-ci-fixes/09-02-SUMMARY.md`
</output>
