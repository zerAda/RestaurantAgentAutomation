# � Database & Workflow Consistency Audit: RUTHLESS EDITION

## � Audit Execution

This is not a suggestion. The following discrepancies represent technical debt that threatens system stability and security. This audit was executed with a "Zero Tolerance" policy for naming drift and logic loopholes.

## 🤬 CRITICAL FAILURES (REMEDIATE OR DIE)

### 1. The `quantity` vs `qty` Idiocy

- **Location**: `W_PAYMENT_CHARGILY.json`
- **Sin**: The workflow developer decided to use `quantity` while the DB architect used `qty`.
- **Consequence**: The "Recalculate Total" logic skips the multiplier entirely. In a production environment, this is effectively a permission for price spoofing. **FIX IT.**

### 2. Monetary Suffix Schizophrenia

- **Finding**: Some tables use `_cents` (correct), others like `payment_intents` use naked `_amount`.
- **Sin**: Inconsistency creates bugs. `INTEGER` isn't enough; the suffix is a contract.
- **Consequence**: Some junior developer WILL pass Euros into a Cents field.

## 🔍 LOOSE ENDS (TIGHTEN THE SCREWS)

### 1. Enum Drift

- **Finding**: `payment_status` in `orders` (text check) vs `payment_status_enum` (type).
- **Sin**: Having two different "truths" for payment status is a recipe for state-machine desync.

## 🤏 MICRO-ERRORS (THE DEVIL IN THE DETAILS)

### 1. Performance Suicide (Missing FK Indices)

- **Finding**: Tables like `restaurants`, `api_clients`, `orders`, and `order_items` have Foreign Keys WITHOUT indices.
- **Sin**: Every time a tenant is deleted or an order is queried by restaurant, the DB does a Sequential Scan. This is amateur hour.
- **Consequence**: System slowdown as data scales.

### 2. String Slop (Missing Normalization)

- **Finding**: No database-level `TRIM()` or `LOWER()` on critical fields like `phone`, `wilaya`, or `commune`.
- **Sin**: Input like `" Algiers "` vs `"Algiers"` will create duplicate logic entries or fail lookups silently.
- **Consequence**: Data fragmentation and "ghost" bugs in the router.

### 3. Timestamp Inaccuracy

- **Finding**: Inconsistent use of `now()`.
- **Sin**: For audit logs, we should ensure transaction-level consistency.

## 🛡️ RUTHLESS ENFORCEMENT PLAN

1. **Workflow Lockdown**: `W_PAYMENT_CHARGILY` must fail HARD on missing fields. No silent defaults.
2. **DB Integrity Triggers**: Implement state-transition guards in `bootstrap.sql`. If a payment is `COMPLETED`, it cannot go back to `PENDING`. No mercy.
3. **The Index Hammer**: Add indices to every single Foreign Key. No exceptions.
4. **The Normalizer**: Add a global `BEFORE INSERT OR UPDATE` trigger to `TRIM` every text column that isn't a long description.

---
*Report generated by Antigravity (Rude Mode) — 2026-02-20*
