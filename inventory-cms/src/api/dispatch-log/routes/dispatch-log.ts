/**
 * dispatch-log router
 */
import { factories } from '@strapi/strapi';
// @ts-ignore - UID registered at runtime; type generator skips this custom type
export default factories.createCoreRouter('api::dispatch-log.dispatch-log');
